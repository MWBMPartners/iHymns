## [unreleased] — alpha

- feat(musicians): **merge/dedup follow-ups — a third credit-table list collapsed, empty survivor fields backfilled on merge, and a create-time duplicate hint** (#1800, follow-on to #1785/#1796). Three of the four items scoped; the fourth assessed and deferred (see below):
  - **`credit_search`'s third table list, collapsed.** `manage/editor/api2.php`'s `credit_search` autocomplete carried its own hand-typed six-table `$kindToTable` map, a THIRD copy of the same fact alongside `ED2_CREDIT_TABLES` (same file) and `MUSICIAN_CREDIT_ROLE_TABLES` (`includes/musician_helpers.php`) — flagged as "discovered, not fixed" in `#1785`'s own guard doc-block. Now reads `MUSICIAN_CREDIT_ROLE_TABLES` directly (its singular keys already match this endpoint's `kind=` convention, so no transform was needed). `tests/php/test-musician-credit-tables-single-list.php` gained a PART C that isolates the `credit_search` case block by source position and asserts no bare table-name literal remains and the shared constant IS referenced — mutation-proven both directions.
  - **COALESCE-fill on merge.** `musicianMergeExecute()` used to silently drop every biographical fact recorded ONLY on the losing side of a merge. It now backfills the survivor's EMPTY `Biography` / `MusicBrainzArtistMBID` / `Disambiguation` / `BirthDate`+`DeathDate` (with precision) from the source before deleting it — never overwriting a non-empty target value. New `musicianMbidColumnExists()` gate joins the existing `musicianProfileColumnsExist()`/`musicianDatePrecisionColumnsExist()` probes (rule #9). The fill runs AFTER the source row's `DELETE`, inside the same transaction the function already owns — `MusicBrainzArtistMBID` carries a UNIQUE key, so filling the target before the source is gone would collide with the source's own still-live value. Identity fields (First/Surname/Suffix/Maiden) are deliberately excluded; ISNI/IPI aren't single columns on this table (multi-row `tblMusicianIdentifiers` children, already covered by the existing `keepIpiIds` carry-over). The report gains an additive `fieldsFilled` key, surfaced in all three call sites' activity logs plus `manage/musicians.php`'s success banner and `api.php`'s JSON response. Runtime-verified against the live scratch DB (17 checks, three disposable-row scenarios, cleaned up with no residue) — `musicianMergeExecute()` owns its own transaction, so this couldn't be wrapped in an outer rollback; verification used explicit cleanup `DELETE`s instead.
  - **Create-time fold-match hint.** The `/manage/musicians` "Add person" drawer's Name field now debounce-checks a new `musicianFindFoldMatches()` helper (reusing the SAME `ihymns_sim_name_normalise()` exact-fold test the duplicate-review page's Bucket A already uses, rule #22) via a new read-only `?action=name_fold_check` endpoint. A soft "possible duplicate of X (view)" hint appears — linking to the matched person's public `/musician/<slug>` page — but never blocks Save. Only wired while the field is editable (Edit mode locks the Name input; renames go through the separate Rename action). New guard `tests/php/test-musician-name-fold-check.php`: source-scan (route wired, hint element rendered) always runs; a behavioural part (exact variant found, unrelated name rejected, `exclude_id` honoured, blank/punctuation-only input never throws) runs against a reachable DB inside a rolled-back transaction. Mutation-proven in both directions.
  - **Deferred: unifying `/manage/musician-duplicates` with `/manage/musicians-bulk-promote`.** Assessed against the `#1215` (`duplicate-songs` absorbing `song-link-suggestions`) precedent and judged NOT a clean, contained merge: the two pages work over fundamentally different candidate universes — bulk-promote finds song-credit names with NO registry row yet (register/merge/skip against an in-use NAME); musician-duplicates finds pairs of EXISTING registry rows that look like the same person (merge/dismiss against a registry ID pair) — a distinction the pages' own code comments already draw explicitly. Unlike `song-link-suggestions`, which was the SAME "compare two songs" concern with a different source, there's no natural absorption here; forcing one would mean reconciling two action vocabularies across ~1,650 combined lines of actively-used bulk POST logic, for a real regression risk. Shipped items above instead; a real unification would need a design pass on the combined candidate/action model before any code moves — a separate, scoped piece of work.

- feat(live-follow): **declare a Quick session's length at "Go Live", and extend a running one mid-service** (#1798, follow-on to #1770/#1792). Today a Quick session auto-closes after a fixed idle window — right for an ad-hoc rehearsal, wrong for a leader covering a whole service including the sermon (a genuinely quiet stretch that would trip the auto-close). Two additions, Service Mode untouched (it's bounded by the scheduled service end, never idle):
  - **Session length at create.** `goLive()` now offers a lightweight duration picker — 30 min / 1 hour / 2 hours / "until I end it" (240, the existing 4-hour hard ceiling) — reusing the established `bootstrap.Modal` pattern already used directly by several `js/modules/*` files (never `app.js`'s `showChoice()`, which only ever offers two options). `live_follow_create` reads the optional `idleTimeoutMins`, clamps it to [5, 240], and — the load-bearing rule — caps it at the host's own ENFORCING org lock when one applies (the org's lock always wins for the host, even when they ask for longer). `serviceMode_resolveIdleTimeoutMins()` grew an additive by-reference out-param (`&$enforcedMinsOut`, default null) that exposes just the enforced layer it already computes internally, so create/extend can each apply the same cap without a second reader of `LiveIdleTimeoutMins`/`EnforceIdleTimeout` (the A3 guard in `test-live-follow-idle.php` polices that single-reader invariant).
  - **New endpoint `live_follow_extend`.** Widens a RUNNING session's window and resets the idle clock to now, so the full chosen window applies from the moment of extending — the fix for "the leader's phone died during the sermon". Authorised for the session's own HOST, an admin/owner of an organisation the HOST belongs to, or a site admin/global_admin — resolved via the HOST's OWN `tblOrganisationMembers` rows, **never the session's own `OrgId`** (Quick sessions keep `OrgId = NULL`, load-bearing for CCLI gating, rule #26). The authorisation DECISION is extracted into `serviceMode_liveFollowExtendAuthorize()` — a real, independently-testable function behind the otherwise-inline dispatcher case, mirroring #1792's `serviceMode_codeOnOtherChannel()` "testable primitive behind a thin case" shape. A HOST extending is capped at their own org's enforced lock (same as create); an org-admin/site-admin extending on the host's behalf is capped only at the global [5, 240] band — they hold the authority to override it. Existence-gated on the #1770 idle-timeout migration (409, not 404/400, so the client can tell "not migrated yet" apart from "wrong code", rule #35); channel-scoped like every other live/service query (rule #26); `validateCsrfRequest()` belt-and-braces (rule #29); rate-limited 60/hour/user (rule #1636 two-call contract). The host bar gained an **Extend** button (same picker, POSTs the new action, toasts "Kept live for …", branches on `409` for "not migrated" vs the generic failure toast).
  - **Org-admin surface.** `/manage/my-organisations` gained a "Members' live sessions" card — active Quick sessions whose host is a member of an org the viewer administers, each with an Extend control hitting the same endpoint — for exactly the dead-phone-mid-sermon case, without needing the leader's own device. Column-existence-gated on `serviceMode_idleColumnsExist()`; hidden entirely pre-migration or with nothing to show.
  - New guard `tests/php/test-live-follow-extend.php` (Part A static: the endpoint's real source contains the channel-scoped resolve, the authorize() call, the [5,240] clamp constants, the idle-columns 409 gate, CSRF + rate-limit pairing; Part B live-DB behavioural: host/org-admin/unrelated-user authorization outcomes via the REAL `serviceMode_liveFollowExtendAuthorize()`, the create-override + enforced-cap formula via the REAL resolver's out-param, out-of-range clamping, and channel scoping — a code belonging only to another channel's session resolves to nothing from this one). Mutation-proven: short-circuiting the org-membership check inside the real authorizer flips the "unrelated user" assertion red (a genuine behavioural catch, not a parallel copy); removing the clamp constants from the real `live_follow_extend` case body flips the static A2 check red. PHP 143→144 / node 53 (unchanged).
  - Docs: `api-docs.yaml` (`live_follow_extend` path + `live_follow_create`'s new `idleTimeoutMins` request field), `help/live-follow.md` + the in-app help topic (the "sermon gap" callout + the new "hasn't been updated for session lengths yet" troubleshooting row).

- feat(admin): **sortable-headers adoption sweep — every admin data-list table, every column, actually wired** (#1786 sweep, closes #1799). The shared multi-column sortable-table module (`js/modules/admin-table-sort.js`, #1786 core) previously reached ~24 admin pages, and even those only keyed some of their columns. This pass:
  - Found and fixed a silent-no-op class of bug on **twelve** already-`cp-sortable`-tagged pages (activity-log, ccli-report, data-health, languages, missing-numbers, notifications, publishers, requests, revisions, tags, tunes, works) that never loaded the module at all — every header click did nothing, with no visible symptom. Same shape as #1799, found on more pages by deriving the check from the tree instead of trusting the existing tag.
  - Completed per-column coverage on four partially-wired tables (feature-gating.php x2, groups.php, licence-types.php, musician-duplicates.php) and fully wired five previously-unsortable pages (api-keys.php, catalogues.php, entitlements.php, my-organisations.php, venues.php) plus two orphaned tables (organisations.php's edit-view Members list, tags.php's canonicalisation-suggestions list).
  - **#1799**: `musicians.php` and `musicians-bulk-promote.php` tagged their table `mus-sortable`, which the module's default selector never matches; `musicians.php` called `bootSortableTables()` into the void, `musicians-bulk-promote.php` never called it at all. Both retagged to `cp-sortable` and fully keyed. Also fixed `musicians.php`'s row id, which read the always-undefined `$p['Id']` instead of `$p['registry_id']` — every row silently collided on `id="mus-person-0"`.
  - New tree-derived, mutation-proven guard: `tests/php/test-admin-tables-sortable.php` (rule #34) — derives its candidate page list from `manage/*.php` minus a documented exclusion allow-list (status/report/dashboard pages), and fails if a table isn't `cp-sortable`, the page doesn't boot the module, or a data column lacks `data-sort-key` outside a small documented exception set.
  - Scope was admin-only by design — the public app has no data `<table>`s (its lists are card/list layouts).

- feat(musicians): **registry-vs-registry duplicate detection, a new review page, and disambiguated merge affordances everywhere** (#1785, follow-up to #1784, under epic #1787). Ten commits, one PR:
  - **The scan** — `includes/musician_duplicates.php`'s `musicianFindRegistryDuplicates()` finds registry rows that are probably the same person, live-computed on every page load (no precompute table — the registry is two orders of magnitude lighter than the song corpus, so `tblSongLinkSuggestions`'s staleness trade-off isn't worth paying here). Blocking (an exact fold, or a metaphone of the first/last name token) keeps this sub-second even at thousands of rows. Three buckets: **A** — fold-equal ("Eddie James" vs "Eddie  James") — high confidence, no scoring; **B** — fuzzy candidates scored with the shared `ihymns_sim_name_score()`; **C** — a registry row whose fold matches ANOTHER row's curated alias. No silent caps (rule #35) — an over-cap block is skipped and the skip is reported, never swallowed.
  - **The review page** — `/manage/musician-duplicates`, the musician-registry sibling of `/manage/duplicate-songs` (#1215): byte-variant groups as cards, fuzzy pairs as a sortable table, one-click Merge (delegating to the ONE shared `musicianMergeExecute()` core, never re-implemented), Dismiss/Undismiss (`tblMusicianDuplicatesDismissed`), a `force=1` + type-to-confirm guard on any pair whose recorded lifespans disagree (the musician-registry analogue of #1218's same-official-songbook guard), and keyboard navigation (j/k/Enter/d/s, `?` for a shortcut legend). CTAs on `/manage/musicians` and `/manage/musicians-bulk-promote` point at it.
  - **"Which is merging into which?"** — the confusing "Eddie James → Eddie James" merge prompt now shows WHY the two look alike (a byte-variant badge: whitespace / punctuation / case / Unicode-form) and each side's id, lifespan, type and credit count. Landed in the merge-target typeahead, the `/manage/musicians` Merge modal (whose credit-preview widget also now shows all SIX roles, not five — `total` already included artist credits, the five visible pills didn't sum to it), and `/manage/musicians-bulk-promote`'s match options.
  - **The shared scorer, extended not forked** (rule #22) — `includes/song_similarity.php` gained a NAME-similarity section (`ihymns_sim_name_score()` etc.) built on the same primitives the song-duplicate scorer uses; the private fork this replaced (`manage/musicians-bulk-promote.php`'s `_musBulkSimilarity()`) is deleted.
  - **The merge core, extracted then hardened** — `musicianMergeExecute()` (`includes/musician_helpers.php`) is now the ONE place a musician merge happens; both pre-existing surfaces (`/manage/musicians`'s modal, the admin API) delegate to it. Hardening closed two live defects found during the extraction: a merge silently stranded `tblSongArtists` credits (the editor already promotes artist names into the registry, but every registry-side cascade/count was missing the sixth table — `MUSICIAN_CREDIT_ROLE_TABLES` is now the ONE list, re-pointing nine forked five-table copies across the app), and a merge silently cascade-deleted the source's aliases and relations with no way to recover them (both now carry onto the target; the source's own spelling survives as a target alias).
  - One migration (`tblMusicianDuplicatesDismissed`, mirrors `tblSongLinkSuggestionsDismissed`'s shape). Six mutation-proven guards (a pure name-similarity/classifier fixture matrix; the shared-scorer allow-list; the six-credit-table single-list check, extended in this pass to cover the two new files; the scan's no-silent-caps + Disambiguation-exclusion behaviour; the merge-core singleness check, keyed per-statement-shape so a missing DELETE can't hide behind a still-present UPDATE).

- feat(live-follow): **Go Live gets a persistent host bar, leader-idle auto-close, a host-CCLI unlock for followers, and an external presentation-app driver** (#1770, owner-directed Option A rework). Seven requirements, delivered as one PR (C1-C8):
  - **Persistence made visible (req #1)** — a fixed red **LIVE** bar now stays pinned to the bottom of every page while hosting (not just the song page), carrying **Show code** (a large code + QR, `/qr.php`-backed per rule #38), **Console**, and **End**.
  - **Leader-idle auto-close (req #2/#5)** — a Quick session now closes itself after a stretch with NO genuine leader interaction (the automated 30s keepalive alone never counts, only a real pointerdown/keydown, a broadcast, or session create). A three-layer precedence chain (app default → org override/lock → user preference, `enforced ?? (user ?? (orgDefault ?? appDefault))`, clamped 5-240 min) is resolved once and stamped on the session at create. New settings surfaces: `/manage/configuration` (app default), `/manage/organisations` + `/manage/my-organisations` (org override + lock), `/settings` (personal preference).
  - **Host-CCLI unlock for Quick followers (req #3/#6)** — joining a Quick session can now additionally mint a `tblServicePresence` row (an additive POST mode on `live_follow_join`, GET stays byte-identical), letting followers read gated lyrics riding the HOST's own CCLI licence for the life of the session — the same mechanism Service Mode congregants already use, entirely dormant behind `content_gating_enabled='0'` + `require_licence:ccli` rows.
  - **External presentation-app driver (req #4/#7)** — a new `service_drive` API action (key-authed via durable, org-scoped `tblServiceDriverKeys` credentials) lets a ProPresenter-class shim / Companion webhook / curl loop drive a church's Service Mode session, writing through the SAME `serviceMode_applyBroadcast()` core `service_broadcast` uses. Free-text section labels ("Verse 2", "Chorus") resolve against the song's render-order arrangement; unresolvable falls back to song-level. An org-admin mints/lists/revokes keys from a new "Presentation-app control" card on `/manage/service-projection`.
  - **`?svc_code=` deep link now actually works (rule #33)** — the Service-Projection join QR has emitted this param since #1339 with nothing reading it back; `service-follow.js` now reads it on every boot and opens a one-tap confirm prompt (never an automatic join).
  - **Optional drive-sections console for Quick hosts (req #6)** — reuses the SAME `ServiceBroadcaster` module Service Mode's operator console uses via a transport adapter, never a fork.
  - One migration (`migrate-live-follow-quick-capable.php`: `tblLiveFollowSessions` idle columns, `tblOrganisations` override columns, the new `tblServiceDriverKeys` table), fully dormant/additive and column-existence-gated throughout. Six new mutation-proven guards (channel-filter presence across the walled tables; the idle predicate's single-source-of-truth + a live-DB resolver-precedence matrix; the host-CCLI branch's reachability + `OrgId`-NULL invariant; the one-broadcaster-core; rate-limit keying for the two new endpoints; the `svc_code` emitter/reader contract). Docs: `api-docs.yaml`, `help/live-follow.md` + the in-app help topic, the wiki. The live two-device verify (#1339/#1792) remains the one genuinely outstanding item — it needs two real devices on one channel and has never once been run.

- fix(live-follow): **joining with a code from a *different* iHymns environment now says so, instead of a dead-end "wrong code"** (#1792). The instinctive test of Go Live / Join Live — start the session on a laptop's dev/preview URL, join on a phone's live PWA — is exactly the cross-channel case the rule-#26 Channel wall refuses (the three docroots share one MySQL but each only sees its own live sessions), and the generic *"Session not found"* / *"That code has expired"* toast made it read as "the feature is broken". Both `service_join` and `live_follow_join` now, when their channel-scoped resolve misses, call a new **`serviceMode_codeOnOtherChannel()`** existence probe (covering both the Quick `SessionCode` and Service rotating-code families with the same active+fresh predicates the real joins use, rule #22 — an existence check, not a fork of the resolver's disambiguation) and, on a hit, return the ONE shared **`SERVICE_MODE_WRONG_CHANNEL_MESSAGE`** — *"That code belongs to a different iHymns environment … make sure both devices are using the same web address."* This does **not** weaken the anti-probe opacity (rule #26 I6): it fires only for a code the caller already holds validly *somewhere else*, so it reveals nothing about a current-channel session's liveness, grants no access, names no specific environment, and rides the same per-IP join rate limit (same 404 status; only the message differs, rule #35). Docs put the same-address testing procedure front-and-centre (`help/live-follow.md` callout + the in-app help topic + the wiki). A signed-in cross-channel *bridge* (deliberately driving a session across environments) was considered and **not** built — it would poke a hole in the wall that exists to stop prod-stale leakage; the same-address procedure + the clearer error solve the actual testing need (surfaced as a non-blocking owner decision on the issue). Guard `tests/php/test-live-follow-cross-channel.php` (tree-derived + live-DB behavioural, mutation-proven: helper `<>`→`=` reddens the Channel-semantics checks; dropping the call from either endpoint reddens the wiring checks). PHP suite 135→136. The live two-device verify (#1339) still needs two real devices on one channel.
- fix(live-follow): **a Quick "Go Live" code now stays valid for as long as the session is alive** (#1792, owner direction — the Kahoot/Mentimeter model). The Live Follow code is *fixed* (never rotates); the only thing limiting it was the join/poll gate requiring a host heartbeat within 180s, a crude host-alive proxy from before #1770's idle-auto-close existed — and it silently locked followers out whenever the leader's phone backgrounded the tab (mobile browsers pause timers). `live_follow_join` + `live_follow_poll` now gate on the new **`serviceMode_liveFollowAliveSql()`**: on a migrated install the session is "alive" as long as the leader hasn't gone idle past the **configurable** `IdleTimeoutMins` (default 15 min, an org can set it to hours), so a backgrounded host phone no longer blocks joins and a new follower can join at *any* point while the session runs; un-migrated installs keep the 180s heartbeat fallback (no idle-close there to bound abandonment). The idle predicate stays single-sourced through `serviceMode_idleFreshSql()` (rule #26 — the wrapper is a thin regime-selector; the G2 guard now asserts join/poll route through it and that it applies the idle predicate). Runtime-verified: a 10-min-stale-heartbeat + idle-fresh session is joinable, an idle-expired one is not. Service Mode's rotating venue code is deliberately unchanged (proof-of-presence for the CCLI unlock, not a fixed code). Supersedes the earlier "widen 180→600s" idea. PHP 143 / node 53.

- feat(gating): **new `/manage/gating` hub — the content-gating family in one place, with an activation-readiness checklist** (#1778 / #1769 P6). Gathers Content Restrictions, Access Tiers, Licence Types, Feature Gating and Entitlements behind one overview, shows the master-switch state (read-only — the flip stays on Configuration, one write path), and runs a readiness checklist (schema present, tier-caps JSON column present, no-op verified, and the six owner decisions #1772–#1777 as attestation rows). The actual enforcement flip and those decisions remain a deliberate owner-only action, exactly as the program requires — the hub never turns anything on. `manage_configuration`-gated (admin-gate-parity green).
- feat(print): **print templates can now be cloned, exported/imported as JSON, and set as the system default** (#1767 Z + J). On `/manage/print-templates`: a per-row **Clone** (duplicates the layout into a fresh row), **Export** (downloads `{name, blocks, pageOptions}` as a JSON file), a collapsed **Import** form (paste that JSON back — it goes through the SAME block/page-option sanitiser as a normal save, so an unknown block type or option is dropped rather than trusted), and **Set default** (enforces the single-default invariant in a transaction and forces the chosen template active). No schema — reuses `tblPrintTemplates`. Verified against a live DB (clone independence, single-default invariant, export/import round-trip). The remaining #1767 scope (multi-page running headers, uploadable layouts, server-PDF, org branding, CCLI print-usage) stays batched on four owner decisions on the issue.

- fix(setlist): **a shared set-list link is now playlist-first — tap a song (or "Start") and Prev/Next just follow, no Import, no "Use"** (#1790). The owner reported the recipient of a shared link had to (1) Import → (2) click "Use" → (3) tap a song before the Prev/Next bar appeared. Investigation (Fable deep-plan, `.claude/setlist-sharing-1790-1791-plan.md`) found the behaviour was **already built** — since #1533 the shared page arms the playback context on tap with no sign-in and no local write (`setlist.js initSharedSetListPage`) — but the page's own copy actively steered users the wrong way: *"Someone shared this set list with you. Import it to use it."* plus **two** prominent "Import to My Set Lists" buttons and **no play affordance at all**, so nobody discovered the tap-to-play that already worked. Fixed as pure client presentation (no schema, no server): the banner now reads *"Tap any song to start — Prev/Next will follow this list. No sign-in needed."*, a primary **"▶ Start set list"** button arms the context and jumps to song 1 (extracted so tap and Start arm identically — modularity rule), and Import is demoted to a single secondary **"Save a copy to My Set Lists"**. The `test-dom-target-integrity` guard caught two now-dangling id lookups during the change (the removed top button) — both cleaned up. node 50/50. The bigger **collab-by-link** rework (#1791 — an edit-scope capability token in the same `tblSharedSetlists`, no email invite/account) is the planned follow-on (schema+server+client), with owner-decision gates G1–G4 pending.
- fix(export): **ProPresenter 7+ export works again — CSP-safe protobuf encoding** (#1788). Every PP7 export (single song AND whole songbook) died with *"Evaluating a string as JavaScript violates … 'unsafe-eval'"*. Cause: `.pro` files are binary protobuf, and the exporter loaded the schema as a **reflection descriptor** (`protobuf.Root.fromJSON`) whose encoder protobufjs builds lazily via `new Function()` on first `.encode()` — refused by the enforcing nonce CSP (#117; no `'unsafe-eval'`). Only PP7 hit it (every other format is plain text/XML). Fixed with protobufjs's own answer for eval-disallowed environments: a precompiled **static** encoder (`pbjs -t static`, new `protos/pp7-proto-static.js` + `tools/build-proto-static.js`) that writes the encode path out longhand against the hand-written `protobuf.Writer` — **zero `new Function`/eval**. `propresenter-export.js` now prefers that static tree (reflection kept only as a Node-test fallback); the three browser surfaces (v1 editor, v2 editor, public `export-ui.js`) load it between the runtime and the exporter. **Proven** by `tests/test-propresenter-static-csp.js`: the static encoder's wire bytes are **byte-identical** to the reflection encoder's for the same payload (so the file ProPresenter opens is unchanged), it round-trips through the decoder, the artifact contains no eval, and a static `.encode()` still runs with `Function`/`eval` poisoned (CSP simulation) — all mutation-proven (bump a wire tag → red). NOT done by adding `'unsafe-eval'` to the CSP (that would gut the app-wide script-injection defence for one format). The `protobufjs-cli` needed to regenerate is a documented one-off install, deliberately not a committed devDep (the generated artifact is committed; runtime + CI never need the CLI). PHP 130/130, node 50/50.
- fix(setlist): **a printed set list no longer includes the song page's screen-only chrome** (#1789, interim). `SetList.printSetList()` injects each song's full `?page=song` fragment into the print document, which carried the "Report a missing song" link + "Suggest a correction" form (and media/external-link chrome) into the printout — messy. The feedback block is now tagged `song-page-feedback d-print-none` in `song.php`, and the set-list print stylesheet (which opens its own Bootstrap-less window) re-declares `.d-print-none` plus the block's class/id, so anything the app marks screen-only is honoured on the printed set list too. The **full** remedy — rendering each song through the #1767 print-template engine so the set-list Print also offers/honours a chosen template (it currently offers none) — is tracked under #1767.
- fix(musicians): **the "1 person is credited but isn't saved yet" counter can no longer get stuck, and the confusing "merge X into X" is fixable** (#1784). Root cause (the weeks-old "Eddie James" case): a song-credit row and its registry row hold the SAME name spelled with different **invisible bytes** — a trailing space, an NBSP. The Musicians list links a credit name to its registry row by **exact byte match**, so it never linked those two and the counter stuck on 1 forever; the fuzzy matcher, which folds the invisible difference, then showed the nonsensical "Merge Eddie James → Eddie James". And the merge button couldn't fix it either: it `trim()`med the candidate, saw it now equalled the target, and **skipped as "nothing to do"** — so the credit row kept its stray bytes. New credits never hit this (every write path trims the name before both the credit-table insert and the registry upsert), so it is purely a **legacy-data** clean-up. Fixed three ways: **(1)** a shared reconciliation core `musicianReconcileCreditNameBytes()` + `migrate-reconcile-credit-name-bytes.php` (Apply-all card + drift-detecting probe) that rewrites every legacy credit row to its registry row's exact spelling (or normalises + auto-registers a name with no registry row at all) — **identity-preserving**, so it can never merge two DISTINCT people (a genuine registry duplicate is reported, not touched); **(2)** the "Add all N now" button now runs the same reconciliation inside its transaction, so it actually drives the counter to zero; **(3)** the fuzzy-merge branch re-points on the collation (`Name = ?` with `BINARY Name <> BINARY ?` to skip already-correct rows) so a stray-byte variant IS caught instead of silently skipped. The register-list query is aligned to the same **byte-exact** definition the banner uses (rule #35 — they can never diverge again). **Verified** end-to-end against a live DB: an injected trailing-space credit row makes the probe pending, reconciliation rewrites it (adopt) + auto-registers the pre-existing unregistered names (register), the probe clears, and a second run is a clean 0/0 no-op; migration-registry + schema-coverage + component-json guards all green. Also **terminology**: the live page still said "Credit People" while the sidebar (correctly, since #1741 P2-B) said "Musicians" — the stale display labels are swept to **Musicians** across `musicians.php`, `musicians-bulk-promote.php` (now "Add Musicians in Bulk") and the songbook editor's person picker, in plainer language that drops the "registry"/"promote" jargon (internal identifiers — `tblMusicians`, `manage_musicians`, `/manage/musicians`, the migration *card* titles — stay unchanged, same UI-copy-only pattern as Catalogue→Collection rule #24). Registry-vs-registry duplicate detection, an easier merge UX that disambiguates same-looking names, and app-wide multi-column table sorting are the larger follow-ups (#1785 / #1786, under epic #1787), planned separately.
- feat(editor): **duplicate a song as a starting point for a new songbook** (#1783). A new **Duplicate** button (and a `?duplicate=<sourceId>` deep-link) copies an existing song — lyrics, chords, credits, tags, external links, arrangement, key, alt titles, metadata — into a hidden staging book and opens it in the editor with the **Songbook and Song number fields empty**; picking a songbook assigns it (the existing #1679 re-key) and it becomes a brand-new song. Reuses the revision-restore copy engine (`ed2_applySongSnapshot`, no second copy loop). Resets on the copy: Number, Verified, recording-level Isrc, media flags (media isn't copied); fresh PublicId; a `'duplicate'` revision. The copy also re-anchors **per-line enrichment** — translations/romanizations (`tblLyricLineTranslations`), Genius-style annotations (`tblLyricLineAnnotations`) and scripture refs (`tblSongScriptureRefs`) — onto the NEW song's lines via a positional source→new line map (the three tables anchor on `tblLyricLines.Id`, rule #21, so a naive `INSERT…SELECT` would point them at the source), best-effort so an un-migrated optional table never aborts the duplicate. **Verified end-to-end against a live DB** (components/lines/credits/tags copied, resets correct, `load_song` reports the empty-fields state; a separate fixture run confirms translations/annotations/scripture land on the new line ids with `EndLineId`/whole-song-NULL remapped correctly). Guarded by `tests/test-editor-duplicate-contract.js` (client↔server↔shell contract + no client-side `'PENDING'` literal) and `tests/php/test-duplicate-copy-set.php` (copy-set: resets, one apply path, enrichment re-anchored, no move/personal-state leak) — both tree-derived and mutation-proven (rule #34), plus an explicit `?duplicate=` shell-read assertion in `test-editor-deep-links.js`. node 52/52, PHP 132/132. Deferred (for consideration, non-blocking): per-line presenter `Note` carry, a `/manage/duplicate-songs` `?duplicate=` emitter, a `songRelocate()` no-redirect flag for staging-origin moves, and D5 counterpart auto-link (best done at assign-time, needs a source marker).
- feat(song): **inline chord charts + transpose now work on the song page** (#299). The `#btn-toggle-chords` button shipped hidden with zero JS, and `transpose.js` gated its widget on `[data-chord]` elements nothing emitted — so chords only ever appeared in print (#1767 R). New shared renderer `includes/chord_display.php` turns a component chord line into per-chord `<span data-chord>` tokens (the unit `transpose.js` rewrites) with whitespace preserved for positioning; `song.php` renders a hidden `.lyric-chords` row above each lyric line only when `$songHasChords` (song-level, cacheable-safe — rule #6), shows the toggle, and `transpose.js::bindChordToggle()` reveals them via `.chords-visible`. Guard `test-chord-display.php` (mutation-proven).
- feat(song): **song tags now render on the song page** (#288). `#song-tags-container` shipped hidden with no JS, so tags rode `song_detail` and were discarded. Tags are song-level, so the shared-cache fragment now renders them server-side as chips linking to `/tag/<slug>` (rule #33); the block is omitted entirely when a song has no tags.
- feat(setlist): **"Save as PDF" on the set-list detail view** (#302). The dead, ambiguous page-level `#btn-export-pdf` (hidden, no JS) is replaced by a discoverable `#setlist-pdf-btn` next to Print, reusing the existing well-titled print document (cover + running order + full lyrics) → the browser's Save-as-PDF. House-style, dependency-free (no server/client PDF library), shared-hosting friendly.
- feat(print): **the Songbook + number block prints nothing for songs not in a published songbook** (owner directive, #1767 follow-up). A song in an unofficial songbook, a catalogue, or any non-published grouping has no meaningful "Songbook + number", so the whole `subtitle` block is suppressed (new `songInPublishedBook()` in `print.js` — authoritative `isOfficial` when present, else the positive-number proxy per rule #24). Documented in-app (Manage → Print Templates) and in `help/exporting.md`.
- feat(songbooks): **songbook lists alphabetise ignoring a leading article** (#150). "The Australian Hymn Book" now files under A, "A Mighty Fortress" under M. New shared `includes/sort_helpers.php` (`ihymns_title_sort_key()` / `ihymns_sort_by_title_key()`) strips one leading article — same vocabulary as the fuzzy dedup fold but preserving the rest of the title for tie-breaking (rule #22, not a re-fork). Wired into both `getSongbooks()` branches; guard `test-sort-helpers.php` (mutation-proven).
- refactor(search): **removed the dead header-search autocomplete** (#307). `_initAutocomplete` had zero callers — the typeahead dropdown never attached to any input. Removed the whole cluster together (the `search.js` methods, `.search-autocomplete` CSS, the `?action=suggest` endpoint + `SongData::suggestSongs()`, the api-docs entry, and the isolated ARIA test) so no half-feature orphan remains; the live search (`?action=search`) is untouched.
- fix(share): **a numberless song no longer renders "#0"** in the share `og:title`, the JSON-LD breadcrumb leaf and the OG preview PNG badge (#85) — all three now guard `(int)null === 0`.
- fix(offline): **the offline indicator counts deliberately-downloaded songs** (#112). `getCachedSongCount()` counted only the recent-views cache, missing #1597's saved-cache, so a user who downloaded a whole songbook saw "0 cached". Now counts both buckets, de-duped by URL.
- fix(import): **bulk importers now persist parsed credits instead of silently dropping them** (#1736). `_bulkImport_saveSong()` — the shared saver behind EVERY importer (OpenSong / OpenLyrics / ChordPro / VideoPsalm / the #1633 interchange format) — parsed writers/composers out of each source document and then wrote **no credit tables at all**, so a bulk-imported song arrived with no writers, no composers, and no `tblMusicians` registry rows (its own comment documented the gap as intentional). It now INSERTs the five core role tables (`tblSongWriters`/`Composers`/`Arrangers`/`Adaptors`/`Translators`) and calls `musicianPromote()` on each name — the SAME `creditEntryNormalise()` decompose + registry-upsert path `save_song_core.php` uses (rule #22), so an OpenSong `<author>` name-string lands as First/Surname/Suffix in the registry exactly as a curator-typed credit does. **INSERT-only by construction:** saveSong already returns `'skipped'` for an existing SongId / normalised-title match before opening its transaction, so the "re-import: replace vs merge vs leave" design question resolves to *leave* (a re-import never reaches the write) and no DELETE is needed. The tree-derived #960 guard (`test-credit-registry-promote.php`) — which its author had pre-wired to expect `song_importers.php` once it started writing credits — now covers saveSong automatically and verifies the promote call. **Verified** end-to-end against a fresh schema: importing a song with `['Mary E. Byrne','Eleanor H. Hull','mary e. byrne']` writes 2 writers (case-dup collapsed), 1 composer, 3 registry rows with decomposed First/Surname, and a re-import skips with no duplicate credits. Independent one-file fix off `alpha` (branch `fix/importer-writes-credits-1736`), not bundled with the songbook/publisher epic. (The remaining saveSong limitation — copyright/ccli/verified/PD/media flags hardcoded empty on INSERT — is separate and still tracked.)
- fix(backup): **admin "Backup Database" no longer dies with "Interrupted / empty output"** (#1771). Two independent defects. **(1) OOM** — each table was dumped with a default **buffered** `SELECT * FROM <table>`, pulling the whole table into PHP memory before the first INSERT was written; on the real corpus (~14k `tblSongs` carrying MEDIUMTEXT lyrics + `tblSongEmbeddings`) that OOM-fatals under a shared host's `memory_limit`, and the fatal fires *inside* the buffered call before the per-table line prints (hence "Interrupted" + empty pane). Now streams **UNBUFFERED** (`MYSQLI_USE_RESULT`), freeing each result before the next table's query (constant memory per table), batches multi-row INSERTs bounded by BOTH row count (200) and byte size (768 KB) so a statement never nears `max_allowed_packet`, and writes **straight into the gzip stream** (no giant uncompressed `.sql` intermediate to exhaust a disk-constrained host). **(2) Un-restorable VIEW dumps** (a second bug the report didn't catch, found during round-trip verification) — the 8 compat/derived VIEWs (`tblCreditPeople` et al. from #1741, `v_ChristianSongs`) were treated as base tables (`DROP TABLE` + empty DDL + INSERTs into a non-existent table). Now enumerates via `SHOW FULL TABLES` and emits VIEWs as `DROP VIEW` + `CREATE VIEW` in a **second pass after all base tables** (name order alone is not enough — `tblCreditPeople` sorts before its base `tblMusicians`). Runs under `MYSQLI_REPORT_STRICT`, `set_time_limit(0)` at entry, and **removes its partial file on any failure** (a truncated backup must never masquerade as good); the apply-all pre-migration `$finalFile` abort-guard contract is preserved. The plan's `ob_flush()`-for-live-progress suggestion was deliberately **not** applied — the dashboard's `?format=text` AJAX runner captures output via `ob_start()`/`ob_get_clean()` and parses a `STATUS:…---\n{body}` frame, so an early `ob_flush()` would corrupt that frame; the empty-output symptom was purely a consequence of the process dying mid-buffer. **Verified** with a full backup → restore round-trip into a scratch DB (exit 0 both ways, 141 tables + 8 views recreated, row counts match). New guard `tests/php/test-backup-streaming.php` (DB-free, comment-stripped source scan — a behavioural round-trip needs a DB and can't run in CI) locks the unbuffered read, result-`free()`, `SHOW FULL TABLES`, view-last two-pass, gzip-direct and partial-file-cleanup properties; mutation-proven both directions.
- test(publishers): **permanent CI guard + `.claude` rule for the publisher registry (#93 / epic #1765, commit 5)**. New `tests/php/test-publisher-registry.php` — a DB-free, comment-stripped source guard (CI globs `tests/php/*.php`, so it runs automatically) locking the cross-file agreements nothing at runtime enforces (rule #35): the shared cores define the functions + vocab callers depend on; `/manage/publishers` delegates to the shared core rather than re-implementing it (rule #22); the songbook editor's write handler reads the **same** `publisher_ids[]`/`publisher_roles[]`/`publisher_notes[]` POST keys the modal emits **and** validates the Role against the shared `IHYMNS_PUBLISHER_ROLES` (never a hardcoded list); and the `/publisher/` route is wired end-to-end in `router.js` + `api.php` + the page file (rule #33). **Mutation-proven per rule #34, and the first draft was wrong-but-green exactly as that rule warns**: two of five mutations slipped through because `strpos('publisherAdminMerge')` still matches `publisherAdminMergeLOCAL` and `strpos('tblPublisherAliases')` still matches `tblPublisherAliasesXX` (the substring trap) — fixed with a word-boundary `\b` matcher, after which all five real-tree mutations (drop a POST key, drop the route, stop delegating a core fn, drop a schema table, stop using the shared Role vocab) go RED, baseline GREEN, restore byte-identical. Also adds CLAUDE.md rule #37 + a red-flag codifying the registry contract (denorm-mirror-not-authority, idempotent-by-Name-not-slug, no re-fork, no hardcoded vocab, honour the `/publisher/` link). Whole PHP suite (111) + node suite (50) green. Full docs (in-app help, wiki, OpenAPI/`admin_publisher_*` API) are folded into the epic's Step-7 sweep (#91), matching how #1765 deferred its own docs.

- feat(publishers): **Public `/publisher/<slug>` page (#93 / epic #1765, commit 4)**. The registry now has a public face — necessary because the admin list (commit 2) and the editor picker rows (commit 3) already emit `/publisher/<slug>` links, and a URL another surface emits is a contract (rule #33). New `includes/pages/publisher.php` (mirroring `/tune/`): resolves the slug via a three-rung ladder — exact `Slug`, PHP name-fold over every `Name`, then the same fold over `tblPublisherAliases` (so a renamed publisher's old-name link still resolves, since a rename banks the old name as an alias) — and renders the publisher header (name + disambiguation + kind badge + subtitle), an "imprint of →" parent link, city, IPI/ISNI chips, aliases, notes, and the list of **songbooks they published** (via `tblSongbookPublishers`, filtered by the public `songbookVisibleSql()` so disabled books are hidden, each a `data-navigate="songbook"` link with a role badge for non-primary roles), plus an inert `application/ld+json` block (Organization, or Person for a person-kind publisher — the rule #30 exemption; no executable inline script). Existence-gated on `tblPublishers` (rule #9) with a friendly not-found / empty state. Wired into the SPA: `case 'publisher'` in `router.js` (→ `params.slug`, forwarded generically as `?slug=`) and in `api.php` (uncached, matching the tune/iswc precedent). **Verified live** against a scratch DB: an 11-point render pass — resolved (name, disambiguation, city, aliases, the linked songbook + its `/songbook/RT` link, JSON-LD), not-found state, empty state, AND both fallback routes (alias-slug + name-fold both resolve to the canonical publisher, rule #33) — all green; `php -l` (page + api.php) + `node --check` (router.js) + fragment-inline-scripts guard (26 files, the ld+json exempt) all green. The songbook page's own registry-publisher chips are a deferred follow-up (its free-text `Publisher` denorm already displays the primary publisher).

- feat(publishers): **Songbook editor multi-publisher picker + Publisher typeahead (#93 / epic #1765, commit 3)**. Wires the registry into the songbook editor — the "multi-publisher copyright" + "typeahead everywhere" deliverables. The free-text `Publisher` field (shared create+edit partial) gains a **registry-backed datalist typeahead** (`?action=publisher_search`, debounced; pre-migration it returns nothing so the field degrades to plain text). The **edit modal** gains a **multi-publisher M:N picker** (mirroring the #831 compilers picker): add publishers by typeahead, each row carrying a **Role** dropdown from `IHYMNS_PUBLISHER_ROLES` (publisher / co-publisher / distributor / imprint / administrator / printer — VARCHAR vocab, not ENUM) and an optional note; DOM order → `SortOrder`. On save the update handler reconciles `tblSongbookPublishers` with a DELETE-then-INSERT inside the existing save transaction, de-duped by `(PublisherId, Role)` (the `uq_book_pub_role` UNIQUE allows one publisher in several roles but never the same role twice), and a bad/absent role coerces to `publisher`. When the picker is non-empty the free-text `tblSongbooks.Publisher` **denorm is re-synced to the primary (first-listed) publisher's name** (the TuneName↔TuneId pattern); an empty picker leaves the curator's typed value untouched. All schema-gated by a `$hasPublishersSchema` probe (pre-migration installs skip the whole picker silently); the reconciled `publisher_ids` are added to the songbook-edit activity-log payload. This is a full admin page (own `<head>`), so the inline picker script is legitimate (rule #30 applies only to api.php SPA fragments); the fragment guard still passes. **Verified live** against a scratch DB: a 7-assertion write-handler behavioural pass — multi-publisher insert with roles, `(id,role)` de-dup collapsing dup + bad-role rows, denorm sync overriding typed text, same-publisher-two-roles allowed, empty-picker clears links but preserves the typed denorm — all green; both new inline scripts node-`--check` clean; `php -l` + fragment-inline-scripts guard green.

- feat(publishers): **Publishers admin CRUD — `/manage/publishers` + shared core (#93 / epic #1765, commit 2)**. Full curator surface for the `tblPublishers` registry, modelled line-for-line on `/manage/tunes` (#1748): list (name, kind, parent, city, songbook-count, alias-count, active) + create + edit modal + delete + merge. **One shared core** `includes/publisher_admin.php` (validate/uniqueness/persist/create/rename-cascade/aliases/usage/delete/merge) backs both the page and the future `admin_publisher_*` API (rule #22/#35 — nothing to drift); `includes/publisher_helpers.php` holds the `IHYMNS_PUBLISHER_KINDS`/`IHYMNS_PUBLISHER_ROLES` VARCHAR vocabularies (not ENUM, rule #20), the slug fold, and the one find-or-create funnel. The edit modal wires **person + location typeaheads** (the #93 headline): a musician-link picker (`?action=musician_search` → `MusicianId`), a parent-publisher picker (`?action=publisher_search` → `ParentId`, self-excluded), and the shared `place-search.js` for the city (→ `CityId`). Every optional table/column is probed once via `publisherAdminProbeGates()` and each FK candidate is existence-checked before store (a stale musician/parent/city id degrades to NULL, never throws); `ParentId` gets a bounded **ancestor-walk cycle guard** (self-parent and A→imprint→A both rejected). A publisher **rename cascades** into the free-text `tblSongbooks.Publisher` denorm on that publisher's linked books (the TuneName-cascade equivalent), and a **merge** repoints songbook links (UPDATE IGNORE past the `uq_book_pub_role`), re-parents imprints, rewrites the denorm source-name→target-name, moves aliases (+ the source's own name becomes a target alias, rule #33), and deletes the source — all in one transaction. `manage_publishers` entitlement wired across all four sites (PHP + JS maps in lockstep, admin-links nav under "Catalogue", the labels map); page gate equals the nav-advertised entitlement (rule #1587). All writes hit the activity log (`publisher.create`/`edit`/`delete`/`merge`) per the owner's "record everything" directive. **Verified live** against a scratch DB: a 20-assertion behavioural pass over the shared core — create, person-persist (kind/musician/city/ipi), FK null-out, parent + cycle + self-parent guards, alias dedup/drop-own-name, usage counts, rename cascade, full merge (repoint/re-parent/alias/denorm), delete — all green; entitlement-parity (PHP↔JS), admin-gate-parity (39 pages) and orphan-inventory (52 checks) all green. The external-links editor panel is deferred (the `tblPublisherExternalLinks` table ships dormant + forward-looking, like `tblTuneExternalLinks` before #1748; merge/usage already handle its rows) — tracked follow-up.

- feat(publishers): **Publishers registry — schema batch (#93 / epic #1765, commit 1)**. Promotes the songbook publisher from the free-text `tblSongbooks.Publisher` scalar to a first-class registry, in ONE forward-looking additive/idempotent schema batch (rule #20): `tblPublishers` (persons + companies via a `Kind` VARCHAR vocabulary; an optional `MusicianId` FK so a musician who also publishes is not duplicated; a `ParentId` self-FK for imprint / **catalogue grouping**, mirroring `tblWorks.ParentWorkId`; `Ipi`/`Isni` NULL-distinct UNIQUE; a `CityName`+`CityId` place-FK mirror; `IsActive` soft-hide), `tblSongbookPublishers` (multi-publisher copyright M:N mirroring `tblSongbookCompilers`, with a `Role` VARCHAR vocabulary + reserved dormant `ValidFrom`/`ValidTo` rights-window columns), and the forward-looking `tblPublisherAliases` + `tblPublisherExternalLinks` (rule #15/#20 — the family never needs a second migration). The free-text `tblSongbooks.Publisher` column is **kept as a JOIN-free denorm display mirror** (same pattern as TuneName↔TuneId), so every existing reader keeps working. New `migrate-publishers-entity.php` backfills the registry from DISTINCT non-empty Publisher strings (CI-folded) and links each book; **idempotency is by Name existence, not slug** — the registry has no `uq_Name` (two publishers may share a name under different Disambiguation), so a naive INSERT-IGNORE-on-slug re-run minted duplicates (`praise-trust-2`); caught by a behavioural round-trip and fixed. Registry card + 4-table OR-probe (rule #19). **Verified live**: fresh `schema.sql` install (4 tables, 8 FKs) clean; migration RUN 1 → 2 publishers + 4 links, RUNs 2–3 → 0/0 (idempotent), case variants folded, empty/NULL skipped. Admin CRUD + editor picker land in the following commits. ⚠️ Owner sub-decision flagged: "catalogue grouping" is implemented as a self-FK imprint hierarchy (the defensible reading, since `tblCatalogues` groups *songs*); trivially changeable to a publisher↔catalogue M:N if that was the intent.
- feat(gating): **#1769 P5 — DM-1 org-licence consolidation (dormant)** (branch `claude/gating-model-review`). Completes the consolidation of org licences into the single authoritative `tblOrganisationLicences` store before the (deferred, gated) legacy-column retire. **Finding on investigation (verify-before-building):** the bulk of DM-1 — backfilling the join table from `tblOrganisations.LicenceType`/`LicenceNumber` — is **already shipped** by `migrate-organisation-licences.php`, and `tblContentLicences` has no writer in the codebase (empty on every normal install). So DM-1 is scoped to the two GENUINE residual gaps, not a from-scratch duplicate: **(A)** that existing backfill never carried `tblOrganisations.LicenceExpiresAt`, so a join row can hold a NULL expiry while the legacy column holds one — `migrate-consolidate-org-licences.php` carries it **fill-NULL-only** (never clobbering a curator's join-row value); **(B)** it consolidates any hand-written org-scoped `tblContentLicences` ghost rows (INSERT IGNORE, **row-count-reported** because a ghost that confers a tier would then be seen by `resolveEffectiveTier()` — a disclosed live effect, per P2-F; zero rows in practice). Verified **no-op for resolution**: `resolveEffectiveTier()` unions on `LicenceType` only and never reads `ExpiresAt`. Data-only (no schema.sql edit); an Apply-all registry card with a data-derived **drift-detecting** probe + a CLI `revert-consolidate-org-licences.php`. Guard `test-consolidate-org-licences.php` (mutation-proven: drop the `IS NULL` fill-guard → red); PHP **121/121** CI-parity; live-DB round-trip verified (carry → idempotent re-run → revert → cascade cleanup; ghost leg confirmed empty). The legacy-column **retire itself stays a separate, gated P6 step** (#1778).
- feat(gating): **#1769 P4 — management surfaces + DM-2 rights-fact derivation** (branch `claude/gating-model-review`, continues P3 below). The write path over the Model-2 vocabulary + facts, all **dormant** (nothing ENFORCES until the P6 master-switch flip). Six commits: **(A)** licence-type CRUD — a new `manage_licence_types` entitlement + `/manage/licence-types` page + four `admin_licence_type_*` API actions over one shared core `includes/licence_type_admin.php` (validate with LicenceKey-immutable-on-update, covers-merge that keeps `{}` stable across a JSON round-trip, delete refusing system/referenced rows, existence-gated reference counts), documented in api-docs.yaml. **(B)** editor **Rights panel** — the two per-song rights facts (`tblSongs.LyricsRightsLicenceKey`/`MusicRightsLicenceKey`) join the v2 editor via `ED2_META_FIELDS` + an existence-gated `metadata_field_update` branch (validates against the live registry, 422 on an unknown key, `admin.song.rights_set` audit) + a DOM-first `rights-panel.js` fed by `window._iHymnsLicenceTypes`; `load_song` emits the songbook-default prefill hint. **(C)** songbook **default rights** — `/manage/songbooks` edit modal sets `Default{Lyrics,Music}RightsLicenceKey` (registry-validated, column-gated) with an opt-in **fill-NULL-only** apply-to-songs sweep (`IS NULL` never overwrites a curator's key). **(D)** **DM-2** `migrate-derive-rights-facts.php` — copies each song's existing `require_licence` restriction into the matching fact column via the ONE shared fold `rightsFactColumnForLicence()` (lyrics/music/none), fill-NULL-only + idempotent, data-only (no DDL), an Apply-all registry card with a data-derived **drift-detecting** probe + a CLI revert tool. **(E)** deferred-P0 tidy — `require_*` restrictions normalise `Effect='deny'` server-side (engine-dead, the value was already ignored) + hide the control; `gating-noop-verify` gates on `manage_configuration` with a matching nav row. **(F)** an **activity-log CI guard** (`test-gating-admin-activity-log.php`) that tree-derives every gating-write admin surface and fails the build if one never calls `logActivity()` — which caught + fixed `manage/tiers.php` logging only on failure (success `admin.tiers.*` now logged). Every gating action is now audited (owner directive). Six new mutation-proven guards (`test-licence-type-admin`, `test-rights-panel-fields`, `test-songbook-rights-defaults`, `test-derive-rights-facts`, `test-restrictions-effect-honesty`, `test-gating-admin-activity-log`) + a `§(g)` fact-column containment lock added to `test-gating-pipeline-structure.php`; PHP 120/120 + node 50/50 CI-parity; DM-2 + the fill-NULL-only sweep verified against a live DB. **Still owner-gated (P6):** the master-switch flip + the `/manage/gating` hub (#1772–#1777, and the dead songbook/feature restriction pickers).
- refactor(gating): **#1769 P3 — emitter adoption + the song-page fork collapse** (branch `claude/gating-model-review`, continues P2 below). The public HTML song page carried its OWN inline copy of the tier/presence/media gating alongside the JSON API's; P3 collapses it onto the shared Model-2 pipeline and closes the shared-cache gap — additive/dormant, a **proven byte-identical no-op** in both gating states. New `includes/song_page_gating.php` (`songPageGatingDecide(array $viewer, …)`) reads the already-resolved caps/presence off the viewer struct; `song.php` now builds ONE viewer via `accessViewerContext(…, [])` (apiKeyScopes=[] is load-bearing — null would gate a bypass-key render) and delegates the whole decision, resolving nothing itself. Proven the P2 A→B→C way: the legacy maths was extracted verbatim, frozen as **4608 goldens** (`tests/php/fixtures/song-page-gating-goldens.json`), and the viewer-driven form proven to reproduce every one (`test-song-page-gating-equivalence.php`). The page's DELIBERATE pre-existing quirk (presence ORs into sheet/midi, not just audio, unlike the API) is **preserved byte-identically**, pinned by the golden replay and filed as an owner decision (#1774). `songbook_export` now builds the viewer ONCE and maps `accessApplySong` (a perf win — was rebuilding the recursive-CTE viewer per song); the single-song emitters stay on the delegates by decision (in-code notes prevent future churn). Cache gap closed: when gating is on, `api.php` marks the viewer-dependent `page=song` fragment `Cache-Control: private, no-store` + excludes it from the shared ETag/SW cache, and the service worker's new `swResponseCacheable()` honours no-store at its incidental put sites (deliberate offline downloads stay saveable, re-gated on next fetch — the #1388 posture). Auth needed NO client change (the `ihymns_auth` cookie already rode `loadPage` fetches; the stale song.php:223-227 note is corrected). `test-gating-pipeline-structure.php` gains the song.php fork-collapse + cache-exclusion guards (its `$viewer[…]` key check is now tree-derived). Full mutation checklist run break→red→restore; PHP 114/114 + node 50/50 CI-parity. **Commit F deferred** (wiring the presence token into `checkBulkAccess` is a state-(b) delta → #1777, owner sign-off). Further P6 first-enable follow-ups filed: #1774 (presence-OR divergence), #1775 (audio-media.php tier gate), #1776 (SW cache-clear on login).

- refactor(gating): **#1769 P2 — one licence registry, one viewer resolver, one enforcement pipeline** (branch `claude/gating-model-review`; P0/P1 landed earlier in this batch). The content-gating logic was scattered — tier/ccli/presence resolution inlined at the top of both `contentGatingApply()` and `contentGatingMediaAllowed()`, the media kind→cap switch copy-pasted, and the licence vocabulary hardcoded in six sites that had already drifted. P2 consolidates to **facts × grants × one resolver × one pipeline**, additive/dormant and a **proven byte-identical no-op** in both gating-off and gating-on states. New: `includes/access_context.php` (`accessViewerContext()` resolves "who is asking?" once into an `ACCESS_VIEWER_KEYS` struct, with the old throw semantics preserved exactly; `accessViewerAssemble()` is the pure DB-free seam), `includes/access_resolver.php` (`accessApplySong()`/`accessMediaAllowed()` make every field decision branch-for-branch identical to the deleted `_contentGating*LegacyCore` seams; `accessResolve()` is the unwired P3/P6 primitive; a per-song rights-fact gate is present but **triple-locked dead** — `SongData` selects neither rights column, no row carries a value, the guard skips null/''), and `includes/licence_registry.php` (the ONE reader of `tblLicenceTypes`, degrading to the byte-exact P1 seeds on any un-migrated install). `contentGatingApply()`/`contentGatingMediaAllowed()` are now thin delegates that lazy-require the pipeline (breaking the require cycle). The six licence-vocab sites (`organisations.php`, `restrictions.php`, `my-organisations.php`, `ccli_validator.php`'s conferral overlay, three schema COMMENTs) now single-source from the registry — the conferral is a proven identity for every input (`mrl`/`custom` confer nothing → the preserved `'free'` fallback). `TIER_CAPS` gains an optional 5th `enforce` element (`tierCapEnforce()`, inert in P2). **Proof:** the pre-refactor outputs were frozen as 384 goldens and replayed byte-identically through the new pipeline (`test-gating-equivalence.php`, DB-free); `test-gating-pipeline-structure.php` + extended `test-gating-noop.php`/`test-gating-registry.php` + the updated `test-media-kind-cap-shared.php` lock the structure; the whole mutation checklist was run break→red→restore (rule #34). Full PHP suite 113/113 CI-parity. Enforcement stays entirely dormant behind `content_gating_enabled='0'`. Two owner decisions deferred to P6, non-blocking: `mrl`/`custom` tier conferral (#1772) and the `RequiresCcli` tier-name-vs-cap gate (#1773). Data migrations (DM-1 org-licence consolidation, DM-2 existing-gating→per-song-facts) and activity logging land in P4–P6.
- feat(qr): **QR codes now come from the CueRCode service via its API — throughout iHymns** (owner directive 2026-08-05; `github.com/MWBMPartners/CueRCode`). QR generation moved off the client-side vendored `qrcode-generator` (used by the #1767 R print block and the #1339 Service-Projection join QR) onto our CueRCode REST API. CueRCode auth is a secret `X-API-Key`, so generation is **server-proxied** and the key never reaches a browser: `includes/cuercode_client.php` (modelled on the battle-tested `intapps_client.php` — SSRF-hardened host-bound URL, size-capped aborting write-callback, no redirects, SSL verify, timeouts, returns null/never-throws, dormant until keyed; `cuercode_api_key` registered in `secretSettingKeys()` = encrypted at rest) calls `POST /api/v1/generate` and decodes the base64 data-URI to bytes. The ONE client-facing surface is **`qr.php`** (mirrors `og-image.php`): `GET /qr.php?data=&size=&format=svg|png&ecc=` streams the QR bytes with `Cache-Control: immutable`, rate-limited (fail-open), and answers **503 (no body)** when CueRCode isn't configured/reachable so callers degrade to the always-shown URL/code text. Both surfaces now emit a plain `<img src="…/qr.php?data=…">` — **no client-side QR library, no async pre-pass** (the print renderer's `qr` block became a pure synchronous `<img>`; CSP already allows `img-src 'self'`). The vendored `qrcode-generator` is fully removed (config `libraries` entry, `download-vendor.sh` step, and `tests/test-qr.js`), replaced by the tree-derived, mutation-proven **`tests/test-qr-cuercode.js`** (both surfaces use `/qr.php`, key stays server-side, no client-side QR fingerprint remains — comment-stripped so retirement comments don't false-positive). ⚠️ **Dormant until an iHymns→CueRCode API key is provisioned** and pasted on `/manage/configuration` (deploy-time, non-blocking — mirrors the intapps #1726 pattern); the live round-trip can't be verified without it, everything else is (syntax, guard, graceful 503 degradation). New CLAUDE.md rule #38. Full design: `.claude/qr-cuercode-integration-plan.md`. **Still to wire (this branch, follow-up):** the CueRCode credential fields on `/manage/configuration`.

- feat(print): **print-template enhancements — the no-schema block/page/visibility system** (#1767, owner-approved full scope A–AM; branch `claude/print-templates-1767`). The song-print system's layout is a `BlocksJson` ordered block list + `PageOptionsJson` (both existing JSON columns), so this whole slice adds **no schema** — the one-pass foundation (#1350 / rule #20) is already paid. Shipped (all verified through the SHARED renderer `renderTemplateBodyHtml()`, the single source of truth for both the printout and the admin live preview): **4 new content blocks** — Scripture reference (N, `tblSongScriptureRefs` via `?include=scriptureRefs`), Tune + metre (O, base `tuneName` + MeterCode via `?include=tune`), Themes/tags (P, `song.tags`), and a QR-code of the permalink (R, vendored `qrcode-generator` via an async pre-pass so the sync renderer + byte-identical preview are untouched; degrades to the URL text if the library fails); **3 block options** — lyrics alignment + font scale (A) and songbook-abbreviation mode (B); **5 page options** — page size A4/Letter/Legal (G), line spacing (F), high-contrast (V), accent colour (AM) and ink-saver mono (AB), built on a new `PRINT_PAGE_OPTIONS` registry mirroring the block-type pattern; and **conditional block visibility** (Y) — a universal `showIf` (hasChords/hasCcli/hasScripture/…) so one template adapts per song. **The load-bearing mechanism (rule #35):** the block-type set, its `renderBlock()` switch, `$BLOCK_SCHEMA`, `PRINT_PAGE_OPTIONS`↔`$PAGE_OPTION_SCHEMA`, and `PRINT_SHOWIF_CONDITIONS`↔`$SHOWIF_CONDITIONS` previously agreed only by a comment; new CI guard `tests/php/test-print-block-registry.php` derives all of them from the tree and asserts agreement (incl. per-type option keys), mutation-proven in every drift direction (its own PHP parser was caught mis-reading a nested `choices` key on first run — red, not silently green, rule #34). Every feature behaviourally verified with node against the shared renderer incl. empty-data grace and bad-value fallbacks. **Decision-gated remainder batched to the owner on #1767** (server-PDF pipeline W/X/AC/AD/AJ; org branding/white-label D/K/L; uploadable layouts E; usage log AK; thumbnails AL) — rule #20 forbids freezing that schema until 4 owner decisions land; none block this slice. Buildable-now follow-ups still open: Z (clone + JSON import/export), J (system default), and the H-family multi-page/pagination. Plan: `.claude/print-templates-1767-plan.md`.

- fix(apple): **9 Swift test factories couldn't compile — `SongDetail(...)` missing the 6 #1752 identity fields** (second Apple-CI hotfix, masked behind the LOC-budget failure). Once the loc-budget step passed, Apple CI got further and hit a real `swift build` error: the #1752 work added `subtitle`/`disambiguation`/`firstPublishedYear`/`copyrightYears`/`copyrightHolder`/`externalIds` to `SongDetail` but didn't update the package's test construction sites, and the struct **deliberately relies on the synthesized memberwise init** (documented in its header — no defaults). Appended the 6 fields (all `nil`) to the 9 stale `SongDetail(...)` calls across `SongDetailMediaTests`, `OfflineStoreSavedSongsTests`, `ProjectionResolverTests`, `SongComparison{ViewModel,Engine}Tests`, `OfflineStorageViewModelTests`, `ProjectionViewModelTests`, `TVProjectionBridgeTests`, `TVServiceFollowBridgeTests` — matching the already-correct shape in `SongDetailTests`. Chose updating the call sites over hand-writing an init-with-defaults because the model's header documents the no-memberwise-init decision AND an ~85-line init would breach `SongDetail.swift`'s own 400-line budget. `Work(...)` was checked and has no equivalent gap (no test constructions; custom `init(from:)`). ⚠️ Swift not compiled here (no toolchain) — verified by reading each construction against the struct's property list; the PR's Apple CI run is the compile check.

- fix(apple): **`SongDetailView.swift` breached the 400-line LOC-budget tripwire; render-site guard hardened to survive the split** (post-merge CI hotfix for PR #1762). The #1752 catalogue work (disambiguation + subtitle lines) grew `SongDetailView.swift` to 420 lines, failing `Scripts/loc-budget.sh` (#1395). Per the file's own documented "every section is its own file" convention (and the `SongDetailView+Handoff.swift` precedent), the header block — a pure function of `SongDetail` — is extracted into a new **`SongDetailHeaderView.swift`** (parent back to 386 lines). This surfaced a rule-#34 gap in `test-native-identity-contract.js`: its render-site check was a hardcoded `{SongMetadataView, SongDetailView}` two-file list, so the legitimate extraction turned it spuriously RED. Rewritten to be **tree-derived** — scan every IHFeatures view file — AND **specific**: it matches the property access `detail.<key>`, not the bare token `<key>` (which for common SwiftUI words like `subtitle` appears in eight unrelated files, making a substring check unable to ever fail). Mutation-proven both directions: deleting the `detail.subtitle` render → RED, deleting `detail.firstPublishedYear` → RED, restore → green. ⚠️ Swift not compiled here (no toolchain) — the extraction is a behaviour-preserving pure move, CI-gated by `swift build`/`swift test`.

- fix(db): **`schema.sql` could not install on MariaDB 11.7+ — `tblSongEmbeddings.Vector` is a reserved word** (post-merge CI hotfix for PR #1762). `VECTOR` became a reserved keyword in MariaDB 11.7 (CI runs 11.8.8), so the unquoted column `Vector MEDIUMBLOB` in `tblSongEmbeddings` (#1090 P11, the dormant semantic-search cache) was a syntax error — the fresh-install test (`test-schema-installs.php`) failed with "created 139 of 140" because the install aborts at the first error. This slipped through the local pre-merge run because that test **skips without a DB** and the local MariaDB was 10.11 (pre-reservation); CI's MariaDB service is what exposed it. Fixed by backtick-quoting the column (`` `Vector` ``) in **both** `schema.sql` and `migrate-song-embeddings.php` (rule #19 byte-identical mirror) — the standard reserved-word remedy, zero name-divergence risk. Proven: the full 140-table `schema.sql` now installs cleanly against a real MariaDB, and the whole PHP suite is 106/106 with the DB present. A tree-scan confirmed `Vector` was the only bare reserved-word column.

- chore(deps): **Dependabot multi-branch version updates + a security-fix backport workflow** — GitHub does two separate things and only one is `dependabot.yml`-configurable. **Version updates** (`.github/dependabot.yml`): `target-branch` is per-entry and singular, so keeping `main`, `alpha` and `beta` all current means one entry per branch — npm (repo root) and github-actions are declared on all three; gradle (`/appAndroid`) and swift (`/appApple/Packages/iHymnsKit`) on `main` (mobile version PRs on the release branches can be added later). All grouped (minor+patch) so each ecosystem/branch yields at most a couple of PRs/week, labelled, weekly on Monday. **Security updates** are NOT configurable there — GitHub auto-opens a fix PR against the default branch (`main`) ONLY, with no fan-out knob — so `.github/workflows/dependabot-security-backport.yml` carries a qualifying merged fix (authored by `dependabot[bot]`, or labelled `security`/`backport`) to `alpha`, `beta` and `release-candidate`: a `pull_request_target: [closed]` trigger (+ `workflow_dispatch` for a manual PR number) cherry-picks the merge commit (`-x`) onto each release branch and opens a PR, a conflict files a tracking issue instead of a broken PR, and a **non-existent branch is skipped gracefully** (`release-candidate` does not exist yet — the workflow activates it the moment it does). ⚠️ PRs opened with the built-in `GITHUB_TOKEN` do not auto-trigger `on: pull_request` CI (GitHub's recursion guard); the header documents adding a `BACKPORT_PAT` secret to have `test.yml` run automatically.

- chore(version): **significant minor bump `0.4001.0` → `0.4100.0`** for the `wave3-fixes` batch — reflecting the scope of the #1741 catalogue-expansion epic follow-ups + the #1201/#1761 API-envelope + OpenAPI redesign, rather than the per-merge `+1` the `version-bump.yml` auto-bumper applies. Bumped by hand in `infoAppVer.php` and mirrored into `api-docs.yaml`'s `info.version`; the alpha auto-bumper will still apply its standard `feat(`→minor `+1` on merge.

- feat(api): **uniform v2 response envelope across the public/PWA API + web PWA + Apple clients** (#1761, completing #1201). The public `api.php` now speaks a versioned contract: a client sending `X-API-Version: 2` (or `?v=2`) gets every response in ONE shape — success `{ ok:true, data:<payload> }`, error `{ ok:false, error:{ code, message, …preserved } }` with a meaningful HTTP status and a stable machine `code` (the #1677 lesson: branch on status/code, never prose). **v2 is opt-in**, so absent/other = legacy v1 bare payloads BYTE-FOR-BYTE — already-deployed and offline-cached web/native clients are unaffected; this is a versioned migration, not a flag day. **One server chokepoint:** `sendJson()` (all ~971 call sites) funnels through the pure, unit-tested `apiEnvelopeWrap()`/`apiEnvelopeError()`/`apiContractVersion()` extracted to `includes/api_envelope.php`; non-`sendJson` responses (streaming/CSV exports, the `health` probe) are legitimately outside the JSON envelope. **Clients updated to match, transparently:** the web PWA's `apiFetchJson()` sends the header (only there — the ~10 raw-`apiFetch()`+`.json()` callers stay v1) and unwraps `data` / throws structured errors with `.status`+`.code`; the Apple `APIClient` sends the header and its transport (`performOnce`) unwraps the envelope once via `unwrapEnvelope()`, so all ~25 `*Decoding.swift` decoders are UNCHANGED. The unwrap condition (`ok:true` + `data`) is collision-free — no natural payload has that top-level shape. **Verified end-to-end against a live server + dev DB:** v1 `songbooks` returns the bare payload; v2 returns `{ok:true,data:{songbooks:…}}` with `X-API-Version`/`Vary` headers; a v2 error returns HTTP 400 `{ok:false,error:{code:http_404-style,message}}`; v1 error stays bare. New contract test `tests/php/test-api-envelope.php` (20 assertions) unit-tests the pure logic AND asserts all three surfaces are wired (rule #35) — mutation-proven across the server wrap, web unwrap, and Apple unwrap. Full PHP suite (106 files) + node suite (50) green; `api-docs.yaml` documents the versioning header + `ApiEnvelope`/`ApiError` schemas. ⚠️ Swift **not compiled** here (no toolchain) — the Apple transport change is defensive/tolerant (non-envelope bodies pass through unchanged) and CI-gated (`swift build`/`swift test`). CSRF-on-every-write and read rate-limiting were already banked; this closes the last breaking part of #1201.

- docs(api): **comprehensive per-action OpenAPI breakout of the v2 Song Editor API** (#1201 core). All **50** `manage/editor/api2.php` actions — previously covered by a single prose block "pending the #1201 breakout" — are now individual `/manage/editor/api2.php?action=…` path items in `api-docs.yaml` (+2246 lines, purely additive), under a new **`Editor API v2`** tag that documents the shared conventions (editor-role + per-action entitlements, the `X-Requested-With` CSRF gate on every POST, the uniform `{ok,…}` envelope, and HTTP-status-based error branching 400/404/409/422). Derived from api2.php's authoritative self-documenting header; the implementer verified every `ed2_requireEntitlement()` by grep (correcting an assumed `edit_songs` gate on `song_external_id_*`, which is editor-role-only) and fixed 5 `operationId` collisions with the legacy v1 editor tag (281/281 unique). The `/manage/api-docs` Swagger UI loads the spec dynamically, so it surfaces automatically. **The contract guard is now widened**: `tests/php/test-openapi-actions-exist.php` replaces its narrow 5-action (#1608) "mentioned in prose" check with a full **tree-derived per-action coverage** assertion — every api2 dispatcher action must have a real path item — with a self-cleaning exemption list (empty today); both directions mutation-proven (delete a path item → RED; phantom doc → RED). **Already-banked scope confirmed** (not re-done): CSRF is on every state-changing endpoint on BOTH surfaces (public `api.php` top-of-file `X-Requested-With` gate + `validateCsrfRequest()`; editor gate), and read rate-limiting exists (rule #28). **The remaining #1201 scope is BREAKING** (uniform envelope across the public read endpoints, versioning, full HTTP-status error normalisation) — it changes response contracts the shipped PWA + native apps depend on, so it is split to **#1761** for an explicit owner decision rather than shipped autonomously. Verified: `test-openapi-actions-exist.php` 10/10 green, `test-orphan-inventory.php` + `test-editor-api2-contract.php` green, YAML parses.

- feat(catalogue): **native apps (Apple + Android) surface the #1741 catalogue expansion** (#1752). Apple (`iHymnsKit`): `SongDetail` decodes the five #1750 identity keys + `externalIds` (all Optional/`decodeIfPresent` so pre-#1750 payloads and persisted offline blobs still decode); one shared copyright fold (`CopyrightDisplay.swift`) for `SongDetail` + `Work` (rule #22); disambiguation/subtitle/rights/externalIds rendered; the `/musician/` deep link now resolves (was the one AASA-claimed path bouncing to Safari) and `CanonicalURL` emits it; `Work` decodes the nine P4b keys; the musician view switches the frozen `credit_person` action → canonical `musician` and renders IPI/ISNI. **Web (the only server change):** `SongData::getMusician()` emits an always-present `identifiers` array from an existence-gated `tblMusicianIdentifiers` read (bound, `try/catch → []`, additive on both envelopes — rule #5/#9), proven live. Android: forward-compatible fields only (no data source yet — the Phase-2 API client is #1756). New guard `tests/test-native-identity-contract.js` (node 49→50) tree-derives the keys, strips nested Swift comments, and holds web↔Apple↔Android in lockstep; a rule-#34 gap (Work-key count hard-`=== 9`) was caught by the adversarial pass and fixed to a `>= 9` floor (mutation-proven). ⚠️ Swift/Android **not compiled** here (no toolchain) — `swift test` / `gradle assembleDebug` are a tracked pre-merge CI step. Deferred follow-ups filed: #1756/#1757/#1758/#1759. No schema change. **This completes the #1741 catalogue-expansion epic's follow-up queue** (#1749/#1751/#1754/#1755/#1750/#1748/#1752) on web + Apple.

- feat(catalogue): **Tune admin CRUD — `/manage/tunes` + `manage_tunes` + `admin_tune_*` API** (#1748). The `tblTunes` family (tunes/aliases/credits/external-links) shipped dormant in #1741 P1 with no curator write path; this adds it (modelled on `manage/works.php`): list/create/edit/delete/merge, aliases + credits (musician typeahead) + external-links sub-panels, all gated on the new `manage_tunes` entitlement (PHP + JS mirror + nav row, held in lockstep by the parity suites). One shared core `includes/tune_admin.php` backs BOTH the page and the four `admin_tune_*` API actions so they can't drift (rule #22/#35). Credit-role vocab centralised (`IHYMNS_TUNE_CREDIT_ROLES`, VARCHAR-not-ENUM); external-links whitelist widened + `IHYMNS_LINK_ENTITY_TYPES` + an AppliesTo tick UI on `/manage/external-link-types`. **Two STRICT-mode correctness bugs the adversarial pass caught + fixed with column-level gating (rule #9):** `tblTunes.Subtitle`/`.Disambiguation` (added by the *tune-enrichment* card) and `tblTuneCredits.MusicianId` (named `CreditPersonId` until *musicians-rename*) were read/written ungated, which throws on a partially-migrated install — now behind a `tuneAdminColumnExists()` probe (`hasTuneEnrichCols` / `hasCredits`-requires-`MusicianId`), **proven live** against a scratch pre-enrichment schema (the ungated pre-fix SELECT throws `Unknown column 'Subtitle'`; the gated path degrades cleanly). Guard `test-tune-admin-surface.php` (tree-derived) hardened to scan the shared core, drop `T_INLINE_HTML` (a JS-comment decoy can no longer satisfy a "PHP references X" check), check both external-links whitelists, and lock in the gating — every assertion mutation-proven. 105 PHP + 49 node green. No schema change.

- feat(catalogue): **public song page + API + JSON-LD surface the #1741 P1 identity fields** (#1750). P1 added `tblSongs.Subtitle`/`Disambiguation`/`FirstPublishedYear`/`CopyrightYears`/`CopyrightHolder` and P5 made them editable, but `song.php` rendered none and the wire lacked them. Now: `song.php` shows a disambiguation parenthetical in the `<h1>`, a subtitle `<p>` (song `lang`/`dir`), a "First published YYYY" line, and a `$copyrightDisplay` **precedence fold** (split years+holder when present, else the legacy `Copyright` denorm — never both) in header/footer/rights-panel; pure server markup, no `<script>` (rule #30). One instance-cached `_songIdentityCols()` probe + one `_songIdentitySelect()` fold feed BOTH read paths (`_fetchSongRow()` + `getSongs()`) with identical always-present normalisers, so the direct and bulk-rendered song paths agree (rule #22). The five keys are now on `song_detail`/`song_data`/`songbook_export`/`bulk_songs` (byte-identical to the editor's `ED2_META_FIELDS` spellings — **this is the #1752 native contract**), ungated by design (`content_gating.php` untouched). New opt-in `include=externalIds` block exposes the recording-ID store (`{idScope,idType,idValue,source}`) while **never** leaking `SourceRef`. JSON-LD gains `alternativeHeadline`/`disambiguatingDescription`/`datePublished`/`copyrightHolder`/`copyrightYear`(bare-4-digit only)/`copyrightNotice`. The song page's tune anchor now prefers the `tblTunes` registry slug (one computation, replacing two duplicated name-folds). New guard `test-song-identity-render.php` derives the column set from schema.sql's `#1741 P1` doctags, comment-strips every source, and (a deliberate upgrade over the P4b guard) uses `tsirVariableUsageCount()` to catch the wrong-but-green where a deleted render survives a bare "quoted key present" check. Verified independently: 104 PHP + 49 node green; live data-layer probe (rolled back) confirmed both read paths + the opt-in block + the empty-case degrade; own mutation proofs (deleted render → RED, exposed SourceRef → RED). No schema change.

- feat(catalogue): **#1749 FULL UNIFICATION — `tblSongExternalIds` is the recording-ID authority, `tblSongs.Isrc` a synced denorm** (epic #1741; owner escalation 2026-08-03 "do full unifications now also"). Builds on the Phase-1 union+dual-write below. **One projection rule** (`SONG_EXTERNAL_ID_ISRC_PRIMARY_ORDER` + `songExternalIdIsrcProjectionSql()` + `songExternalIdUnionArmSql()` — marker-first via `<=>`, then lowest `Id`, `CHAR_LENGTH<=15`) and one store→column "last word" `songExternalIdSyncIsrcDenorm()` (rule #22). `songExternalIdMirrorIsrc()` now returns `?string` and re-projects on **both** return paths — a cleared ISRC **promotes** a manual second-recording row, an inserted one **heals** a pre-cutover mismatch. Panel add/delete + `metadata_field_update` run the sync in-transaction and echo `isrcDenorm` as a response **key-presence** signal (rule #35), consumed by `metadata-tab.js`/`external-ids-panel.js`. The `/isrc/` + `api.php song_by_identifier` + `lyrics_ingest` read paths union the store arm (one predicate, three consumers). New **data-only** cutover migration `migrate-reconcile-isrc-denorm.php` (no DDL) + registry card heals column↔store both directions; its probe doubles as a live drift detector. **Fixes #1755** — the song **merge** now MOVES the duplicate's store rows to the survivor (`UPDATE IGNORE`), DEMOTES its marker so the survivor never carries two, then re-projects, instead of letting `ON DELETE CASCADE` silently wipe them. **D-3 (defensible default, non-blocking):** `tblSongIdentityMap`'s four provider columns stay frozen legacy, not live-synced — shape-incompatible UNIQUE keys, nothing reads them. **Guards (rule #34, all mutation-proven):** `test-song-external-id-mirror.php` assertion 8 rewritten whole-body-count → per-return-path (removing the sync from the cleared path while duplicating it on the inserted keeps the count at 2 but now goes red); assertion 9 rewritten whole-file co-occurrence → per-statement forward-window; new behavioural `test-song-external-ids-reconcile.php` loads the **real** registry probe and drives both drift checks true→false (inverting either turns it red — the probe's drift logic had had no behavioural coverage), skips cleanly with no DB; `test-song-external-ids-backfill.php` slice anchor re-pinned to the entry's own close (the reconcile insertion had made it over-capture). Verified live against the dev DB (rolled-back transaction): mirror/sync/promotion/demotion/merge-collapse/resolver-union-arm all green. No schema change (a D-3 doc-comment only).

- fix(catalogue): **#1741 follow-ups — ISRC resolver union (#1749) · ingest ISRC mirror (#1751) · P4a-3 trio (#1754)**. `/isrc/<code>` now resolves a code held only in the `tblSongExternalIds` store (a curator's manual second-recording ISRC), not just `tblSongs.Isrc` — a parenthesised `OR … IN (subquery)` union, existence-gated, visibility predicate over both arms (#1749). `lyrics_ingest.php` now mirrors its ISRC writes into the store via `songExternalIdMirrorIsrc()` (new optional `$source` param, `'ihymns-ingest'` provenance; the store-path mirrors the **read-back** column value so a COALESCE fill-if-blank never desyncs the store from the column) (#1751). The `/musician/` JSON-LD crawler branch now resolves name-slugs through the shared ladder; `tblMusicianAliases` becomes a 4th resolver rung paired with the musician page's discography widening (an alias URL resolves to the canonical profile without "losing" its alias-credited songs); `SongData::getSongsByCreditName()` documented as the retained scoped-read exemplar (#1754). **The adversarial-verify pass caught three wrong-but-green guard gaps (rule #34) and all were hardened + mutation-proven**: `test-song-external-id-mirror.php` had no comment-stripping (a stale comment satisfied its byte-equality/call-site assertions); the union guard was blind to gate polarity/dead-code (fixed by extracting the pure `_ihymns_resolve_use_store()` decision + a direct polarity test + an assign-from-gate assertion); the musician guard was blind to a discarded resolver return (fixed by a result-flows-to-`$personSlug` assertion). No schema change.

- feat(catalogue): **writer page consolidated into the musician profile** (#1741 P4a-3, owner decision D4) — the old heuristic `/writer/<name-slug>` bare song-list is retired in favour of the registry-backed musician profile. A top-level `/writer/<slug>` hit now gets a **real HTTP 301** to `/musician/<slug>` (in `index.php`, mirroring the `/people|/person` 301); the SPA fragment serves the musician page directly and `router.js` soft-canonicalises the address bar via `history.replaceState` (the #1343-B `data-*` marker pattern). A **single shared resolution ladder** (`musicianResolveLegacySlugDb()` in `musician_helpers.php` — exact registry slug → `slugifyMusicianName()` punctuation/diacritic fold → registry `Name IN (…)`, fail-open, reused not re-forked) maps a legacy name-slug to its registry row; the same ladder also fixes name-slug `/musician/` credit links from song pages. `writer.php` is deleted; its no-registry-row fallback is subsumed by the musician page's six-role discography, widened to the historic writer name-variant set so a hyphenated credited name still lists (no silent 404, rule #33). The sitemap now emits registry-driven `/musician/<slug>` rows. Also fixes **#1753** (the musician-page Edit button, whose reveal had been stranded in `router.js`'s `page==='song'` branch and never appeared). Two new mutation-proven CI guards (`tests/test-writer-musician-route.js`, `tests/php/test-musician-slug-resolve.php`). Zero schema changes. **This completes the #1741 catalogue-expansion epic's development work** (P1–P6 + P4a-3).

- feat(editor): **song-editor identity fields + tune typeahead + TuneName↔TuneId lockstep** (#1741 P5, #1749) — the v2 editor Metadata tab can now enter the #1741 P1/D5 identity columns (ISRC/subtitle/disambiguation/first-published-year/copyright year(s)/holder), each 409-gated on an un-migrated install; an ISRC edit stays mirrored into the D5 `tblSongExternalIds` store (#1749 dual-write, ownership-keyed so manual rows survive). The plain "Tune Name" text box is replaced by a **bespoke live-search control** backed by a new `tune_search` typeahead (alias-JOINed, metre-filtered via the new pure `ihymns_meter_normalize()` fold) and the **one tune write core** `ed2_songTuneApply()` (`song_tune_set`) — so `TuneName` and `TuneId` are ALWAYS written together and a tune edit can never again strand the registry link (the drift this phase retires). The same lockstep rider is wired into the whole-song save (`save_song_core.php`), the bulk importer (`song_importers.php`), **and the revision-restore path** (`ed2_applySongSnapshot()` now funnels `TuneName` through the same core instead of writing it alone via the scalar loop). The shared `place-search.js` module is GENERALISED (additive `searchUrl`/`parseResults`/`pickMode`/`noun` options, byte-equivalent defaults — never a fork) to back the tune control while leaving the four pre-existing place pickers unchanged (#1594 keyboard/ARIA machinery untouched). New tree-derived, comment-stripped, mutation-proven CI guards: `tests/php/test-tune-lockstep.php` (every `tblSongs.TuneName` write site references the funnel, incl. the restore path) and `tests/test-tune-typeahead-ui.js` (the client control + `place-search.js` default byte-equivalence). Zero schema changes.

- feat(editor): **recording external-ID card-list on the Metadata tab** (#1741 P5b) — the D5 `tblSongExternalIds` key/value store gets its first UI read+write path (`song_external_ids`/`_add`/`_delete`), a card-list whose provider dropdown is built entirely from the `RECORDING_EXTERNAL_ID_TYPES` vocabulary (zero hardcoded slugs), 409 on an un-migrated install, `IdScope` server-derived, `isrc` canonicalised. Curator rows (`Source='manual'`) are distinct from the mirror/backfill rows.

- feat(catalogue): **comprehensive recording IDs — `genius` type + grandfathered-data backfill** (#1747 D5, owner-confirmed) — `tblSongExternalIds` is now the comprehensive single home for recording IDs: a new idempotent migration (`migrate-backfill-song-external-ids.php`) mirrors `tblSongs.Isrc` + the four grandfathered `tblSongIdentityMap` columns (`MusicBrainzRecordingMBID`/`SpotifyTrackId`/`GeniusTrackId`/`IsrcCode`) into the key/value store via `INSERT IGNORE` (idempotent, re-runnable, creates no schema object). Adds a `genius` `RECORDING_EXTERNAL_ID_TYPES` entry (the vocabulary that column always lacked). Data-derived migration probe checks every source (no false-green). Grandfathered columns untouched — still read directly; ongoing dual-write consistency tracked as #1749.

- feat(catalogue): **Tune page keyed on the `tblTunes` registry** (#1741 P4c) — `/tune/<slug>` now reads the real registry (Subtitle/Disambiguation/Meter badge + "Tunes with this meter" cross-links, MusicBrainz/Hymnary chips, composer/arranger/harmoniser/source credits, external links) via a lookup ladder (exact slug → name-fold → alias → the original heuristic **preserved verbatim** so every existing `song.php`-emitted `/tune/` link keeps resolving, rule #33). Uncached, no inline script (rule #30). Consumes the shared external-links panel.

- feat(catalogue): **Work page + admin CRUD surface the identifiers** (#1741 P4b) — `/work/<slug>` gains breadcrumb, subtitle/disambiguation, ISWC/CCLI/BOWI chips that cross-link the P3 `/iswc//ccli//bowi/` resolver, a tune link, a first-published/copyright line, and aggregated writer/composer/arranger credits; `/manage/works` gains editable fields for all of those (+ a MusicBrainzWorkMBID field), validated through the shared `WORK_IDENTIFIER_TYPES` vocabulary (no inline regex, rule #35), persisted via one column-gated UPDATE with `TuneName`+`TuneId` kept in lockstep by the new shared `tuneFindOrCreateByName()`. Extracts the categorised external-links panel into one shared partial (was inlined per page).

- feat(catalogue): **Musician profile page + type/relations/identifier enrichment** (#1741 P4a-1/-2) — `/musician/<slug>` gains an entity **Type** (person/group/character/orchestra/other, VARCHAR-not-ENUM), an editable **Biography** (falling back to the internal Notes when empty), a **Disambiguation** line, and **"Portrayed by"/"Portrays"** relations with dates (e.g. a character persona and its performer); its IPI/ISNI identifier chips now cross-link the P3 `/ipi//isni/` resolver. `IsGroup`/`IsSpecialCase` become derived mirrors written through **one** funnel (`musicianTypeApply()`) — no other code writes them (CI-guarded). `/manage/musicians` gains the matching editor fields + a relations sub-panel (`validateCsrfRequest()`). The categorised external-links panel is now the shared partial. (The `/writer/`→`/musician/` page consolidation, P4a-3, is held pending an owner decision.)

- feat(catalogue): **browsable external-identifier alias URLs** (#1741 P3, epic #1741 / #1747) — the D5 external-ID storage (`tblSongExternalIds`, `tblWorks.Bowi`, the `media_identifiers.php` vocabulary) is now reachable by URL: `/iswc/<code>`, `/ccli/<code>`, `/bowi/<code>`, `/isrc/<code>`, `/ipi/<value>` and `/isni/<value>` all resolve through ONE normaliser (`includes/identifier_normalize.php` — a per-scheme canonical fold registry; the ISWC fold is extracted from `manage/works.php`, which now delegates, killing the duplicate strip the old page carried), ONE resolver (`includes/identifier_resolve.php` — Work-first for iswc/ccli/bowi, song multi-match for iswc/ccli/isrc reading `tblSongs.Isrc` not the identity-map's unique key, musician list for ipi/isni) and ONE page (`includes/pages/identifier.php`, which absorbed and replaced the old `iswc.php`) — not five near-copies (the modularity rule the epic called out). The resolver is table/column-existence-gated so a pre-migration install degrades to an empty result rather than throwing under mysqli STRICT. Guarded by `tests/test-identifier-routes.js` and `tests/php/test-identifier-normalize.php`, both tree-derived from the scheme registry and mutation-tested.

- fix(editor): **v2 `create_song` never recomputed `tblSongbooks.SongCount`** (#1742, finding A3 of the #1737 audit) — the v2 editor is the default, and its create handler did no count recompute at all (`grep -c SongCount manage/editor/api2.php` was 0). Two mechanisms keep that cached count honest: the #791/#793 `trg_songs_songcount_*` DB triggers and an app-level recompute — and iHymns targets shared hosting where `CREATE TRIGGER` is frequently denied, so on the actual deployment target the app-level recompute is the *only* one that exists. The result was invisible on every dev/CI/test box (they have the triggers) and wrong in production: a brand-new songbook's home tile never appeared, because the tiles gate their render on the denormalised `SongCount` and it stayed at 0. The same recompute UPDATE existed inline in three places (a modularity-rule violation), so the fix extracts one shared, `songVisibleSql()`-aware helper — `songbookRecomputeSongCount()` in `includes/songbook_count.php` — and routes `save_song_core.php`, `song_relocate.php` **and** the previously-missing `create_song` through it (post-commit, best-effort). Proven live with the three triggers dropped: a bare insert leaves the cache stale (0 vs a real count of 1), the helper corrects it with no trigger present. Guarded by `tests/php/test-songbook-count-recompute.php` (behavioural double + tree-derived wiring guards), mutation-tested four ways.

- fix(import): **rejecting an unparseable OpenSong file still opened a database connection** (#1740) — `_bulkImport_processOpenSong()` called `getDbMysqli()` + a `MAX(Number)` lookup *before* parsing, because the per-songbook auto-number was a plain `int` argument to the parser; its sibling `_bulkImport_processOpenLp()` parses first. The 5th parser argument is now a `callable $autoNumberProvider` invoked lazily — only when the document actually lacks both `<hymn_number>` and a filename-hint number — so a malformed file is rejected with no connection, matching the OpenLp shape. `tests/php/test-xml-import-routing.php` is now genuinely DB-free (proven with MariaDB stopped: all 15 assertions pass), which its doc-block had once falsely claimed.

- feat(editor): **v2 credits tab — structured First / Surname / Suffix entry** (#960) — the v2 editor could not split a credited person's name into registry parts the way v1 could, so credit writes flattened to a single string. `credit_upsert` now accepts `{name, first?, surname?, suffix?}`, and the shared `creditEntryNormalise()` / `creditPersonPromote()` pair (extracted so `credit_upsert`, revision-restore, the legacy whole-song save and lyrics-ingest all promote identically) writes proper `tblCreditPeople` registry rows. The endpoint returns the registry parts it resolved, never an echo of the client's input.

- feat(editor): **v2 cross-book counterparts** (#1608) — the five song-link actions (`song_links`, `song_link_add`, `song_link_remove`, `song_link_suggestions`, `song_link_suggestion_dismiss`) are ported from v1 into `manage/editor/api2.php`, and the Links tab gains a counterparts panel, so declaring that two `tblSongs` rows in different songbooks are the same hymn no longer requires the retiring v1 editor. Documented in `api-docs.yaml` (the v2 prose block, pending the #1201 per-action breakout) and guarded by the v1→v2 action-parity ledger in `test-editor-api2-contract.php`.

- fix(import): **a single `.xml` upload was hard-routed to the OpenLyrics parser** (#882) — so a real OpenSong document always failed with OpenLyrics' own "no `<title>`" error (OpenSong has a title, just not where OpenLyrics looks). A new `_bulkImport_processXmlAuto()` picks the primary parser via the one shared `_bulkImport_looksLikeOpenLyrics()` discriminator the ZIP-import loop already uses (so a single-file and a ZIP-entry decision can never disagree), and on a primary-parse miss tries the other parser once before giving up with a combined, both-formats-named error. Wired into both editors; guarded by `test-import-format-coverage.php` (UI list ↔ api2 set ↔ parsers) and `test-xml-import-routing.php`.

- test(annotation): **a baseline guard so the annotation program (#1158) can't regress** — `tests/php/test-annotation-coverage.php` hard-asserts, tree-derived and green today, that every source file under `includes/`, `manage/includes/` and `js/` (208 files, 100%) opens with a block doc-block, so the measured ~100% doc-block coverage can't silently erode as new files land. Labelled-register (ELI5/Why) adoption is printed as an advisory backlog signal (45%, matching the audit) and never gates the build — raw comment density is a proven-misleading proxy (template shells score low while being fully annotated), so the file-by-file backfill of the large logic files stays a human-reviewed program. Mutation-tested both ways.

- feat(intapps): **the MWBM-IntAppsAPI gateway integration lands, entirely dormant** (Epic #1725) — a server-proxied, cache-first, fail-open client for MWBM's shared feature-flag gateway, shipped with zero observable effect: `tblAppSettings.intappsapi_enabled_channels` ships with no row at all, and with it absent the whole thing is a byte-identical no-op, proved against the real local instance (six endpoints diffed merge-base-vs-branch-tip with an empty result, zero `tblIntAppsSync` references across those requests in MySQL's own query log, and three deliberate mutations — forcing the new `remoteFeatures` field to always emit, removing the enablement guard on the cache read, and pointing a capture at the extensionless `/api` route that has no router script locally — each proven to turn the relevant check red before being reverted). **The signer is the whole point of the exercise**: the gateway's own five bundled client examples sign the WRONG canonical string (`METHOD|PATH|TIMESTAMP|BODY`, filed upstream as MWBM-intAppsAPI#120) — `intappsSign()` is implemented against the gateway's actual verification source (`HmacValidator.php`, pinned commit `6816ed8`) instead, and a checked-in local stub gateway (a faithful, line-for-line port of that same source, structurally unable to ever deploy — it lives outside every directory the deploy workflow mirrors) proves the real client's signature is accepted end-to-end over real loopback HTTP, and that the vendor's own wrong examples are correctly rejected. **The pre-launch stress test's one BLOCKER** — an enabled-but-un-migrated docroot hitting the missing `tblIntAppsSync` table on the public home page under mysqli's STRICT reporting mode — is fixed with a try/catch belt over an INFORMATION_SCHEMA-probe brace on every table access, reproduced and proven live (renaming the table away under load returns a graceful degrade; removing the guard reproduces the real HTTP 500 verbatim in the server log, then the guard is restored). A second real bug was found and fixed while building the end-to-end proof: the single-flight refresh lock was held for its full 10-second window regardless of how fast the actual fetch completed, so the admin "Refresh now" button would report "another refresh in flight" for up to 10 seconds after the PREVIOUS click had already finished — fixed by releasing the lock the moment each attempt's outcome is committed, success or failure. Enablement is a per-channel allow-list (the same mechanism `apple_web_login_enabled` already uses), so alpha can canary the integration without touching production on the shared database. One real consumer ships in the same batch — the Song-of-the-Day card's presence on the home page — to prove the flag pipeline is exercised end to end, not merely renderable, per the stress test's own warning against shipping a kill switch nothing calls.

- fix(web): **three advertised features were silently dead** (#1714) — a pre-release correctness sweep hunting the silent-no-op class, and all three findings share one root cause: **#812 removed a UI element and left its consumers behind.** (1) `/` and `Ctrl`/`Cmd`+`K` did nothing at all, while both are still advertised as "Open search" in the shortcuts overlay *and* on `/help` — and because `preventDefault()` ran first, `/` did not even type a slash. (2) **On the last song of every songbook, `→` navigated backwards**: the handler selected positionally with `a:first-child` / `a:last-child`, and since the next slot rendered as *nothing* rather than a placeholder, the container held a single child — which satisfies both selectors. Users oscillated between the final two songs. (3) Pressing **Enter in the `/search` box reloaded the app and discarded the query**: the form has one field, no submit button and no `action`, which per the HTML spec's implicit-submission rules means Enter submits it, and the Enter handler was bound to the field #812 deleted. The form now also carries `action`/`method`/`name`, so the no-JS path is correct rather than destructive. Three guards added, each derived from the tree — and **two were wrong-but-green until mutation-tested**: one counted markers in raw source and was being satisfied *by its own explanatory comment*, which contained both literals it looked for; the same weakness then turned up in a second guard, where **23 ids in this tree exist only inside comments**. Also found and left as product calls: the public song page fetches `$row['tags']` on every read and never renders them (#288), and search autocomplete (#307) is fully implemented, endpoint and all, but reachable from nowhere — **with a test that has always passed, because it builds its own input and drives the function directly.** A passing test over unreachable code is the most misleading artefact in this repo.

- fix(a11y): **turning High Contrast ON made the app less legible** (#1150, #1151) — the WCAG 2.2 AA pass for the public release, and its headline finding is that the accessibility feature was *causing* the accessibility failure. `accessibility.css`'s high-contrast block only ever redefined **Bootstrap's** `--bs-*` tokens, while `app.css` carries a second parallel set (`--text-primary`/`--text-secondary`/`--text-muted`, `--glass-bg`, `--card-border`) that dozens of components read directly and which stayed at light-theme values. The bottom navigation — the most-used control in the app, on every page in every auth state — computed to **~1.8:1**, down from a compliant ~4.7:1 in ordinary light mode; every header icon to **~1.44:1**. The cascade detail is worth keeping: `.btn-header-icon` declares its *own* `color`, and an ancestor's `!important` only wins for elements the ancestor selector matches, so `.navbar { color:#fff !important }` never reached it. Fixed at the shared source rather than per-component, and scoped entirely inside `[data-ihymns-contrast="high"]` so the normal light/dark design cannot be affected. Second critical: **card reordering had no keyboard alternative at all** (2.5.7 Dragging Movements, new in 2.2) — and `enterEditMode()`'s own doc-comment claimed "Move up/down" buttons existed. `git log -p` confirms they never did, since the original #448 commit; a comment describing a feature that has never existed is worse than no comment, because it stops the next reader checking. Real Move buttons added on both layout surfaces, mirroring the already-compliant `setlist.js` pattern. Also: `home.php` had `aria-hidden="true"` over a strip hosting real focusable buttons, so a screen-reader user could land on a control announced as nothing at all; orphaned radios and checkboxes with no accessible name (a `<th>` labels a *column*, never a control); ~25 icon-only buttons relying on `title`, which is never exposed on keyboard focus; and register/reset password fields advertising `current-password`, which discourages password managers from offering to *generate* one. **#1150/#1151 stay open deliberately** — there is no browser in the build container, so every ratio is derived from the CSS cascade and no real screen reader was involved; closing them on a static pass would be exactly the false completeness this branch keeps finding.

- fix(security): **a same-origin hole the sweep was not sent to find** (#1709, #1712) — fixing #1709's non-standard-port bug surfaced a second, more serious one in the same function: because the port was dropped from `Origin` and never compared, `https://example.com:9090` matched a deployment on `example.com:443`. Port is part of an origin (RFC 6454 §4), so anything an attacker could get answering on another port of the same host inherited the ability to forge state-changing writes. The fix resolves the port against the scheme the header itself states, never one we infer — behind a TLS-terminating proxy the origin process cannot reliably tell http from https, and guessing would reject every legitimate write. Also fixed: the debug cookie was minted without `Secure` behind a TLS proxy — **the third site of a defect #1648 already fixed**, whose own comment says the two cookie helpers disagreeing "was the actual defect" (a fix applied at two of three sites is how a class survives being fixed); an incomplete shared HTML escaper; and `delete_song` using `validateCsrf()` alone, which added no security at all (the file-level gate already requires the robust check) while contributing rule #29's sporadic stale-token failure — now much more visible since #1695 widened that endpoint to editor+. Verified clean across the board: zero SQL injection over 463 interpolated sites, zero auth gaps across all 64 admin pages, zero XSS, uploads stored outside the docroot with byte-sniffed MIME and no zip-slip.

- fix(ci): **the ESLint step had linted nothing for its entire life** (#1711) — a CI step named "Lint JavaScript (ESLint)" probed for a config, found none (there never was one), and took an `else` branch running config-less `eslint@latest`, which since ESLint 9 does not lint but *crashes*. `|| exit 0` swallowed it, so the job went green. `package.json` advertised four lint scripts while eslint, stylelint and htmlhint were **none of them** dependencies. A green tick for zero checks, on every run. Now real: a bug-catching config (deliberately not a style enforcer — a mass auto-format would bury real history in `git blame` for zero correctness gain), CI fails on error, and the gate was proven able to go red. `require-atomic-updates` is off with the reason recorded, because it flagged the idiomatic disable-button-await-re-enable pattern six times, all correct code. Eight real findings fixed, one of them a trap: deleting an "unused" `const format = readInt(2)` in the MIDI parser would have **corrupted every parse**, because `readInt()` advances the cursor.

- feat(songs): **curators can delete songs again, and the widen actually takes effect** (#1695, stage 3 of epic #1692) — `delete_songs` returns to editor+. It was narrowed to admin-only in stage 1 for one stated reason — a delete was permanent and took the revision history with it — and that comment ended by promising to revisit the line once deletion became recoverable. #1694 made it recoverable, so the reason is gone. The parity suite supplied the mutation drill for free: flipping the two maps and running it **before** touching any fixture turned the E1 baseline row and MUT-6 red unprompted, which is precisely what "the suite said so, loudly, the moment the default moved" was written to mean. MUT-6 has now flipped direction for the second time, and the regression it catches is henceforth a silent *narrowing*. **The half the issue itself missed:** `effectiveEntitlements()` lets a stored override replace the code default outright and `saveEntitlementOverrides()` writes the whole map on every save, so on any install where `/manage/entitlements` has ever been saved, changing the default in PHP is a **silent no-op** — the code says editor+, the row says admin+, and the row wins. Hence a data-only migration, and it is deliberately **conditional**: in the database, "the interim value we shipped" and "the operator locked deletion down on purpose" are the same bytes, so a blanket clear would silently *widen* a privilege on a site that had decided otherwise. It prunes only the exact stage-1 value; a `global_admin` lockdown, a custom set or an already-widened value are all preserved, and unparseable JSON is reported rather than rewritten. Deletion and restore now **notify every `purge_songs` holder** from inside the write core rather than from the endpoints — a safeguard invoked by callers is one a future funnel forgets, which is exactly how #1694 found the delete cascade duplicated across two endpoints. It is post-commit and never fatal: a missed notification is a degraded safeguard, a failed delete is a broken feature. The actor is excluded, so a lone admin deleting a song notifies nobody. One bug caught before shipping: filtering the settings push-kind list server-side on `$currentUser['role']` would have hidden the switch from *everyone* including admins, because `api.php` never sets `$currentUser` (now filed as #1710) — the server states the requirement and the client, which knows the auth state, applies it.

- fix(tests): **the shared unit walker mis-named closures and minted phantom `match` units** (#1703, #1707) — `tests/php/lib/php_source_units.php` had **no test of its own** while already underpinning four suites, which is the worst possible place for an untested component: a wrong answer is invisible in every consumer at once, and each consumer reports its own green tick. Three defects, two root causes. The naming rule accepted the next `T_STRING` after `function` wherever it landed, so `function ($a, int $b)` became the unit `int` and `function (): bool` became `bool` — a name can only sit *between* `function` and its `(`. And a `match` arm's `default =>` tokenizes as `T_DEFAULT`, the same token `switch`'s `default:` produces, so the case-splitter minted a phantom unit part-way through a real case; now tracked by match brace-depth, ORed with a `=>`-vs-`:` lookahead because the two signals are independent and each alone has a blind spot. **Both issues judged these latent; they were already live on the tree** — measured before and after, 10 files minting type-named units went to zero, and `case default` units went 34 → 18, i.e. **16 phantom units removed** (`api2.php` alone 5 → 1). Nothing was producing a wrong *result*, which the issues correctly observed: where a `match` split a case, the SQL and the marker vouching for it happened to land in the same fragment. "Happened to" was the whole problem — the failure is directional, and a vouching call separated from its SQL by an intervening `match` yields a **false RED**, which per rule #34 is how guards get weakened or deleted rather than fixed. The nastiest instance is #1703's: a closure whose parameter type shares a name with a real function, **declared first**, took that function's unit name outright, so an assertion reading `$units['validate']` read the closure's body while believing it read `validate()` — no error, no warning, and which one won decided purely by declaration order. The new `tests/php/test-php-source-units.php` (27 assertions, `tests/php/lib/`'s first direct test) was written and run **red before the fix** — 10 red, each for the predicted reason, including one file emitting `case default` *and* `case default#1` where the phantom had stolen the genuine label's name. It pins the anti-over-correction cases too (a real `switch` default still mints its unit; abstract declarations don't swallow the next function; an arrow function doesn't swallow what follows), and derives its corpus from the tree so a type-named unit anywhere under `appWeb/` fails the build by name.

- feat(songs): **deleting a song is now recoverable** (#1694, stage 2 of epic #1692) — until now, Delete in the editor ran a cascade `DELETE` that took the song, its components, credits, media links, tags **and its entire revision history**, with no undo anywhere. A soft delete replaces it: five additive columns on `tblSongs` (`IsDeleted` + who / when / why / note), and `WHERE IsDeleted = 1` **is** the queue — no workflow tables, because the adversarial pass on the plan could not produce a question the row itself did not already answer. The new `/manage/deleted-songs` lists them with **Restore** (one `UPDATE`, and that genuinely is the whole recovery — a soft delete writes nothing else anywhere, so favourites, set lists, Work membership, revisions and redirect chains were never touched and need no repair) and **Purge**, the old destructive cascade, which now lives in exactly one place and is reachable **only** from the deleted state. Nothing can hard-delete a live song in one step any more, by anyone. Purge is gated on its own new `purge_songs` entitlement rather than `delete_songs` — the two are the same role set today, so the split reads as academic, but #1695 widens `delete_songs` back to editors and without a separate key the irreversible operation would silently widen with it (the alternative, a hardcoded role check, is on the review red-flag list). Purge keeps a server-enforced type-to-confirm (the #1218 §2 pattern, hardened: the client arms the button, but the POST itself must carry the typed id, so a replayed or hand-crafted request cannot skip the ceremony) and an optional relink target, chosen at purge time because that is when the redirect write actually happens. The read-path sweep is the bulk of the risk and was done as a census, not a grep, and a CI guard now re-derives it from the tree on every run rather than trusting a list anybody typed (rule #34): **109 `tblSongs`-reading units across 189 files, every one of them either filtered or explicitly marked**. 53 carry a `@deleted-visible` marker because showing hidden rows there is *correct* — `/manage/deleted-songs` itself, obviously, but also `getMissingSongNumbers()`, which deliberately counts a hidden song's number as **occupied**, because `(SongbookAbbr, Number)` on `tblSongs` is a plain index and not unique: filter there and a curator fills the "gap", someone hits Restore, and the songbook quietly has two songs on one number with nothing in the database to stop it. `admin_export` emits `isDeleted` across JSON, CSV and XML behind a column-existence probe, so an un-migrated install exports exactly what it does today. 107 assertions cover the lifecycle cores — 39 on the pure surface (SQL-fragment minting, the reason vocabulary, the one refusal verdict, and nine hostile table aliases including the `$`-vs-`\z` PCRE trailing-newline trap) and 68 across five behavioural modes run against a mysqli double: un-migrated, partially applied, migrated, purge, and the one that matters most — when the `INFORMATION_SCHEMA` probe **itself** fails, reads degrade to today's behaviour but writes **refuse**. "I could not check" must never become a write.

- test(ci): **the bare-`fetch()` guard was scanning two subdirectories and stopping at the first regex literal** (#1696, #1700, #1701) — three defects found in sequence from one small deletion. `songbookIsOfficial()` in `js/constants.js` had **zero callers anywhere in the tree** — written as the enabling helper for #1531 part 2 and never wired — so it reads to the next person as though part 2 were half-built; deleted, along with the registry field only it consumed (the API still sends `isOfficial`, so re-adding is one line when part 2 is real). It survived because `test-orphan-inventory.php` derives its corpus from dispatch surfaces, schema tables and entitlement labels, and a plain exported JS helper is none of those — whether that guard should grow an unimported-export check is now tracked as #1702 rather than left to the next audit to rediscover. Pulling that thread exposed two bugs in `test-api-client-usage.js` itself. Its scan list was `js/modules/**` + `js/utils/**`, one directory level short of the tree it means to cover, so `js/app.js` and `js/constants.js` had never been scanned at all — and `constants.js` did contain a bare `fetch()`, which **must stay one**: `/api?action=songbooks` language-filters its response, and that call populates a NAME LOOKUP, so routing it through `apiFetch()` would filter the registry to the user's chosen languages and make every list view silently fall back to the bare abbreviation for any book outside the filter. It is allow-listed with that reason recorded, so the obvious "widen the scan, fix what it finds" cannot break it. Far more serious: the guard's comment/string stripper had **no state for a regex literal**, so `replace(/"/g, …)` opened a string state that never closed and **blanked the rest of the file** — reported as zero violations, not as an error. Twelve files ended the walk broken, ten of them already in scope, including the modules that actually make API calls; the guard had reported green for a year while scanning part of each. (Checked: the newly-visible regions contained no hidden violations — the defect was the blindness, not a backlog.) Fixed with regex-literal handling plus, more importantly, a new assertion that **fails the build whenever a file's scan derails** — a heuristic has gaps, and this turns every gap into a loud failure somebody must look at instead of half a file's coverage reading as all of it. Nine new self-tests cover the quote-in-regex shapes and, equally, four division cases proving the inverse error is not made; four mutants against the real tree confirm each part can fail, including one placed in the exact position the old scanner could not reach.

- fix(songs): **an old link to a moved song can no longer land on a different song** (#1689) — looking up a song by an id that no longer exists fell back to matching on the song *number* in that songbook. Numbers are not unique in the database and a song's number can be edited independently of its id, so for a song that had been moved, merged or removed, that fallback could match whatever now occupies the number and return it as a perfectly ordinary success — the wrong song, HTTP 200, no indication anything had been substituted, and nothing in any log. The forwarding note that knew where the song had actually gone was only consulted *after* the fallback had already answered. The redirect layer is now asked first, and a claimed id skips the number match entirely so the existing forwarding path answers instead — meaning the caller still learns that the id changed (and can update what it had stored) and a removed song still reports "removed" rather than "not found". A forwarding note that says only "this was removed" counts just as much as one that points somewhere new: without that, the number match would put a different song in the removed one's place. This was always possible, but became much likelier when moving a song between songbooks started re-keying it (#1679). The extra lookup happens only on requests that had already failed to find anything, and on an installation where the forwarding table has not been created yet, behaviour is unchanged.

- feat(accounts): **closing an account disables it rather than destroying it, and owned content works that out at read time** (#1698, closes the orphan half of #1697) — an account is now one of three states: **Active**, **Disabled** (reversible, data intact) or **Erased** (an anonymised tombstone). Before this, "delete" was a hard row delete in two places, which **destroyed** the user's own set lists outright (`ON DELETE CASCADE`) while **orphaning** their published templates (`ON DELETE SET NULL`) into rows literally nobody could edit or remove without database access. Both states are now offered and, per the owner's requirement, clearly distinguished on screen — previously anything not active rendered identically as "Disabled", so an admin could not tell a reversible switch-off from an irreversible erasure. Content owned by a locked account becomes **visible but read-only** rather than vanishing: a collaborator on a disabled owner's set list keeps `canRead`, loses `canWrite`, and is told why, so third parties are not punished for somebody else's account closing. Reactivation is one `UPDATE` and every surface is correct on its next read, because the lock is **derived, never stamped onto rows** — a stamping pass leaves an account half-locked if it is interrupted, has to be exactly inverted on reactivation forever, and silently misses every new table that grows a user foreign key. The erasure path derives what to do with each referencing row from the foreign key's own `ON DELETE` rule rather than a hand-typed table list, and **refuses** rather than improvising when it meets a rule it has no policy for (`RESTRICT`, `NO ACTION`, an empty clause, or a rule from a future MySQL). Every bearer token and the stored Apple refresh token go with the account. Two entitlements arrive with it: `manage_setlist_templates` (the admin override that makes an author-less template manageable again) and `publish_public_templates`, which gates `is_public = 1` in **both** template write paths — the other half of #1697. An erasure requires the username **typed** to confirm (#1218 §2), because a mis-clicked row and the right one otherwise produce an identical "are you sure?". Almost none of this is new enforcement: `getAuthenticatedUser()` has always filtered `IsActive = 1` and `manage/includes/auth.php` carries thirteen more enforcement points, so both inactive states were already locked out of every authenticated path — adding a second gate would have meant two checks that must agree with nothing making them. 100 behavioural assertions, mutation-tested; the surfaces were enumerated from `schema.sql`'s `REFERENCES tblUsers` rather than guessed, and the partition over them is proven exhaustive.

- fix(editor): **one stale foreign key no longer blocks every songbook move** (#1690) — the move's cascade pre-check answered ONE schema-wide question and memoised it, so an install with a single non-cascading FK could not move a single song, even though a RESTRICT `fk_media_song` can only endanger a song that actually HAS a media row. It now refuses **per song**, only when that song really has rows in the affected child table (the owner's option A); the `INFORMATION_SCHEMA` catalogue is still read once per request, so a healthy install pays **zero** extra queries. The check was also structurally blind to the opposite drift: a column with no foreign key at all leaves no `INFORMATION_SCHEMA` row, so "everything I found cascades" was vacuously true on an install that never ran `migrate-song-softref-fks.php` — and a move then **silently stranded every `tblSongRevisions` row** for that song, with no error, no rollback and nothing to grep. A missing FK is now detected against `SONG_RELOCATE_EXPECTED_SONGID_FKS`, the 41 declarations derived from `schema.sql` and pinned to it by CI. Two migration probes were fixed with it: `songid-prefix-fixup` sat permanently in the "already applied" expander on any install with clean prefixes (so the one card that repairs those four FKs was never offered), and `song-softref-fks` keyed off a single "representative" constraint, reporting a partial apply as complete. Both now source their constraint names from the same const, so a probe, a refusal and a migration cannot drift.

- feat(push): **iHymns can now actually send a push notification** (#311 / #1671 F6) — `tblPushSubscriptions`, `push_subscribe` and `push_unsubscribe` have existed since #311 and **nothing had ever sent a push**: there was no VAPID keypair, no payload encryption and no service-worker handler, so a subscription was a permanently untested surface that read as working. All three links now exist. `includes/web_push.php` implements VAPID (RFC 8292, ES256) and Web Push payload encryption (RFC 8291 over RFC 8188 `aes128gcm`) directly on `openssl`, because this project has no Composer — and it is proven correct against **RFC 8291 §5's own published example vector**, byte for byte, rather than against itself. Messages are encrypted end-to-end: the push service that relays them cannot read the contents. Settings gains a Notifications card (on/off for this device, plus one checkbox per notification kind), `/manage/notifications` gains VAPID key generation, a broadcast form and "send a test to my devices", and the service worker gains `push` + `notificationclick` handlers that reuse an open tab instead of piling up new ones. **Entirely dormant until an operator generates a keypair**, and per-user opt-ins live in the `ihymns.push` settings namespace so adding a notification kind is one line of PHP. ⚠️ No push has yet been delivered to a real device — the cryptography is verified, the delivery path is not.

- feat(setlists): **set-list templates work, and a service plan now survives syncing** (#301 / #1671 F4) — applying a template to a set list used to produce a running order that the very next sync silently discarded, for signed-in users only; that is why the templates UI was deliberately not built in the previous pass. Set lists now keep their plan (`tblUserSetlists.SlotsJson` — run the "Set-list service plans" migration), the "From Template" menu that had been hidden and unreferenced since #301 works, each song slot is filled from a plain dropdown of the set list's own songs, and "Save as template" turns a set list you have built into a shape you can reuse. Templates can now also be **renamed and deleted by whoever created them** — previously every template, including a public one visible to everybody, was permanent and uneditable from the screen that made it. Over-long service orders are **refused** rather than silently shortened.


- feat(devices): **you can finally see — and sign out — the devices signed in to your account** (#1671 F1, remediation Batch 8) — `action=devices_list` and `action=device_signout` shipped complete and hardened in #1409/#1511 (a CSRF gate, a per-**user** rather than per-IP rate limit, and a DELETE whose `UserId = ?` sits inside the WHERE so it can only ever reach the caller's own tokens) and then had **zero callers** on web, Apple and Android alike. Somebody who signed in on a borrowed laptop had no way to revoke it. Settings → Profile now carries a "Signed-in devices" card listing each session with its platform, app version and when it was last seen, a "This device" badge on the one you are reading, and a Sign out button on every other. The current device deliberately gets **no** button — revoking it works, but it is indistinguishable from the Sign Out three inches up the page. Signing a device out is destructive, so it confirms first, announces the outcome through the shared assistive-technology announcer, and moves focus to a control that still exists rather than dropping it to `<body>` and losing a keyboard user's place. Failure kinds are told apart by **HTTP status**, never by matching the server's sentence (rule #35).

- feat(requests): **you can now find out what happened to a song you requested** (#1671 F2, remediation Batch 8) — `action=my_song_requests` has worked since #280 with **no caller anywhere**, while `song_request_submit` has always linked a submission to the signed-in user and curators on `/manage/requests` have been moving `Status` through pending → reviewed → added / declined and filling in `ResolvedSongId`. All of that was written and never read: you could send a request into silence and never learn the outcome. The `/request` page now carries a "Your requests" list below the form — each request with its status, a plain sentence saying what that status means, and a link straight to the song once one is added. It refreshes the moment a submission succeeds, so a request you just sent appears immediately rather than looking lost. Signed-out visitors get the sign-in prompt instead of a hidden section, because hiding it teaches nobody that signing in is how you track one. `Status` is a growable VARCHAR by design (rule #20), so an unrecognised value renders as itself rather than vanishing.

- feat(editor,song): **musical key, tempo and time signature are real** (#1671 F3 / #298, remediation Batch 8) — #298 shipped the `tblSongKeys` table, both endpoints, and a hidden markup block on the song page, then shipped nothing that read or wrote any of it. The table has had **no writer for its entire life**, the markup was `display:none !important` with not one line of JS referencing its ids, and `transpose.js`'s key display reads `data-key`, which `song.php` only emits `if (!empty($song['key']))` — a field `SongData` **never sets**, so that branch has never once executed in production. The v2 editor's Metadata tab now has a **Musical key** panel (a closed key list, a bounded tempo, a time-signature picker, one explicit Save) and the public song page shows the resulting `Key: Bb · 96 BPM · 4/4` badge and hands the key to the transpose controls so their +/- buttons can finally name the key you have transposed into. The block's two orphan transpose buttons were **deleted rather than wired**: they were a second, divergent copy of controls `transpose.js` already renders, and wiring them would have put two transpose widgets on one page.

- fix(api): **`song_key_save` had three guaranteed 500s and nobody had ever run it** (#1671 F3, remediation Batch 8) — building the UI for a never-called endpoint found each of them within minutes, all the same species: the handler's caps did not match the column definitions, and mysqli runs under `MYSQLI_REPORT_STRICT` so a rejected write **throws** rather than returning false. It bound **NULL into `TimeSignature`, which is `NOT NULL`** — so saving a key without a time signature, the most ordinary thing a curator can do, was error 1048 and an uncaught 500; it capped `original_key` at 10 characters for a `VARCHAR(5)` column (error 1406); and it `(int)`-cast `tempo` into an `INT UNSIGNED` column, turning `"fast"` into a stored 0 and a negative number into error 1264. An unknown `song_id` hit the foreign key and threw 1452 where a 404 belongs. All four now answer with a status and a stable machine-readable `reason`. The decisions live in the pure `includes/song_key.php`, which `tests/php/test-song-key.php` **calls** — 83 assertions, every one mutation-proven to be able to fail. The gate also stopped hardcoding `['editor','admin','global_admin']` and now asks `userHasEntitlement('edit_songs', …)`: the list was byte-identical, so no role gains or loses access today, but since #1590 an operator can retune that entitlement at `/manage/entitlements` and the hardcoded copy meant the control silently did nothing here. 401 and 403 are now distinct, because the editor has to be able to say "your admin session is not linked to the app API" rather than "access denied".

- docs(api): **the OpenAPI page described a `song_key` API that does not exist** (#1671 F3) — the GET parameter was documented `songId` (the handler reads `id`), the POST body `{songId, key}` (it reads `{song_id, original_key, tempo, time_signature}`), the response a bare string (it is an object of three fields), and both were called "the caller's preferred key", implying a per-user setting rather than one shared curator-owned value. A consumer following that page could not have made a single successful call. It drifted precisely because nothing exercised it — documentation is not a mechanism (rule #35). Corrected against the handlers, including the full `reason` vocabulary.

- refactor(settings): **ONE user-preference store, and it can now be shared with a second product** (#1671 F5, remediation Batch 7) — there were TWO stores doing the same job. `action=user_settings` writes a JSON blob on `tblUsers.Settings`; it is live, synced across devices, and fed by a strict whitelist in `js/modules/settings.js`. `action=user_preferences` / `user_preferences_sync` wrote a parallel table, `tblUserPreferences` — the same concept, the same payload shape, un-namespaced, and with **no caller anywhere in the tree** (verified by file-walk across appWeb, appApple and appAndroid, because `rg` cannot see `appWeb/.sql/`). Two stores for one concept is a data-integrity trap rather than a dormant feature: whichever a future client picks, the other holds stale truth forever with nothing to say so. The duplicate pair is **deleted** — cases and OpenAPI paths — and `tblUserPreferences` drops via a manual, `confirm=1`-gated migration card that **refuses to run unless the live table is empty**, so a row written by some out-of-repo caller is a hard stop rather than silent data loss. What survives gains the write contract that makes a shared store possible: **POST may name a `namespace`** (`^[a-z][a-z0-9_]{1,31}(\.[a-z][a-z0-9_]{1,31})?$` — `ihymns.web`, `ilyricsdb`, `apple`) and the server replaces **only that subtree**, leaving siblings untouched; GET accepts the same parameter to read one back. The merge is at the namespace **root and deliberately not recursive** — a deep merge makes deleting a key impossible, because the client sends the subtree without it and the merge keeps the old value, silently, forever. Caps are **413 refusals, never truncations** (the rule #1649/#1662 established the hard way), and the total-blob backstop is measured against the **result** of the write rather than its input, so an account that is already over it can still shrink itself back under instead of being trapped above the ceiling. Omitting `namespace` is byte-for-byte today's whole-blob replace, so `settings.js` and the shipped native apps are untouched — with the corollary stated in code, docs and the spec: **a namespace-less POST still clears every namespace**, so isolation holds once every writer is namespaced. The decisions live in pure functions in `includes/user_settings.php` and are pinned by `tests/php/test-user-settings-namespace.php`, which **calls them and asserts the answers** rather than pattern-matching the source — the lesson of `test-transaction-fatal.php`. Ten mutations (deep merge, sibling wipe, `$` for `\z`, case-insensitive names, cap-on-input, truncate-not-reject, `[]` for `{}`, null-for-absent, encoder flags, skipped validation) were each proven to turn it red, and three correct-but-textually-different rewrites proven to leave it green.

- fix(editor): **the songbook-move hardening, hardened — five of its own claims were true only inside the function that made them** (#1679 follow-up, remediation Batch 4b review) — an adversarial pass over the previous entry found the mechanism right and its REACH wrong in five places, each the same shape: a property established in one file, defeated in another. **The transaction-fatal re-throw now holds end-to-end.** `songRelocate()` carefully re-throws MySQL 1213 (deadlock) so the caller cannot commit a transaction InnoDB has already rolled back — but ten best-effort `catch (\Throwable)` blocks sit between the move and the commit on the two save funnels, and every one of them ate it again, so the endpoint still answered `ok: true` naming a song id that no longer existed. The two codes now live in ONE predicate, `songRelocateIsTransactionFatal()`, which opens every one of those catches. (Accurately: MySQL's default `innodb_rollback_on_timeout=OFF` rolls back only the failing statement, so the false success strictly needs 1213; 1205 is treated as fatal because with that variable ON it behaves identically.) **The v2 songbook list is the real catalogue.** It was derived from the loaded song index, so a songbook with zero songs contributed no row and a curator could not move the first song into a book they had just created — a regression against v1, which has always read the real list. `load_index` now returns it, and the control re-fills when it arrives, so a deep-linked song is no longer stuck with one option. **The refusal reaches the person who triggered it.** The cascade pre-check's message names the migration card to run, and was routed through an admin-only diagnostic field on an endpoint that admits the `editor` role — so the role most likely to hit it saw "Server error." It now travels in its own ungated `error_hint`, recognised by exception TYPE rather than by matching the sentence. **"This is the ONE mint" was false**: four other paths issue SongIds and all of them probed `tblSongs` only, so an import could still re-issue an id a live redirect claims. The claim CHECK — not the mint — is now shared (`songRelocateIdTaken()`), adopted by the v2 numberless-id allocator, the lyrics-ingest create path and the canonical-id backfill migration; the bulk-importer family is documented as not yet covered rather than half-done. Plus three small ones: the save core now derives the songbook column from ONE snapshot instead of two, the redirect-table probe has a strict mode so a broken probe cannot silently switch the claim check off, and `song_detail`'s OpenAPI path gained the `include` parameter its shared handler has always read (with the 410 body's `gone` flag now documented on both paths). Guards extended and mutation-tested: nine mutants against the PHP suite, four against the JSDOM one — including a derived check that EVERY best-effort catch in the save core re-throws first, so a new one added later is covered without anybody remembering to list it.
- fix(editor): **hardening the songbook move — it could refuse a save it should have refused early, re-issue a redirected id, strand a junction row, swallow a security-relevant failure, move a song nobody asked to move, and fire on a keystroke pause** (#1679 follow-up, remediation Batch 4b) — two adversarial reviews of the move helper found the mechanism sound and six real gaps around it. **The re-key now checks its own assumptions first:** it depends on every foreign key to `tblSongs(SongId)` being `ON UPDATE CASCADE`, which a *fresh* install satisfies but four migration-created FKs (`fk_link_song`, `fk_alt_song`, `fk_media_song`, `fk_work_song_song`) do not — and the migration that adds them shows as "not pending" on any install that never renamed an abbreviation. On such an install the move used to explode mid-transaction and roll back the **whole** save, losing the curator's lyrics, components and credits behind a generic "Failed to save song"; it now refuses before the first write and names the migration to run, the way the two existing mass-rename paths already did. **The editor's id mint no longer re-issues an id a redirect still claims:** moving the highest-numbered song out of a book frees its slot, and re-minting it would have served an old bookmark 200 OK with a *different* song — worse than the 404 the redirect exists to remove. (That first landed as one mint's private probe, and this entry originally said "the id mint" as though there were only one — see the follow-up bullet above for the four other minting paths it did not cover.) **`tblSongbookEntries`' home row now follows the song** (its `SongId` cascades, but the book, number and `IsHome` flag do not — the row was left claiming the song's home is the book it just left, with the vacated number still occupied), including the case where the song is already a member of the destination book. **The content-restriction rewrite is fatal again:** it was the one security-relevant step and the one made best-effort, so a failure silently dropped a restriction — withheld content became readable and the move committed anyway. **A deadlock (1213) or lock-wait timeout (1205) during the cached-count refresh is re-thrown** instead of logged: both roll back the entire InnoDB transaction, after which the caller happily committed nothing and reported success for a song that no longer existed under that id. **A save that never mentions the songbook no longer moves the song:** the old test compared an already-defaulted value, so a partial save re-keyed the song into `Misc`, cleared its number and wrote a permanent redirect. And in the **v2 editor the songbook is now a proper list, not a text box** — as free text with a 500 ms debounce, every keystroke pause that happened to spell a real songbook performed the whole irreversible move; it is now a `<select>` of real songbooks, saved on change, behind a confirmation that says plainly that the song gets a new id, loses its number, and leaves its old link behind as a redirect. The v1 editor also stopped writing the stale number back after a rename, which had been quietly undoing the clear on the very next save. Two new guards: `tests/php/test-song-relocate-hardening.php` (the six server-side properties, ordering included) and `tests/test-v2-songbook-move-ui.js` (the move dialog driven for real in a headless DOM); both mutation-tested, and one of them caught its own first version passing on doc-block prose.
- feat(editor): **moving a song to another songbook now re-keys its ID and leaves a forwarding address** (#1679, remediation Batch 4) — `tblSongbooks.Abbreviation` **is** the SongId prefix (rule #27): `MP-1008` means "MP, number 1008", and the PWA router, the OG-image endpoint and several API validators all parse it that way. Changing a song's songbook wrote `SongbookAbbr` and nothing else, so the song's id claimed one book forever while its column claimed another — silently, with no error and nothing to grep for. A new shared `includes/song_relocate.php` does the move properly: it mints the next free `<Abbr>-NNNN` id in the destination book, re-keys the row in **one** `UPDATE` (all 25+ FKs to `tblSongs(SongId)` are `ON UPDATE CASCADE`, so writers, components, lyric lines, media, tags, favourites, history and revisions all follow atomically — including `tblSongRedirects.NewSongId`, so redirect chains that pointed at the old id repoint themselves), clears `Number` (the owner's stated default — v1 parity, and it dodges a collision with whatever already holds that slot in the target book), rewrites the one soft reference the cascade cannot reach (`tblContentRestrictions.EntityId`, which is polymorphic and un-FK'd — leaving it behind would silently **drop** a content restriction), and writes a `tblSongRedirects` row with the new `Reason='move'` so every existing permalink, bookmark and share still resolves. `PublicId` is deliberately untouched: it exists precisely to survive a move. **Four write funnels** were wired, not the three the plan named — the fourth (the v1 editor's revision restore, which wrote `SongbookAbbr=?` in its scalar UPDATE) was found only by grepping the tree, which is the argument for deriving these lists rather than typing them. Both restore paths (v1's hand-written UPDATE and v2's `ed2_applySongSnapshot()`) now **exclude** the songbook: a restore writes scalars only, so restoring an old abbreviation would recreate the exact mismatch this fixes, as a side effect of an action aimed at the lyrics. The whole-song save reuses the existing `assignedId`/`previousId` response contract (#1380), which is why the v1 editor needed no client change at all; v2's metadata tab hands the new id back to the shell, which re-opens the song and history-replaces the `?song=` URL. On the read side, `?action=song_detail` now **follows** the redirect layer before 404-ing (the HTML fragment has done so since #1343, the JSON path never did) — it serves the current record plus `redirectedFrom`, and answers **410 Gone** for a tombstone rather than a 404, so a native app or PWA offline cache holding a moved id can learn the new one instead of failing forever. New `tests/php/test-song-relocate-funnels.php` derives every per-song songbook write in the tree from the source (three SQL shapes, on comment-stripped tokens, with book-wide abbreviation renames excluded by construction rather than by an allowlist that could go stale) and fails the build if any of them skips the helper; it was mutation-tested against the real files eleven ways, including the exact trap three previous batches shipped — a call downgraded to a doc-comment mention, which it correctly still flags.
- chore(cleanup): **remediation Batch 3 — dead code, dead settings and doc-truth fixes** (orphan inventory §§2.3/2.5/2.8/2.9/3.1/6.2, work items X1–X7/F7/F8) — the "delete it or say why it stays" half of the 2026-07-30 remediation pass, executed under the permanent orphan guard landed in Batch 1. **Deleted**: the v1 editor's `get_translations`/`add_translation`/`remove_translation` trio (`manage/editor/api.php`) — fully implemented, zero JS call sites ever, superseded by the live public `song_translations` action; `includes/pdf_export.php` (Setlist PDF Export, #302) — never `require`d, setlist printing went the browser-print route instead; `places-api.php?action=get` — its own header claimed "used by edit forms that want to re-render a chip", no edit form ever did; and the pre-#398 `setlist_schedule`/`setlist_schedule_save` legacy pair — superseded by the live `setlist_schedule_set`/`_clear`/`_current`/`_upcoming` family, whose own yaml doc had drifted from what the code actually implemented (a second, independent doc-vs-code disagreement found during verification). Each deletion took its `api-docs.yaml` path(s) and its orphan-guard allowlist entry with it in the same commit — an allowlist entry for something no longer dispatched fails the guard as "stale," so guard and code cannot drift apart. **Doc-truth**: six undocumented-dormant public actions (`song_by_identifier`, `person_by_identifier`, `songs_list`, `my_organisations`, `songs_by_tag`, `song_revisions`) plus `custom_tags`'s GET twin gain explicit "Dormant" notes in the OpenAPI spec instead of being presented as ordinary live endpoints with silently zero callers; `manage/help.php`'s API-surface summary drops the three now-deleted verbs. **Schema**: `tblSessions`, `tblUserPurchases`, `tblUserPermissions` and `tblMigrations` — schema.sql-only, no migration ever created them, no app code ever read or wrote them, a guessed 2019-vintage shape in `tblUserPurchases`'s case — are removed from `schema.sql` per owner decision D2 (default: drop all four); the existing generic "Drop Legacy Tables" card on `/manage/setup-database` picks them up automatically since it diffs the live database against `schema.sql` at runtime, so no new migration card was needed — **the drop itself is deliberately left un-run on every environment**, same as every other manual/confirm-gated destructive card. `tblUserGroupMembers` is removed the same way, alongside fixing the `user_access` action's UNION arm that read it (#1670) — that arm threw under STRICT mysqli on any migrated install, 500ing the endpoint every time; the fix repoints group lookup at `tblUsers.GroupId`, the column membership actually lives on. **Settings**: the `captcha_provider`/`captcha_site_key`/`captcha_secret_key`/`ads_enabled`/`ads_provider`/`ads_publisher_id` seed descriptions are reworded from claiming working bot-protection/advertisement features to explicit "RESERVED — not wired yet" text (a new migration reaches existing installs, since `INSERT IGNORE` never does); the `app_status` emit of all six is untouched, since removing a field a native decoder might read defensively is its own class of regression. Two gaps found during verification, not blocking, are filed rather than silently absorbed: #1685 (the `motd`/captcha/ads dead-settings family, only the captcha/ads description half fixed here) and #1686 (the retired `setlist_schedule` GET also exposed an org-wide view the live family's four actions don't replicate — `tblSetlistSchedule.OrgId` is now write-orphaned; nothing calls either pair today so no live behaviour regresses).
- feat(setlists): **no cap, explicit deletion, and an optional expiry — a set list now goes away only when you say so** (#1661) — the 50-set-list cap was never intentional and never specified; it was simply the number somebody typed, and #1649 had to work around it because the server *inferred* deletion from a set list being missing from a sync payload. Those are different facts, and conflating them is what silently ate the tail of large collections. The cap is **removed entirely** — the only limit left is a generous 4 MiB whole-body ceiling that answers **413**, a loud, retryable rejection instead of an invisible, permanent truncation. Deletion becomes something a client **says**: a new `deleted: [id, …]` list writes a permanent tombstone (`tblUserSetlistTombstones`), and **the presence of that key — even as `[]` — is what marks a protocol-2 client**, for which absence-based deletion is retired outright. A tombstoned id is dead **forever**; re-creating mints a fresh client id. That rule is deliberately deterministic rather than a timestamp comparison against the incoming `updatedAt`, because a client clock is not evidence — a device running fast would otherwise resurrect everything it had ever deleted. Separately, each set list may now carry an **optional expiry** (`tblUserSetlists.ExpiresAt`, `NULL` = never expires, which is what every existing set list gets); the **server** converts an expired set list into a tombstone with `reason: "expired"` on the next read, so expiry and deletion propagate through one mechanism and two devices with different clocks can never disagree mid-service. **Native app sync behaviour is completely unchanged** — the Apple and Android clients never send `deleted`, so they stay on the legacy, #1649-guarded path, and their decoders ignore the added response fields. ⚠️ The migration is **web-run**: apply "Setlist tombstones + optional expiry" on `/manage/setup-database` for each environment after deploy; until then every new read is existence-gated and behaviour is byte-for-byte what it is today.
- fix(licensing): **organisation CCLI licences did not work at all — one resolver now answers "does this user hold a valid CCLI licence?"** (#1668) — the question had three answers that disagreed, and the organisation half was severed at the storage layer rather than merely ignored. Every org-licence UI that ships (`/manage/organisations`, `/manage/my-organisations`, the `org_admin_licence_add/_change/_remove` API trio) writes `tblOrganisationLicences`; `getUserEffectiveLicences()` read that table **zero** times, consulting instead `tblContentLicences` (which has no writer anywhere in the codebase, so it is permanently empty) and the legacy `tblOrganisations` columns. A church that entered its CCLI number the only way the site offers contributed nothing to its members' access — and nothing to the Service-Mode Phase-3 congregant unlock, which read the legacy columns alone. Meanwhile the personal half forked three ways: `licences.php` accepted any non-empty `CcliNumber`, while `content_gating.php` and `api.php`'s `tier_check` each ran their own copy of one SELECT that also demanded `CcliVerified` — so two gates could return opposite answers about the same user and the same song. New `userHasValidCcli()` is the single resolver; the two duplicate queries are deleted and both call sites delegate to it. **Validity is deliberately FORMAT-ONLY**: CCLI publishes no verification API and no check-digit algorithm, so nothing here can set `CcliVerified` truthfully at scale and requiring it would deny essentially every genuine licence holder. A number must be all digits and of normal length, judged by the existing `validateCcliNumber()` — the one place that rule lives. `CcliVerified` is kept, still written, and surfaced as an advisory `verified` field, but it gates nothing. **Nobody loses access**: the personal path is not tightened, org members who were wrongly excluded now pass, and `tier_check` accordingly returns org-derived allowances the native apps can see. All of it stays inert while `content_gating_enabled='0'`. The dead `ccli_validation_enabled` setting — whose seeded description called it the CCLI enforcement switch while nothing read it — is removed, with a migration so existing installs shed the false text that an `INSERT IGNORE` seed could never reach. New `tests/php/test-ccli-resolver.php` exercises the decision logic directly and then derives from the source tree that neither the licence query nor the CCLI format rule has a second copy; it was mutation-tested against the real files, which caught two of its own assertions passing because a doc-comment happened to contain the string they searched for.
- feat(editor): **`/manage/editor/` now serves the v2 Song Editor** (#1601 scope item 2) — `manage/editor/index.php` becomes a 302 redirect to the v2 shell, forwarding the query string verbatim, so all six places that link into the editor with instructions in the URL (the admin nav, the dashboard card, Revisions' "Open in editor", Missing Numbers' prefill, Duplicate Songs' link opener, and the public song page's Edit button) land on v2 without each needing its own change; the browser carries a URL fragment across the redirect on its own. 302 rather than 301 so the decision stays reversible server-side instead of being cached indefinitely by browsers that already visited once. Three escape hatches: `?legacy=1` per-visit (also what editor2.php's own "Legacy" button now links to), `tblAppSettings.editor_v2_default='0'` fleet-wide with no migration needed (an absent key means v2, and any DB error while checking it still resolves to v2 rather than throwing on the way in — flippable from a new "Song Editor" card on `/manage/configuration`, not only via a raw SQL statement), or reverting the commit. v1 is deliberately **not** retired (scope item 3) — nothing on this branch was runtime-verified (no MySQL, no browser exist in this environment) and retirement also needs cross-branch coordination, since beta and main share one database and are a month behind on unrelated history. Ships only once every parity gap the #1601 audits found was closed and the P0 write-blocker below was fixed.
- fix(editor): **v2 editor could not write anything — every save returned 403** (#1677) — api2.php rejects any POST lacking `X-Requested-With: XMLHttpRequest` (the same-origin CSRF gate, #1307); v2's own API client sent that header on reads only, never on either write helper. So every mutation — save, create, delete, component upsert/reorder, metadata/credits/tags/links/media writes, media upload, bulk verify/tag, revision restore, the per-line enrichment endpoints — returned "Cross-site POST blocked" the instant the v2 UI touched anything, while browsing and loading worked fine (all GETs). It survived unnoticed because v1 was still the default editor and v2's write paths were barely exercised. Fixed client-side rather than by loosening api2's gate. New `tests/php/test-editor-api2-contract.php` derives both the client's header usage and every action literal it sends from source and cross-checks them against api2.php's real cases, so a future typo'd action name or a dropped header fails the build instead of failing silently.
- feat(editor): **v2 gains a chords editor, an arrangement editor, and per-line translation/annotation panels** (#1627) — the last day-one parity blockers before the cutover could ship. v1 persisted the song's running order (`ArrangementJson`) through its whole-song save; v2's granular per-field save model had no endpoint that touched the column at all, so flipping the default would have frozen every existing arrangement as read-only fossil data. A new `arrangement_update` api2 action plus a shared `includes/arrangement.php` validator fix that — used by both the writer and the #1618 cutover-gate auditor, so the two rules can never silently diverge. The client editor ports v1's pool/strip UI onto move-left/move-right buttons instead of drag-and-drop, sidestepping another unpinned SortableJS CDN load (#1587/#1647) and fixing the same keyboard/touch gaps #1644 closed elsewhere. Chords and per-line language/translation/annotation editing land in the Structure tab, anchored on `tblLyricLines.Id` (rule #21) via a `lineIds` field the editor's read shape had silently been dropping — without it, per-line enrichment had endpoints and storage but no UI could ever reach them. Two opposite-shaped clearing traps in `component_upsert` (an empty chords textarea must send `[]`, not `null`; once per-line languages becomes an array it must never be re-sent as `null`) are both covered by new tests, and both new panels' failure-state UI was rewritten to branch on HTTP status codes rather than pattern-matching the server's error prose — closing a "two things that must silently stay in step" gap of the same shape as #1581.
- feat(editor): **v2 gains `?tab=` deep-links, the sidebar songbook filter + 3-way sort, `bulk_tag_detach`, and the per-format export lines-per-slide option** (#1628) — restoring v1 controls v2 had dropped. `manage/revisions.php`'s "Open in editor" link carries `?tab=history`, which v2 ignored entirely — reintroducing, on the very button #1623 had just fixed, the identical silent no-op that fix removed. Plain text search with a 300-row cap couldn't express "browse songbook X in number order," so the songbook filter and v1's three sort orders return, sharing v1's numeric-not-lexical comparators so the two editors never disagree on ordering. Bulk tagging was add-only in v2 (v1 offered add/remove in one action); `bulk_tag_detach` restores the ability to undo a bad bulk-tag without touching songs one at a time. The export "lines per slide" control was silently wired to a literal 0, so no format ever split a long verse across slides; it now reads live at export time and shares v1's localStorage key so an operator's projector preference survives the cutover. Also documents, rather than hides, one genuine behaviour difference: v2's revision Restore returns to the state a revision row recorded *after* the edit, while the legacy editor's Restore undid the edit and landed one revision further back — both the confirm dialog and the tab's intro copy now say so.
- feat(editor): **v2 honours every deep link other pages already emit** (#1680) — `?songbook=`/`#number=` (Missing Numbers' prefill) and the `?open=` alias. Found while preparing the route flip: `manage/missing-numbers.php` links "Open in editor" with a songbook + number in the URL fragment, which v2 had no handler for at all — the third instance of this exact bug shape, each caught only by hand after the previous one shipped (#1623 fixed `?open=`, #1628 fixed `?tab=`), because an API diff finds missing endpoints and a UI diff finds missing screens — neither enumerates the URLs other pages already point at the editor. An existing (songbook, number) pair now opens rather than silently minting a duplicate song on the same number. New `tests/test-editor-deep-links.js` derives every `/manage/editor/?…` link in the tree and every parameter it carries, then checks each against what the v2 shell actually reads — so a new deep link added anywhere in the app is covered automatically instead of needing its own follow-up fix.
- fix(editor): **de-orphan four v1 `api.php` consumers ahead of retirement** (#1629) — callers outside the editor UI that had nothing to do with the v1/v2 cutover but would have broken invisibly the moment v1 is removed. Content Restrictions' name-first picker (`user_search`/`org_search`, never actually an editor feature) and the admin Songbooks bundle-export button are repointed at their api2/public equivalents; the global bulk-import progress poller's stale-job fallback is repointed at api2's status endpoint; and `/manage/duplicate-songs` gains its own CSRF'd, `edit_songs`-gated `unlink` action so removing a `tblSongLinks` row no longer requires "unlink it in the editor first" — a dead end once v1 retires — clearing the way for #1608's editor-side suggestions panel to retire too.
- feat(editor): **`easyworship_export` gets its own api2 endpoint** (#1678) — the v2 editor's own Export menu called v1's `?action=easyworship_export`, which had no v2 equivalent; retiring v1 as planned would have silently broken a button that lives inside v2 itself. The RTF-building helpers are extracted verbatim into a shared `includes/easyworship_export.php` that both api.php and the new api2 case call, so v1 and v2 stay byte-identical while v1 still exists.
- fix(security): **one shared emitter for the Bootstrap CDN tags — four pages had drifted to three pinned versions, two with no SRI at all** (#1676) — the v2 editor's bespoke `<head>` (`editor2.php`, promoted to the default editor by this same branch), `import2.php`, and the public sign-in gate were all loading Bootstrap CSS/JS from a CDN with no `integrity` attribute, while the shared registry, `channel_gate.php`, and both editors disagreed on which version was even pinned. A new `includes/bootstrap_assets.php` centralises the `<link>`/`<script>` markup — deliberately the smallest reusable unit rather than a whole shared `<head>`, since the existing `head-libs.php` bundle is too coarse for a page that wants only these two tags — and `bootstrap_icons` joins `APP_CONFIG['libraries']`, with `download-vendor.sh` now fetching the icon font files as well as the CSS (a CSS-only vendored fallback would otherwise silently render every icon as an empty box). `tests/test-vendor-sri.js` was rewritten to derive the set of pages needing coverage from the tree instead of a hardcoded list, and found two more affected pages than the originating issue named.
- feat(import): **iHymns interchange JSON importer, writing straight to the database** (#1633) — additive/merge only, never truncating, following the same contract as the existing ZIP importer rather than reviving the retired `migrate-json.php`. Routes through the existing `_bulkImport_upsertSongbook()`/`_bulkImport_saveSong()` pair, so it inherits the "existing rows are sacred" contract and the normalised lyric-line write path (rule #25) for free. `.json` was already claimed by the VideoPsalm importer, so a content sniff (all three iHymns top-level keys present, VideoPsalm's capital-`Songs` key absent — case-sensitive, so a false positive is structurally impossible) redirects a genuine interchange file onto the new path; anything ambiguous stays on the old one. Enforces its own 8 MiB pre-decode ceiling rather than trusting the existing 25 MB cap, since `json_decode()` is not a streaming parser and a decoded JSON graph was measured at roughly 5.8× the file's bytes on this corpus — the same class of OOM risk as #929. Validation is all-or-nothing before any transaction opens, since a malformed interchange file means the file itself is broken, not that one hymn is unusual.
- fix(tools): **repair `npm run build:proto`** (#1634) — three faults. The script pointed at the now-retired legacy editor's proto directory, the same stale-path defect #1631 had already fixed in a sibling script but missed here; the documented `npm run build:proto` command was never actually registered in `package.json`; and the tool's protobuf JSON output was non-deterministic (protobufjs doesn't guarantee stable key order), so regenerating the bundle produced a ~3,300-line diff that was pure reordering noise around any real schema change — exactly the moment a genuine delta most needs to be reviewable. Output is now sorted recursively and verified byte-identical across two consecutive runs; the committed bundle is renormalised once, verified semantically unchanged first.
- feat(setlist): **finish collaboration — notifications, the shared-with-me list, and enforced permissions** (#1638) — collaboration had shipped write-only: an owner could invite someone by email, pick view/edit and see them appear in the modal, but the invitee got nothing. No notification was created, the invitee-facing endpoint had zero callers on any platform, both listing endpoints were owner-only, and `Permission` (view/edit) was never read anywhere — the picker was decorative. The legacy `setlist_collaborators`/`_collaborator_add`/`_collaborator_remove` trio (unfixably broken — it referenced columns that never existed on `tblSetlistCollaborators` and always threw under mysqli's strict mode) is removed from both `api.php` and `api-docs.yaml`. A new shared `includes/setlist_collab.php` resolver answers "may this viewer read/write somebody else's setlist," failing closed on an unrecognised `Permission` value or an ambiguous lookup; collaborative writes go through a single targeted-UPDATE endpoint that structurally cannot delete or create a setlist in the owner's namespace, and shared setlists are held client-side in memory only, never written to the localStorage array the per-user sync payload is built from — closing the same class of cross-account data-loss risk #1649 fixes elsewhere. `setlist.js` gains a "Shared with me" section and a `?shared=` deep link so the new notification's action URL actually lands somewhere.
- fix: **sync the PHP/JS entitlement maps; honour `song_requests_enabled`; fix the bare `?id=` deep-link on Credit People** (#1641) — the JS entitlement mirror had drifted 13 keys behind the PHP source of truth, latent today only because nothing in the JS client currently gates on any of the missing keys — the failure mode is a client-side control that would silently never render for someone actually granted it. `song_requests_enabled`/`max_song_requests_per_day` were seeded in `schema.sql` and honoured only by the older Apple-only `song_request` endpoint; the web's `song_request_submit` read neither and hardcoded a limit of 5, so disabling requests via the admin setting did nothing for the web — the same "documented control that doesn't control" shape as #1668. And the public person page's "Edit this person" button linked `/manage/credit-people?id=<Id>`, which the admin page read only inside an AJAX branch, so a bare `?id=` landed on the unfiltered, unscrolled full list with nothing selected or explained.
- fix(schema): **`ccli_validation_enabled`'s seeded description asserted a licensing control that doesn't exist** (#1668) — nothing in the codebase reads the setting; the real controls are `content_gating_enabled` plus `require_licence:ccli` restriction rows (rule #28). Unlike the rest of the same audit's other dormant, do-nothing settings, this one doesn't merely do nothing — it reads like the master CCLI-enforcement switch, so an operator who set it to `1` would reasonably believe copyright enforcement was live. The seed description now says plainly that the key is unwired and names the real controls; the row is left in place (an `INSERT IGNORE`, so existing installs keep the old text regardless) pending a product decision on whether a distinct toggle is wanted at all.
- fix(security): **admin gates now derive from the same entitlement map the nav advertises; the `/manage` session cookie honours `X-Forwarded-Proto`; `setlist_get` is rate-limited** (#1648) — eight admin pages (three more than the originating audit found, caught only because the fix derives the nav-to-gate pairing from `admin-links.php` instead of hardcoding it) were gated on hardcoded roles while the nav advertised admin-editable entitlements; narrowing an entitlement at `/manage/entitlements` therefore looked like it revoked access while every admin-role user could still reach the page by typing the URL. Behind a TLS-terminating proxy, `$_SERVER['HTTPS']` is unset even on a genuine HTTPS request, so the `/manage` session cookie could be minted without `secure` — it now checks `X-Forwarded-Proto` the way `includes/auth_cookie.php` already did. And the unauthenticated `setlist_get` share-link lookup had no rate limit against its 32-bit share ID; it's now throttled at 120/min like `search`.
- fix(a11y): **Present mode is a real modal dialog; Service Mode section changes are announced instead of silent; the scroll-to-song race is gone** (#1646 items 1 & 3, #1665) — Present mode's fullscreen overlay was a plain `<div>`, so Tab moved a keyboard user onto page content hidden behind it and nothing signalled a screen reader that anything had opened; it now carries `role="dialog"`/`aria-modal`, traps focus, and announces each slide change by its label. A congregant following a live service heard nothing when the leader changed sections — a purely visual scroll with no non-visual equivalent — and the scroll also ignored `prefers-reduced-motion`, since `scrollIntoView`'s `behavior` option isn't overridden by the reduced-motion CSS. The scroll-to-song trigger also fired on a fixed 600ms timer after navigation, so on a slow connection or a long song the elements it queried for hadn't rendered yet and the congregant silently stayed at the top of the page; it's now a bounded wait for the elements to exist, cancelled if a newer broadcast supersedes it.
- fix(a11y): **sortable admin table headers stay `columnheader`s** (#1646 item 2) — `th.setAttribute('role', 'button')` replaces a `<th>`'s implicit `columnheader` role rather than adding to it, breaking the header/data-cell association for a screen reader across all thirteen opted-in admin lists (#842/#844) and, since `aria-sort` is only valid on `columnheader`/`rowheader`, silently disabling the sort-state announcement it was meant to add. Fixed per the ARIA APG table pattern — a real `<button>` nested inside the `<th>`, leaving the header cell's own role alone.
- fix(security): **SRI + a vendored fallback for the SortableJS CDN load** (#1647) — the card-layout drag-reorder feature loaded SortableJS from jsDelivr with no `integrity` attribute despite being pinned to an exact version; the comment explaining why turned out to record that a placeholder hash had been committed once, had silently broken the whole reorder feature, and someone had removed the integrity check to get it working again rather than fixing the hash. The vendored same-origin fallback is what makes keeping SRI safe this time — a bad hash now degrades to a working local load instead of a dead feature, removing the incentive that caused the original regression.
- fix(a11y): **SPA navigation no longer reads the entire new page aloud** (#1645) — `<main>`, where the router injects every page fragment, was `aria-live="polite"`; each navigation therefore queued the whole new page — breadcrumb, toolbar, every lyric line — for announcement, competing with the router's own focus move onto that same element. Replaced with a one-line "you have landed on X" announcement (focus first, then announce, since a live region queues behind speech already in progress) via a new shared `js/utils/announce.js`, extracted from a private announcer `setlist.js` had already built for itself because the global live region had never actually worked for it.
- fix(a11y): **the setlist Arrangement editor is usable by keyboard and touch, and its chip names contain their visible text** (#1644) — every chip was a `<span role="button">`, which has no native activation behaviour, so Enter/Space did nothing despite the chip being focusable and announced as a button; reordering was wired only through HTML5 drag-and-drop, which doesn't fire from touch input on mobile — the device a worship leader arranging a song live would actually use. Chips are now real `<button>` elements with explicit move-left/move-right controls alongside drag-and-drop as an enhancement, with focus explicitly restored to the moved chip after each re-render. Separately, chip `aria-label`s used the unabbreviated section name ("Verse 1, position 2 of 5") over visible text reading "V1" — a WCAG 2.5.3 Label-in-Name failure that breaks voice control — now leads with the visible abbreviation.
- fix(sync): **stop capped per-user syncs deleting the tail of a user's collection; add a sync watermark; warn on truncation** (#1649) — the three per-user sync handlers (set lists, favourites, custom tags) capped an incoming payload with `array_slice()` and then, in replace mode, deleted every server row absent from that already-truncated list — so a user with 60 set lists who synced 50 had the other 10 permanently deleted on the very next auto-sync, with nothing logged and nothing shown. A new shared `includes/user_sync.php` makes a truncated payload skip deletion entirely (it degrades to a merge, since a clipped payload can't assert what doesn't exist) and adds an optional `since` watermark so an older device can no longer delete another device's newer, unseen rows. Client-side, the PWA now sends that watermark, absorbs every sync response instead of discarding it, fixes two amplifiers (an offline-drain overwrite that could wipe the local cache too, and a hardcoded `'replace'` on setlist-sharing that bypassed the reconcile gate every other caller respects), and toasts once per session when the cap actually bites.
- fix(a11y): **high-contrast and colour-vision-deficiency modes were dead across the entire `/manage` admin surface and both editors** (#1643) — the CSS keyed on the `data-ihymns-contrast`/`data-ihymns-cvd` attributes that `admin-theme-init.php` correctly stamps on `<html>` existed only in the file loaded by the public site's shell, so every admin page announced the intent (the attributes were set) and shipped none of the styling to honour it; a curator who picked High Contrast or a CVD mode simply got plain dark theme, with no error anywhere. A new admin-surface CSS block fixes the stylesheet's absence; a hardcoded six-songbook CVD badge selector list (which matched a class nothing in the codebase actually emits) is re-keyed onto the real markup with a generic fallback so a songbook outside the named six still gets a non-colour cue. The audit that found this derived the "which pages need the stylesheet" list from the tree rather than hardcoding it, and caught two more affected pages than the originating report named.
- feat(setlist): **playback mode — tap a song to start, a fixed floating nav bar, arrow-key navigation** (#1533) — extends the #94 setlist-navigation machinery (prev/next, position, disabled ends) rather than building a parallel component. Fixes the bug under the feature: `getNavigation()` looked the list up via `getById()` in `getAll()`, so a SHARED setlist (which has no local record) could never be navigated at all, however it was reached. Replaced with a playlist CONTEXT — `STORAGE_PLAYLIST_CONTEXT`, sessionStorage, deliberately not localStorage, since "I am working through this list" belongs to this tab's session and localStorage would resurrect last Sunday's service the next time someone opened a song and would leak the context between tabs — that carries its own song order, so a shared list and an own list navigate identically; the old `activeSetListId` path survives as the fallback since `applyCustomArrangement()` still reads it. Arming is now on TAP from both the own-list detail and the shared-list view, not only the "Use" button most people never pressed. The bar is now fixed-position on `<body>` rather than an in-flow alert after the breadcrumb (the old one scrolled away exactly when a musician mid-service needs Next) — which meant `renderSongNavigation()` had to tear down BEFORE any early return, since a fixed element doesn't vanish when the SPA swaps page content and the router calls it on every navigation, not just song pages. Also: next-song title, "3 of 12", an explicit exit, arrow-key navigation (bound once, refusing to steal keys from text entry, comboboxes or modifier combos), and an aria-live announcement (the SPA changes the song under you with no document load, so a screen reader otherwise gets silence); `body.has-playlist-bar` reserves padding so the bar can't cover the final lyric line. New `tests/test-setlist-playback.js`, 22 assertions.
- fix(admin): **Revisions Audit "Open in editor" now actually opens the song** (#1623) — `manage/revisions.php` linked to `/manage/editor/?open=<id>`, but `editor.js` reads `?song=` — the established convention (#407) every other `/manage/editor/` link uses — so the editor loaded with nothing selected and no error. Fixed the link and kept `open` as an alias so a bookmarked link starts working instead of failing the same invisible way. Also wires `?tab=history` to the existing `openHistoryModal()`, which nothing previously called from a URL, so the History button's tooltip promise is now true.
- fix(ci): **pass the commit message to `version-bump.yml` as an env var, not shell source** (#1622) — a commit message is attacker-controllable text; interpolating it with `${{ }}` directly into a `run:` body pastes it into the script BEFORE the shell parses anything, so its contents become script source. Found the benign way: the #1611 stopgap commit's message quoted `"DRY RUN — nothing has been changed"`, whose embedded double quote closed the assignment early and the shell took the next word as a command, failing the Version Bump job on beta (exit 127). The same mechanism with a backtick or `$(…)` executes arbitrary code on a runner holding `contents: write`. Fixed with the GitHub-documented pattern — the value arrives as an environment variable the shell never re-parses.
- refactor(web): **migrate every call site to the shared client and DELETE the `window.fetch` monkey-patch** (#1031, second and final commit) — `search.js`, `song-of-the-day.js`, `settings.js` and `router.js`'s four page/data fetches now call `apiFetch`; the global `window.fetch` override `songbook-language-filter.js` installed is gone. Two files deliberately NOT migrated, verified rather than assumed: `place-search.js` is a classic `<script src>` on seven admin pages with zero import/export statements (a dynamic `import()` broke its keyboard test under jsdom), and it turns out `places-api.php` never reads `X-Preferred-Languages` — the premise for migrating it was simply wrong; `service-broadcast.js` has no direct fetch calls at all, its network access goes through an injected `apiCall()` closure. `search.js`'s two hand-rolled `EVT_FETCH_FAILED`/`EVT_FETCH_SUCCEEDED` dispatches are removed — it used to be the only module signalling connectivity by hand, but `apiFetch` now dispatches on every request, so keeping them would double-fire the offline indicator's listeners. All 19 JS suites pass; `requestUrlOf()` and its #1593 regression test move to the new module intact.
- feat(web): **add the shared API client `js/utils/api-client.js`** (#1031, first of two commits) — foundation for retiring the `window.fetch` monkey-patch; this commit is INERT, nothing imports it yet. `apiFetch`/`apiFetchJson` centralise per-request headers (`X-Preferred-Languages`, `X-Requested-With`, injected auth) that `songbook-language-filter.js` previously solved by replacing `window.fetch` globally — a pattern that had already hit two failure modes: a bug in the header logic became a failed REQUEST wherever the caller happened to catch it (#1593 — reading `input.url` on a `URL` object returns `undefined`, breaking ten unrelated call sites), and the patch only applied if `bootSongbookLanguageFilter()` had installed it, which the router imports only on home/songbooks/settings — so on a cold load of `/search` an anonymous user's saved language filter was silently ignored (not previously reported; found while scoping this issue). `apiFetchJson()` also detects the #1566 SPA-catch-all-returns-200-HTML trap and raises a clear error instead of a `SyntaxError` from `.json()`. Auth headers arrive through an injected `setAuthHeaderProvider()` rather than an import, avoiding a `user-auth.js` ↔ `api-client.js` load-order cycle. `STORAGE_LANGUAGE_FILTER` joins `constants.js`, keeping its pre-convention key spelling on purpose since it is already in real users' browsers.
- fix(security): **close eight pre-gating security gaps before the flag can be flipped** (#1388) — every change here is a verified no-op while `content_gating_enabled='0'` on all three docroots; they matter because they stand between the flag and a leak on the day it flips. The theme: payload gating is not asset gating. New `contentGatingMediaAllowed($kind, $userId, $presenceToken)` in `includes/content_gating.php` gates media BYTES, mirroring `contentGatingApply()`'s kind→cap mapping so the two can never diverge — `/song-media/<id>` (`song-media.php`) now applies it as a second gate alongside the existing entity check (403 on denial, previously no tier cap at all), and the `bulk_audio` offline manifest resolves `play_audio` once per requester and empties the manifest when denied. `songbook_export` (`api.php`) previously carried an entity gate only — the widest lyric leak in the API, since one request returned every surviving song's full `components` regardless of tier — and now maps each song through `contentGatingApply()` exactly like `song_detail`. `song-media.php` also now passes the Service-Mode presence token (the 5th parameter has existed since #1335 but was never wired), so a present congregant is no longer denied media that `song.php` deliberately grants them. Service-Mode's `serviceMode_presenceCcliNumber()` now requires a heartbeat inside the freshness window, not just `IsActive` — an operator's shut-down-without-clean-end laptop previously left a standing CCLI unlock live for everyone who had ever joined. First-admin registration TOCTOU closed on both paths (`auth_register` and the magic-link path) with a transaction + `SELECT … FOR UPDATE`, so two registrations racing on a virgin install can no longer both become `global_admin`. Logout now clears per-user service-worker caches (a new `CLEAR_USER_CACHES` message) so the next user of a shared device isn't served the previous user's fragments — deliberately spares the offline-download/media caches, which are deliberate user intent. `validateCsrfRequest()` no longer accepts `X-Requested-With` alone when both `Origin` and `Referer` are absent (per the Fetch spec, browsers send `Origin` on every non-GET, so absent-both means "not a browser POST"); the rejection is logged. `test-gating-noop` extended with 15 assertions pinning the new media gate's off-behaviour per kind; the full existing CI suite passes. `/manage/gating-noop-verify` against the live DB remains owner-gated.
- cleanup(web): **delete dead `js/utils/transpose.js`; correct the lyrics-cutover gate count** (#1612, #1615, #1618) — the app's real transposer is the unrelated `js/modules/transpose.js`; the dead file had zero importers anywhere except its own header example, a doc comment in `format-export.js`, and — the dangerous one — the service-worker precache list, removed in the same commit (this SPA's catch-all `.htaccess` answers an unmatched path with 200 + the HTML shell, so `cache.addAll()` over a deleted path would have silently cached an HTML page under a `.js` URL, the same failure class as #1566). Separately, `verify-lyrics-cutover.php`'s header and the drop migration both claimed "10"/"G1–G13" gates; enumerating the actual `gate()` registrations found NINE implemented (G1, G2, G3, G5–G10). Each of the four missing IDs was traced rather than assumed folded in: G11 is correctly split across this verifier and the drop migration's Stage 0(b); G13 is subsumed by G2 (which already compares `ChordsJson`); the ArrangementJson-ordinal half of G4' and the G12 Id-stability smoke are implemented nowhere — a substantive gap in the drop's precondition, tracked separately as #1618 rather than papered over here.
- fix(deploy): **What's New no longer truncates mid-sentence** (#1589) — `deploy.yml` built `public_html/data/whats-new.md` from the first THREE `## ` sections of `CHANGELOG.md` under a 48 KB byte cap, but `## [unreleased] — alpha` had grown to 70,627 bytes on its own, so `head -c` cut *inside section one*: testers saw a single giant blob, truncated mid-word, that never reached a second release. A section-count budget cannot bound output size, because a section has no bounded size. The extractor is now ENTRY-based — at most 10 top-level bullets per release, 5 release headings (counted on headings actually EMITTED, so an empty `## [unreleased] — alpha` never burns a slot), 50 entries overall — with the byte budget checked *before* an entry is written, so the output always ends on an entry boundary instead of mid-sentence. Headings print lazily, only when one of their entries is emitted, so a release with nothing under it leaves no orphan heading. `head -c` + `iconv -f UTF-8 -t UTF-8 -c` are kept as backstops (`markdown_lite.php`'s `ENT_SUBSTITUTE` depends on never being handed a partial UTF-8 sequence), and `LC_ALL=C` pins `length()` to bytes rather than characters. The real changelog now yields 17,278 bytes across 5 releases instead of 49,152 bytes across 1.
- feat(ci): **version bumps now close the changelog's unreleased section** (#1589) — `version-bump.yml` renames `## [unreleased] — alpha` to `## [<newversion>] — <date>` and reopens a fresh empty `## [unreleased] — alpha` above it, staged in the **same commit** as the version bump so #1596's post-bump deploy dispatch actually ships it. This bounds section size permanently and makes the What's New page self-healing rather than degrading with every entry. Four skip-not-fail guards make it idempotent: `CHANGELOG.md` must exist; exactly one line must equal `## [unreleased] — alpha` verbatim (`grep -xF`, case-sensitive — the file also holds historical `## [Unreleased]` headings a loose match would hit); that section must hold at least one top-level `- ` entry (a re-run finds it empty and no-ops, so no empty version heading is ever emitted); and `## [<newversion>]` must not already exist. The rewrite lands in a temp file whose post-conditions (non-empty, one unreleased heading, one new-version heading, exactly one more `## ` than before) are re-verified before it replaces the original. Alpha-only, because `changelog.yml` rewrites the same file on beta; the retry loop now aborts a conflicted rebase instead of burning its remaining attempts against a half-rebased worktree. New guard `tests/test-whats-new-extraction.js` lifts the awk program **out of `deploy.yml`** and runs it against the real `CHANGELOG.md` (13 assertions, no copied logic), and is proven to fail against the pre-fix workflow.
- perf(api): **stop `bulk_songs` re-hydrating a whole songbook once per song** (#1598) — a comment claimed the bulk-download loop avoided per-song hydration; it didn't, because `includes/pages/song.php` unconditionally re-fetched and clobbered the injected song, then called `getSongs($songbook)` — a full book hydration — purely to compute prev/next, once per song in the loop. For a 3,517-song book that's roughly 3,517 whole-book hydrations in one request, which is why downloading a large songbook for offline use timed out or exhausted memory while small books worked fine. Fixed additively (an injected song/prev/next now survive) with the single-song path asserted unchanged. Prerequisite for #1597's offline-download fixes below to be worth anything.
- fix(offline): **seven verified defects in bulk offline downloads, several destroying data while displaying success** (#1597) — bulk-downloaded songs and recently-viewed songs shared one 2,000-entry cache budget, so opening a single song after downloading a 3,517-song book trimmed roughly 1,500 downloaded entries as if they were merely "recent"; the service-worker's cache keep-list never protected the audio cache, so every deploy silently deleted every downloaded audio file; offline navigation fallback existed only for song pages, so a perfect download of anything else 503'd; eviction and size-reporting used the wrong URL shape and silently missed files; a song's progress bar could hang forever from a count mismatch between per-song and per-book totals; `navigator.storage.persist()` was never called, leaving Safari free to evict a whole download after seven days; and the Settings "include audio offline" toggle dispatched an event nothing had ever wired a listener to. Downloaded songs and recency now have separate budgets and the media cache is protected across deploys; offline navigation falls back for every page type.
- ci: **serialise deploys per branch, then bump the version on alpha pushes too** (#1595, #1596) — `deploy.yml` had no concurrency group, so two pushes to the same branch within the deploy window could run two simultaneous `lftp --reverse --delete` mirrors against one docroot and interleave destructively (one run's delete pass, working from a stale listing, removing a file the other had just uploaded) — both report success. Deploys now queue rather than cancel (`cancel-in-progress: false`, deliberately the opposite of the Apple workflow's setting, since cancelling a half-finished `--delete` mirror is unsafe). Separately, `version-bump.yml` ran on `beta` only, so `alpha` — where nearly all work lands — could go long stretches without a version bump; one prior batch shipped 98 commits under one unchanged version string and needed a manual bump. Alpha is now included, with the bump commit's `[skip ci]` (correctly kept, to avoid re-running the whole CI matrix for a one-line string change) compensated for by an explicit `deploy.yml` dispatch, safe only because of the concurrency fix above.
- feat(a11y): **consolidate all nine site typeaheads onto one shared keyboard + ARIA combobox helper** (#1594) — a reported "location picker is mouse-only" bug turned out to be the healthiest of nine divergent typeahead implementations; the more likely cause was Chrome's address-autofill overlay intercepting keystrokes on an address-shaped field (mitigated with an autocomplete token Chrome doesn't recognise as an address hint). The audit found three genuine defects along the way — Tab silently discarded the highlighted option instead of committing it, `aria-activedescendant` was never set so the highlight was invisible to a screen reader, and Escape inside the Credit People drawer closed the whole Bootstrap offcanvas instead of just the suggestion panel — and generalised the fix into a shared `js/modules/combobox-a11y.js` (key semantics + ARIA bookkeeping only, so each of the nine call sites keeps its own fetch/render/commit logic), now used by location search, tag entry, credit-person/place pickers, service broadcast, work-member search, comparison and general search, with deliberate asymmetries called out in code (service broadcast gets no Tab-commit, since broadcasting a song on a stray Tab is worse than the convenience it offers).
- fix(lang-filter): **a URL-object bug in the global fetch header patch silently killed ten unrelated call sites, not just Song of the Day** (#1593) — `songbook-language-filter.js`'s `window.fetch` monkey-patch resolved a request's URL with `input.url`, correct for a string or `Request` but silently `undefined` for a `URL` object (whose property is `href`); the very next line called `.startsWith()` on that `undefined` and threw, escaping the module's own `try`/`catch` one line later. Because the patch wraps every fetch on the site, this wasn't a missing header — it was a failed request wherever the caller's own catch block happened to swallow it, and Song of the Day's caught error cleared its own markup, making the card silently vanish with no error visible anywhere. The fix (`requestUrlOf()`, handling string/URL/Request explicitly, wrapping the whole patched body so a future header bug degrades to an unheadered request rather than a failed one) also incidentally fixes the same latent crash in search, related-songs, shuffle, the numeric keypad and compare — none of which had been reported yet. Song of the Day now also tells a genuine "no song matches your filter" apart from a caught error, instead of rendering the two identically as nothing.
- test(importers): **repair and wire the OpenSong, VideoPsalm and FreeShow importer suites into CI** (#1538, #1575) — all three had been failing for a purely mechanical reason (each scraped its own source text for a code block that had since moved) but were never referenced by `test.yml`, so nothing ran them and the ongoing red went unnoticed — a suite nobody runs is a worse gap than a suite that's absent, because it looks like coverage exists. Adds a new 62-assertion FreeShow parser suite against real archived `.show` fixtures and wires all three into CI.
- fix(csp): **extract Request-a-Song's inline `<script>` — the SPA fragment inline-script allowlist is now empty** (#1572) — the last surviving case of #1565's failure shape: an executable inline script inside a shared-cache SPA fragment, silently refused by the enforcing nonce CSP since the fragment response can never carry the document's per-request nonce. The form's entire client-side path, including the offline submission queue (#337), had therefore never run — every submission silently fell through to the no-JS fallback. Extracted to a real ES module wired from the router, reading its prefill inputs from `data-*` attributes the fragment now emits. `tests/php/test-fragment-inline-scripts.php`'s allowlist is now genuinely empty, making the guard absolute against any future violation.
- feat(service-mode): **congregant join QR code on the projection screen** (#1339) — typing a rotating join code off a projected screen was the friction; the QR removes it, opening the app on the correct same-origin host (respecting the per-environment `Channel` wall, rule #26) with the code still shown on-screen and readable, since the QR is `aria-hidden` and never the only way to join. Uses a vendored, exactly-pinned QR encoder (SRI-hashed, falling back to a local copy per the #1587 pattern) rather than a hand-rolled one, verified by an independently-written from-spec QR decoder run against the library's real output in CI — proof the payload actually decodes correctly, not just that it produced something. Scanning the projected code with a real phone at projector distance remains unverified pending owner testing.
- feat(song): **render per-line translations on the public song page, behind a "Show translation" toggle** (#1089 / #1100 P1) — corrects a standing belief that per-line translation had no write path; it did (the v2 editor's enrichment panel has written them for a while) — what was missing was any reader. Nothing in `song.php` or the JS modules displayed an approved translation once a curator entered one, so the feature was invisible end-to-end on the side that mattered. The toggle and its rows render only when a song actually has approved translations, wired as a real ES module from the router (never a fragment inline script, since `page=song` is shared-cache and can never carry a nonce — rule #30) and kept deliberately distinct from the unrelated whole-song-translation section that already exists lower on the same page. The full bilingual side-by-side reader remains open as #1100.
- feat(export): **ChordPro export now emits inline `[chord]` markers — the format's entire point** (#1080) — ChordPro export had been lyrics-only since it shipped, which is the chord-chart format with its purpose removed. Chords are aligned to lyrics by word index (there are no stored character offsets in this schema; the app's chord model is one chord line per lyric line, not a positioned overlay), verified code-point-safe against both a diacritic-heavy Portuguese fixture and a genuine astral-plane character, which matters given roughly two-thirds of the catalogue's songbooks are non-English. Also fixed the same gap in the v2 editor's own ChordPro export, which mapped components without their chords and would have kept producing lyrics-only files even after the public page was fixed.
- perf(songbook): **render the songbook listing page from the scoped slim index instead of hydrating every song in the book** (#1037, two commits) — `includes/pages/songbook.php` called `getSongs($bookId)` — full lyric-component hydration for every song — purely to list titles; for Mission Praise's ~3,500 songs that's one of the two most expensive public pages on shared hosting, on every uncached view. First moved to the existing slim index (with a captured-and-reproduced baseline proving every UI element survives — the unofficial-songbook badge, display-abbreviation handling, language badges, offline-download hooks — since a silently dropped badge is exactly this project's most common regression shape); then, since the slim index took no filter argument, the page was still pulling all ~14,500 catalogue rows and filtering to one book in PHP — `getSongsSlimIndex()` gained an optional songbook-scoped `WHERE`, purely additive for the index's other callers.
- fix(auth): **rate-limit password reset, harden per-account login lockout, add a real `/health` liveness probe** (#1028, #1027, #1022) — `auth_forgot_password` generated reset tokens with no rate limiting at all, so a known account could be flooded on every request; the limiter is keyed carefully to avoid becoming the account-enumeration oracle the endpoint's identical-200-either-way response was designed to prevent (#898). Login lockout is per-IP only (`tblLoginAttempts` has no `UserId` column), hardened as far as possible without a schema change — the deeper fix needs an owner-gated migration and is tracked rather than guessed at here. The pre-existing `action=health` is a songbook-completeness report, not a liveness check; a new, genuinely minimal `/health` returns 200 when the database answers and 503 otherwise, leaking no version, hostname, schema or error detail — something an uptime monitor can watch to catch the next outage before a tester does.
- fix(sql): **`migrate-json.php` now requires `confirm=1`** — the legacy pre-#1010 JSON bootstrap importer TRUNCATEs `tblSongComponents`/`tblSongComposers`/`tblSongWriters`/`tblSongs`/`tblSongbooks` and re-imports the (now months-stale) repo `data/songs.json`, and was reachable from `/manage/setup-database` via a single unconfirmed GET (`?action=migrate`) — its only guard was a client-side `confirm()` dialog, which does nothing against a bookmarked link, a replayed URL, or a curl/automation request. Now gated server-side exactly like its sibling destructive scripts (`restore.php`, `drop-legacy-tables.php`): `--confirm` (CLI) / `?confirm=1` (web) below an unconditional safe preview, plus the setup-database card's existing type-to-confirm speed-bump (#1298). commit `82c4ee15` (cherry-picked to `beta` as `cd678484` and `main` as `4df899ce`).
- fix(service-mode): **ad-hoc sessions no longer start already-expired** (#1576). `service_session_start`'s no-schedule-selected path hardcodes a placeholder 10:00/90-minute window; `serviceMode_occurrenceEndUtc()` capped the computed end at a hard now+4h ceiling but had no floor, so an evening ad-hoc start produced an `ExpiresAt` already in the past — every presence row was inserted expired and congregants saw "The service has ended." seconds after joining, with no visible failure on the operator's projection page. Adds an `$isAdHoc` floor (still subject to the existing hard ceiling), scoped to the ad-hoc path only — a genuinely-past *scheduled* occurrence still reports honestly expired. Also fixes the projection page's date field defaulting to the UTC calendar date instead of the browser's local date. +79 assertions in `tests/php/test-service-mode-occurrence.php`. commit `c911abcc`.
- ci(apple): **run `swift test` serially** (#1558) — the Apple `build` job's dominant CI failure (2 of 3 runs on PR #1585) was cross-suite Keychain contention: Swift Testing runs different `@Suite` types concurrently by default, and several suites drive the real macOS Keychain through `LANRemoteIdentityFactory`; on an unsigned, entitlement-less `swift test` binary with no unlocked login keychain, concurrent `SecItemAdd`/`SecItemCopyMatching` load makes the daemon intermittently return `errSecItemNotFound`. Each suite's own `.serialized` trait only prevented overlap within itself, not against other suites. Workflow-only change (no test-code edit); the proper fix — a Keychain round-trip probe that lets headless CI cleanly skip these suites — is tracked in #1558/#1559 pending a machine that can run `swift test`. commit `79f17670`.
- fix(deploy): **exclude `data/audio` and `data/music` from the docroot mirror** (#1584, commit `8ef7b587`) — the deploy mirrors `public_html/` in `--reverse --delete` mode with no exclusion for `data/`, and the song audio (`/data/audio/<SongId>.mp3`) + sheet music (`/data/music/`) exist on the SERVER only (git tracks zero files under `appWeb/public_html/data/`), so every deploy was telling lftp the media library shouldn't exist. Got sharper once #1583's What's New generator started writing `public_html/data/whats-new.md` before the mirror ran — `data/` gained a local counterpart, so the delete became surgical instead of wiping the whole directory. Scoped to the two media subdirectories, not a blanket `^data/` exclude (which would also stop What's New deploying). **Not yet verified** whether the media survives on any of the three live servers — needs host access, tracked in #1584.

- feat(web): **What's New page + clearer environment signalling** (#1583). Alpha testers had no way to see what changed between deploys, and therefore no way to know what to test — `CHANGELOG.md` lives at the repo root and is never deployed (`deploy.yml` mirrors only `public_html/`). A new deploy step extracts the three most recent version sections into `public_html/data/whats-new.md` (hard-capped, UTF-8-safe), and a new `/whats-new` SPA fragment renders it through a purpose-built `includes/markdown_lite.php`. That renderer is **escape-first** — it `htmlspecialchars()` the entire input *before* transforming a strict subset (headings, bold, code, scheme-allow-listed links, lists) — which an adversarial pass then failed to defeat across 27 vectors including `javascript:`/`data:`/protocol-relative URLs, attribute-break attempts and HTML nested inside every markdown construct. Alongside it, two environment affordances: the existing env badge (#812) is now **colour-distinct** (Alpha amber / Beta blue — they were identical, so two open tabs were indistinguishable), and both the badge's dropdown and the footer version now link through to What's New. Environment clarity is not cosmetic here: Live Follow sessions are walled per environment, and a cross-environment join fails with only a generic "wrong code" toast (#1339). One bug was caught in review and fixed before merge — passing `ENT_QUOTES` explicitly to `htmlspecialchars()` **drops** the `ENT_SUBSTITUTE` that PHP 8.1+ supplies by default, so a byte-capped excerpt cut mid-UTF-8-character rendered as a completely **blank card**; measured at 69 of 10,000 sampled cut points, and guaranteed on the first deploy since the real CHANGELOG (72,694 bytes) exceeds the cap.
- feat(web): **global JavaScript error surfacing** (#1582). The PWA had **zero** `window.onerror` and **zero** `unhandledrejection` handlers — every uncaught error died console-only. That is the precondition that let the public Export feature stay broken for seven weeks (#1565), and it recurred twice more in the same session (#1568, #1576). A new `js/modules/error-monitor.js`, booted before anything else in `app.js`, catches uncaught errors and rejections, shows one generic toast, and beacons a deduplicated one-liner to a new `client_error_report` endpoint. Defences that matter: a **three-layer throttle** (per-fingerprint once per 10 min persisted in `sessionStorage` so a crash-reload loop cannot re-beacon on every reload; a 10-per-page cap; a token bucket) verified to hold under 50-error bursts; a **re-entrancy guard** so an error thrown while showing the toast can never re-enter the handler; and a **privacy scrub** stripping bearer tokens, 64-hex strings and query secrets from both the client and the server side, with stack frames reduced to `pathname:line`. **No new table and no migration** — it reuses `logActivity()` and `tblActivityLog`, which already carry environment, route, user-agent and a JSON details column; review it at `/manage/activity-log` filtered to `Action=client.jserror`. Deliberately NOT routed through `analytics.js`, which is consent-gated: operational error telemetry that vanishes for consent-declined users would recreate the exact blindness this exists to remove.
- fix(web): **Settings language filter silently never refreshed Song of the Day** (#1581). DOM event names are case-sensitive, and three modules disagreed: `settings-language-filter.js` dispatched `ihymns:language-filter-changed` while the only listener, `song-of-the-day.js`, wanted `iHymns:…` — a spelling only `songbook-language-filter.js` emitted. So changing the filter from a songbook page worked and changing it from Settings did nothing, with no error. Two further dead wires were found in the same census: `offline-ui.js` listens for an `ihymns:offline-settings-changed` that nothing dispatches (which is also why the "include audio offline" preference is inert for tile downloads), and the editor dispatched an `iHymns:languages-loaded` nobody listened for (removed). Rather than patch the casing, every event name now lives once in `js/constants.js` — the module that already centralises storage keys "to prevent key-name drift (#139)" — and a new `tests/test-event-names.js` **bans raw `ihymns:*` literals outside that file** and asserts every name has both a dispatcher and a listener, so this class cannot recur.
- docs(live): **Live Follow and Service Mode user documentation** (#1577) — two adjacent in-app help topics (`help.php`'s "Following a Live Service (Service Mode)" / "Go Live — Lead Your Own Live Session (Live Follow)"), a new top-level `help/live-follow.md` guide, `wiki/Live-Follow-&-Service-Mode.md`, and a Live Sessions section in Apple's native `HelpView`. The two features share a lookalike code and join dialog but are started completely differently (any signed-in user from a song, vs. a church admin from the admin console) — every doc conflated them, which is why Live Follow looked permanently broken when it was actually being driven with Service Mode's prerequisites. All four now lead with an explicit "they are different features" comparison.
- fix(web): **the public Export menus now actually work — they never had** (#1565, #1566, #1567, #1568, #1570; tests #1569). A user reported that Export ▾ → ProPresenter 7+ did nothing on both a single song and a whole songbook. It turned out **no format had ever worked on any public page** since the feature shipped: the only code binding those menu items was a nonce-less inline `<script>` inside the song/songbook fragment, and the document sends an **enforcing nonce CSP** (#117) that refuses it. Bootstrap still opened the dropdown, so the menu looked alive while nothing was bound — hence the silent no-op with not even an error toast (the "Preparing export…" toast is the first statement *inside* the never-attached listener). Born broken: the CSP shipped 2026-04-06 (`a21ea99f`), the export UI 2026-06-07 (`5dedf5d4`) — nothing regressed, the two simply never met. Because the fragments are **shared-cache** responses (`$_cacheablePages`) they can never carry a per-request nonce (rule #6), so the fix is the pattern this repo had already established for `home.php`: wiring moved into real ES modules that the **SPA router** imports after the fragment loads, with IDs sourced DOM-first from `.page-song[data-song-id]` / the new `.page-songbook[data-songbook-abbr]` (the record id, correct even when the route used an alias, #1343-B) and the route param as fallback. The same cause had silently killed the **Present** button (#297) — extracted to `js/modules/present-mode.js` in the same pass, fixing a leaked document `keydown` handler on close while there (the old code listened for `'remove'`, which is not a DOM event, so the cleanup never ran). Behind the CSP break sat a **second, fully masked bug** (#1566): `propresenter-export.js`'s proto-bundle URL was document-relative, so from `/song/MP-1008` it fetched `/song/protos/proto-bundle.json` — which the SPA catch-all answers **HTTP 200 with HTML**, passing the `response.ok` guard and throwing inside `.json()`. Now root-absolute; the `bundleUrl` override is preserved. Also fixed: editor v2 loaded `propresenter-export.js` without the protobuf runtime so PP7 export always failed there (#1567); the single-song PP7 export was un-`await`ed so post-init failures became unhandled rejections with no toast; and the two hand-maintained format menus (which had drifted — songbooks silently lacked **ChordPro**, while a dead `FORMATS` constant in `export-ui.js` lacked ProPresenter 7+) are now one shared `includes/partials/export-menu.php` (#1570). **Why nobody caught it:** the ProPresenter suite passed 54/54 while injecting the bundle straight into `init()`, so the runtime fetch path had never once executed; `export-ui.js` had zero coverage; and CI ran no node tests at all. #1569 closes all of that — a new `tests/php/test-fragment-inline-scripts.php` CI guard bans executable nonce-less inline `<script>` in any SPA fragment (comment-stripping so `home.php`/`person.php` mentions don't false-positive; a count-exact self-cleaning allowlist for the one remaining case, #1572), plus a bundle-URL regression test that exercises the real fetch, a first-ever `export-ui.js` smoke suite, and the node tests wired into CI. Every new test was **proven able to fail** against the pre-fix tree before being accepted.
- feat(apple): **App Intents / Siri / Shortcuts + Spotlight catalogue search** (Apple Phase-1.5, #1415). The native app becomes a first-class citizen of the system surfaces: four **App Intents** — Open a Song, Play the Song of the Day, Go Live (host a service), Join a Service by code — exposed to **Siri + the Shortcuts app** via an `AppShortcutsProvider`, plus **Spotlight indexing** of the whole song catalogue so a hymn is findable straight from system search and a result tap deep-links into it. Built entirely on existing seams rather than new machinery: the intents drive the app through the SAME `DeepLinkRouter`/`AppNavigationState` path a Universal Link or Handoff continuation uses (via a new `pendingDeepLink` mailbox), `OpenSong`'s `SongEntity` query REUSES the one shared `songsMatching` matcher (never re-forked), and a Spotlight-result tap round-trips through the identical resolve seam. A `SpotlightIndexer` (in IHAppSupport, framework-neutral core + a `#if canImport(CoreSpotlight) && !os(tvOS)` live path — the only correct gate, since watchOS has no CoreSpotlight and tvOS prohibits its types) reindexes only when a SHA-256 corpus fingerprint changes or every 14 days, batched over the ~14k songs; the trigger also finally wires the Phase-1 `OfflineStore.upsert` resync path that had shipped dormant, so Siri entity resolution works offline. Content-gating-safe: intents/entities/Spotlight carry SongID/titles/codes only — never lyric text (a Lock-Screen/Spotlight-visible surface). Sign-in-gated where it must be (Go Live); anonymous where the web is (Join by code). No new entitlement or `project.yml` change. +36 Swift-Testing tests on the pure cores (Spotlight entry/fingerprint/indexer batching + tap round-trip, the intent→route map, the SongEntity resolver, the catalogue→cache→index sync); `swift test` green, iOS + watchOS + tvOS package cross-compiles green, SwiftLint 0, and the macOS app-scheme build's `appintentsmetadataprocessor` extracts the intent metadata cleanly. **Device-only (owner verifies):** real Siri phrase matching, the Shortcuts listing, Spotlight surfacing + result taps, cold-launch intent resolution. Full design: `.claude/apple-native-strategy.md` §2.3#4.
- fix(apple): **accessibility sweep across the Phase-2 live/remote surfaces** (#1562) — VoiceOver quick-wins found in review: `.accessibilityElement(children: .combine)` was swallowing the destructive "Stop Mirroring"/"Disconnect" buttons into their status-text group (stripping the button trait + destructive role) on `ServiceMirrorCard` and `RemoteControlSurfaceView`; the tvOS follow surface's only "Leave" control lived in a tap-to-reveal strip undiscoverable under VoiceOver; live-status changes (mirror phase, tvOS join errors, watch wake/failure notices) were silent instead of announced; and placeholder-only text fields lacked real accessibility labels.
- feat(apple): **Live Activities + Dynamic Island (now-singing) + the APNs push-bridge** (Apple Phase-2 PR-16, #1429). A "now-singing" Live Activity on the Lock Screen + Dynamic Island while a host drives a live session — current song / section / live-state, with **Next/Prev App-Intent buttons** for the host — kept fresh in the background by the server pushing updates over APNs. Two verifiable halves + a device-only tip. **Backend (verified):** a new shared `includes/live_activity_push.php` fires a push on every session state-change and end (hooked into all six broadcast/end sites — `live_follow_update`/`service_broadcast`/`*_leave`/`*_end` + the two supersede paths), building Apple's ActivityKit `aps` envelope from navigation intent ONLY (song title + section + displayState — **never lyric text**); **entirely dormant** until an APNs key is provisioned (new `/manage/configuration` card) AND a device registers a live-activity token — a verified no-op otherwise. A security fix: a `kind=liveActivity` `apns_register` now requires host/operator authorisation (was: any authenticated user against any active sessionId). **Client (verified to compile + unit-test):** a new `IHLiveActivity` iHymnsKit target — the `NowSingingActivityAttributes`/`ContentState` wire contract (matching the PHP `content_state` byte-for-byte against one shared fixture both languages test), pure reducer/section-navigator/intent-bridge cores, and the iOS-only ActivityKit controller + `LiveActivityIntent`s + the lock-screen/Dynamic-Island UI (all `#if os(iOS)`-gated so watchOS/tvOS/macOS still build); the `AppRootViewModel` drives it from the existing host event flow (never a second stream consumer), registers the activity's push token (scoped to the session, never logged), and `hostAdvanceSection` powers the Next/Prev intents. No schema change (`tblApnsTokens` #1410 was forward-looking). +37 Swift-Testing tests + a 57-assertion PHP suite; `swift test` green (874), iOS + watchOS package cross-compiles green, SwiftLint 0, `php -l` clean tree-wide. **Device-only (owner verifies):** the actual Live Activity start/update/end, Dynamic Island rendering, the real APNs push round-trip, and the Next/Prev buttons need a physical iPhone + a provisioned APNs `.p8` key + the Push Notifications capability — a device-matrix is on #1429. Full design: `.claude/apple-native-strategy.md` §2.3.
- feat(apple): **tvOS server-following projector — join a live Service session on the TV** (Apple Phase-2 PR-15, #1428). The multi-site/overflow + **LAN-fail degradation rung**: when the direct LAN TV remote can't reach the screen (AP isolation / VPN / firewall) or the screen isn't on the venue LAN at all, an Apple TV can instead **follow the Service session over the internet** — a new **Live** tab where the operator enters the venue join code and the TV mirrors whatever the leader is showing (song / section / blackout / logo), polling ~1 s. It reuses the whole existing stack rather than forking any of it: the PR-10 `ServiceModeEngine` + `LiveFollowerReducer` follower spine (gaining only a defaulted `role: .projector` — the server's #1406 projector budget of 90/min + `pollIntervalMs:1000`, already live on alpha, now also **persisted across a cold-start resume**), and the PR-5 `ProjectionViewModel`/`ProjectionCanvasView` render surface (the projector is simply its THIRD driver, after the Siri Remote and the LAN remote). The bridge between them, **`TVServiceFollowBridge`**, is a pure snapshot→projection-plan mapper mirroring PR-6's `TVProjectionBridge` (deduped display-state + song/section diff; maps the server `live|blackout|logo` onto the projection vocabulary, never `.frozen`; a legacy unparsable songId becomes a calm "can't be displayed" notice, never a crash), and **`TVServiceFollowCoordinator`** owns its OWN second `ServiceModeEngine` instance (the single-consumer rule) + a pure `ServiceModeEvent→FollowPhase` table. **Content-gating-safe by construction:** the sync wire carries navigation intent only — never lyric text — and the TV fetches each song under its OWN auth, so gating (and any licensed-content unlock) applies on the TV exactly as in a browser; an internet drop holds the last projection and resumes. No new backend (the #1406 role/budget/interval were already on alpha). +35 new Swift-Testing tests (the pure bridge mapping/dedupe/D-13 matrix, the pure phase table, projector role/cadence-encode + resume-persistence + legacy-record decode); `swift test` green (872), iOS + watchOS package cross-compiles green, SwiftLint 0 violations. Full design: `.claude/apple-native-strategy.md` §2.4.7.
- feat(apple): **optional Service-Mode mirror — the LAN TV remote can also drive the web congregants** (Apple Phase-2 PR-14, #1425). The bridge between the two deliberately-separate live features (strategy §2.4.1): the tvOS app owns the ONE canonical `(songId, componentIndex, displayState)`, a LAN remote command changes it, and — **only when the operator opts in** — that state is also mirrored to a running Service Mode session via the EXISTING `service_broadcast` endpoint so remote congregants stay in sync too. **Zero new backend** (the native call matches the web operator consoles' request byte-for-byte, server-authorised by the operator's own bearer via `serviceMode_userCanOperate`), and the LAN link never carries congregant traffic while the server never carries TV control — so an internet outage degrades ONLY the mirror, never the local projection. A new `@MainActor @Observable` `ServiceMirrorController` (injected broadcast closure — never touches `ServiceModeEngine`/`RemoteControlSession`/`RemoteSessionActor`/IHRP) taps the coordinator's `.controlling` echo (the TV's ACKED state — so mirroring on it IS "mirror-on-ack"), dedups on `songId|componentIndex|displayState` (lineIndex/revision excluded — the web congregant view has no line cursor), translates the LAN display vocabulary onto the narrower web one (`frozen→live`), and is single-flight + latest-wins-coalescing (never a queue). Failures never disrupt LAN control: `unauthorized`/403/404 stop the mirror with an honest notice, anything else degrades + retries the latest state once. The `ServiceMirrorCard` on the control surface is OFF by default and signed-in-gated — the operator types the session number (pre-filled from `IHSettingsStore`) and taps Start. Logs carry transitions + numeric session ids only (never a song id, join code, presence token, or bearer). Persistence helpers relocated into `RemoteControlCoordinator+Persistence.swift` for the LOC budget (the `+UIPhase.swift` precedent). +18 new Swift-Testing cases (controller dedup/coalescing/failure matrix via a gated spy, the `service_broadcast` wire shape, settings round-trip); `swift test` green, SwiftLint 0 violations. **Operator UX note:** the web operator consoles don't yet surface the numeric session id — a small display-only follow-up (owner decision) makes the number copy-able; until then it's read from the console URL. Full design: `.claude/apple-native-strategy.md` §2.4.1.
- feat(apple): **Apple Watch pocket remote — background wake-and-reconnect relay for the LAN TV remote** (Apple Phase-2 PR-11, #1423). The Watch drives the venue TV **while the iPhone is backgrounded / locked / in a pocket**: a reduced remote (Prev/Next verse, Lyrics/Black/Logo, honest status — strategy §2.4.2's set exactly) whose taps ride WatchConnectivity's documented background-wake (`sendMessage` from the watch launches iHymns in the background, locked-after-first-unlock included), where a new **`HeadlessRelayDriver`** — a second, process-lifetime session OWNER beside the screen-scoped `RemoteControlCoordinator` — reconnects the last-connected saved TV over PR-7's pinned fast path (saved token + pinned fingerprint + last-good address, <1 s typical, 4 s hard deadline), forwards the intent, replies synchronously, and idle-disconnects after a 20 s linger. **`Sources/IHLive/LANRemote/` carries ZERO diff** — no pairing/TOFU/Keychain/token-custody change; the headless driver is a new CALLER of the existing, security-reviewed session API, and a background burst that unexpectedly lands in a pairing ceremony is CANCELLED (`repairNeeded`), never completed. The seam is layered pure-first: a versioned watch↔phone wire protocol v1 (`WatchRelayCommand`/`WatchRelaySnapshot`/`WatchRelayReply` + a strict-version codec — install skew becomes honest "update" copy, never a decode crash), the pure `WatchRelayRules` route/merge tables (single-owner arbitration: the foreground coordinator always wins, and four awaited retire hooks stop any headless linger the instant a foreground connect starts), the pure `HeadlessRelayPolicy` wake/connect/linger state machine (a bounded ≤4 in-flight buffer, NO cross-wake queueing — a late-firing Blackout is a live-service hazard, so out-of-range is an honest failure, never a queue), the platform-neutral `RemoteControlRelayHub`, and two thin WCSession shells (`PhoneWatchRelayService` with `beginBackgroundTask` command + linger brackets and a force-disconnect expiration handler; `WatchRemoteController` + `WatchRemoteView`/`WatchRootView`, now the watch app's root). No new entitlement, background mode, plist key, or `project.yml`/`apple.yml` change; no credential ever crosses the watch link; no lyric text ever rides the relay (the snapshot is phase/tvName/songRef/displayState/revision only). +56 new Swift-Testing tests across 6 suites (codec round-trips + the <512-byte size guard, the full 36-cell route table + intent-map `isControlIntent` proof, every policy transition row with literal dates, snapshot mapping, hub arbitration, and a real-socket driver loopback E2E: wake-connect → forward → clean idle disconnect, warm path, in-order buffering, dead TV, revoked token, retire mid-linger, empty store); `swift test` green on host + iOS/watchOS package cross-compiles green, SwiftLint 0 violations. Device-matrix verification (real wakes, pocket/locked/BFU/force-quit rows) is captured honestly in the PR body per PR-6/7/8 precedent. Full design: `.claude/apple-phase2-pr11-pocket-spec.md`.
- feat(apple): **native Live Follow (leader + follower) + Service Mode congregant** (Apple Phase-2 PR-10, #1426, #1427). The SERVER-mediated live-worship-sync half of the native app — distinct from the peer-to-peer LAN TV remote (PR-4..8), sharing only the `IHLive` module, never code or protocol. A grown `LiveFollowEngine` actor hosts (`goLive`/`broadcast`, dedup on song+section, 30 s heartbeat + a scenePhase wake-beat, 409-on-broadcast → superseded) or follows (`join`/poll/leave, mutual exclusion with hosting — "host wins," web parity) a Live Follow session; a new `ServiceModeEngine` actor joins a venue's rotating code, polls, and holds the presence-token custody that (once the owner turns content gating on) quietly unlocks copyrighted lyrics for exactly the presence window — the token never rides an event payload, is persisted local-only (UserDefaults, never Keychain/synchronizable), and is logged only as an `IHLogSanitize` fingerprint. Both engines' follower halves drive ONE shared pure spine (`LiveFollowerReducer` — revision-monotonic dedup, the 180 s freshness window, the full server-declared/idle/backoff cadence table) so the two features can never quietly diverge on this logic. The presence-token → CCLI-unlock transport rides the SAME named cookie a browser congregant sends (`ihymns_sf_presence_token`, attached by `APIClient.makeURLRequest` whenever a shape-validated token is set, never a query param) — **zero backend change**. UI: `LiveHubView` replaces PR-7's honest "coming in a future update" placeholder with a real Join-with-a-Code sheet, a sign-in-gated "Go Live," a following screen (reusing the shared `SongComponentView`, never forking a second lyric renderer — content is fetched via the existing `song_detail` read, so nothing on the sync wire ever carries lyric text), a host console (`ShareLink` code sharing), and a bottom status banner on both root layouts. The native Service-Mode OPERATOR console (start/rotate/end a venue session) is deliberately out of scope — the web already serves it responsively, and its native consumers are already scheduled (PR-14/#1425, PR-15/#1428). +64 new Swift-Testing tests (reducer cadence table, mocked-transport engine loops, event→UI-state mapping, presence-Cookie transport rows); `swift test` green across macOS/iOS/tvOS/watchOS cross-compiles, SwiftLint 0 violations. Full design: `.claude/apple-phase2-pr10-spec.md`.
- feat(apple): **tvOS projection canvas + driver-agnostic projection engine** (Apple Phase-2 PR-5, #1504). The tvOS side of the LAN remote — the screen the congregation sees, and the "brain" that decides what's on it. A pure, total **`ProjectionResolver`** is the ONE place "what does *next* mean" is ever defined (relative-intent resolution with clamping, karaoke line-mode vs component-mode, rollover that skips empty components, `scroll` capped + terminating). A `@MainActor @Observable` **`ProjectionViewModel`** owns song fetching (under the TV's OWN auth, so content gating applies on the TV — the LAN link never carries lyric text), the canonical `(songId, componentIndex, lineIndex, displayState)` state, a latest-wins fetch guard, the frozen-snapshot rule (canonical keeps moving behind a freeze; remotes stay truthful), and a driver-facing `AsyncStream<ProjectionState>` whose 4-tuple **exactly matches PR-4's `TVListenerActor.updateState(...)`** — the seam PR-6 (#1421) bridges the LAN listener onto in ~30 lines. Plus **`ProjectionCanvasView`** (full-bleed lyrics/blackout/logo/frozen, reusing the shared `ihLyricLineStyle` typography seam + a karaoke current-line highlight) and **`TVRootView`** (Project · Songbooks · Search tabs) replacing the tvOS placeholder shell — reusing the EXISTING `SongbooksView`/`CatalogueListView` unmodified. The view-model is **driver-agnostic** (no `import Network`, no listener reference), so the Siri Remote (this PR), the LAN remote (PR-6) and a future server-follow projector (PR-15) all drive it without ever re-forking resolution logic. Compiling `IHFeatures` for tvOS **for the first time** surfaced (and fixed, minimally in-place with `#if os(tvOS)`) unavailable-API stragglers in 13 shared views (`ShareLink`/`.swipeActions`/`.draggable`/`DragGesture`/`DisclosureGroup`) — **iOS/macOS behaviour is byte-identical** (one, `SongMetadataView`, became a DRY improvement). **Apple-package + tvOS-shell only; dormant until PR-6 wires the LAN listener onto the seam.** +20 deterministic Swift-Testing tests (10 resolver + 10 view-model, injected fetcher — no network); `swift test` **575 green**, SwiftLint **0 violations**, and a new **tvOS Simulator build step** added to `apple.yml` (the shared library had never been CI-compiled for tvOS before). Full design: `.claude/apple-phase2-pr5-spec.md`.
- feat(apple): **IHLive/LANRemote — IHRP/1 remote-control protocol + TV/remote actors** (Apple Phase-2 PR-4, #1420). The peer-to-peer LAN transport a phone/iPad/Mac/Vision remote uses to drive the tvOS projector **directly** (no server round-trip): a versioned `Sendable`+`Codable` `IHRPMessage`/`IHRPFrame` wire vocabulary (carries ~200-byte **navigation intent only — never lyric text**; the TV fetches `song_detail` under its own auth), a pure length-prefixed **TLV framer** with a hard 4 KB/frame cap (worst-case buffering is bounded — an oversize declared length is dropped the instant its prefix is read, never buffered), a **`TVListenerActor`** (`NWListener`, TLS 1.3, `.unpaired → .pairing → .paired` per-connection confinement + a concurrent-unpaired-connection cap), a **`RemoteSessionActor`** (`NWBrowser` discovery + `NWConnection` connect with **pin-before-trust** self-signed P-256 certificate-fingerprint pinning, **fail-closed** on any mismatch → `.fault`), and **seq+timestamp last-writer-wins** for concurrent multi-remote conflicts. Pairing tokens are SHA-256-hashed at rest and **never logged verbatim**. Pairing *policy* (Keychain persistence + the 6-digit/QR ceremony) is deliberately deferred to PR-6 behind a small `LANRemotePairingAuthority` seam. **Apple-package only — no web/PWA/live surface; dormant until PR-5/PR-6 wire it into the app shell.** deterministic Swift-Testing suites that run on **every** CI runner (TLV framer caps + fuzz, IHRP JSON codec round-trips, pure last-writer-wins ordering, SHA-256 fingerprint known-answer vectors) plus **two environment-gated** suites a headless/unsigned `swift test` binary can't run and that CI cleanly **skips**: keychain-bridged **identity generation** (`lanRemoteKeychainIdentityAvailable()` — the login-keychain `SecIdentity` route returns `errSecItemNotFound` on the runner; forcing the file-keychain isn't an option since the same factory must run on iOS/tvOS) and the live-TLS-127.0.0.1 **loopback** integration (`IHYMNS_LAN_LOOPBACK_TESTS=1` — needs macOS Local Network permission). Both run on a dev Mac and on PR-6's signed on-device test target. `swift test` green, SwiftLint 0 violations, LOC-budget + privacy-manifest + no-secrets gates all pass.
- chore(version): **bump to 0.4000.0** — milestone marker for the Credit-People/admin + Apple Phase-2-foundation batch (`includes/infoAppVer.php`; README badge + table + OpenAPI `info.version` + help.php synced; PWA SW cache auto-syncs, #81). Up from `0.2750.0`. Highlights: **Credit People** — merge-target live-search typeahead (#1500), optional maiden-surname field + migration (#1501), link member-people to a Group person (#1502), bulk-promote cited-but-unregistered names to the registry (#1503), place live-search loading-state + full-hierarchy selection confirmation (#1507), whitespace-trim on identifier/URL entry (#1508); **Admin** — manual delete of a selected database backup with path-confinement + CSRF + confirm (#1509); a fix for a literal `</script>` in inline-script comments that broke Credit People (stray text + dead auto-detect) and the Service-Mode projection/lead pages (#1498); **Apple Phase 2 (foundation, dormant)** — live broadcast payload v2 with the load-bearing bidirectional `blank`↔`displayState` bridge (#1405), projector poll-budget + `pollIntervalMs` (#1406), and a one-pass forward-looking schema batch for the whole remote-control/device-pairing family (`tblAuthDeviceCodes`/`tblSessionControlTokens`/`tblApnsTokens` + `tblServicePresence.Role` + `tblApiTokens` device columns, #1407/#1408/#1409/#1410). Deferred to a follow-up (budget-paced): the Swift observability `IHLog` (#1505/#1506), the §3.2 Activity-Log lifecycle breadcrumbs, and the macOS DMG packaging (#1510).
- chore(version): **bump to 0.2750.0** — milestone marker for the Apple-readiness + hardening train landed on `alpha` this cycle (`includes/infoAppVer.php`; README badge + table + OpenAPI `info.version` synced to match; the PWA service-worker cache version auto-syncs off it, #81). Up from `0.2501.0`. Highlights since 0.2501.0: web Sign in with Apple activation docs + the Apple Provisioning Runbook expanded to full click-by-click TestFlight/App-Store steps with CarPlay entitlement **granted** and a Xcode-emulation testing walkthrough; a security fix redacting `presenceToken`/live-follow `code`+`token` from the Activity Log (CWE-532, #1492); an Apple Deploy CI kill-switch + doc-path excludes + platform-SDK install + export-compliance (#1494); and two owner-approval-pending strategy docs (remote-control observability, sequenced production-readiness). A full open-issue reconciliation closed 10 completed-but-open issues and status-annotated 67 more.
- chore(version): **bump to 0.2501.0** — the native Apple app release train (milestone 0.2500.0) plus secret encryption-at-rest (#1466) (`includes/infoAppVer.php`; README badge + table + OpenAPI `info.version` synced to match; the PWA service-worker cache version auto-syncs off it, #81). Up from `0.1321.0`.
- feat(apple): **native Apple Universal app — Phase 1 client + backend built** — the in-development native Apple app (one Universal app across iOS / iPadOS / macOS / tvOS / watchOS / visionOS, epic #895) reached Phase 1 web-app feature parity on the `feat/apple-universal` branch, together with the backend endpoints and Apple App Site Association (AASA) it depends on. The branch has not been merged to `alpha` or deployed to any live environment.
- feat(auth): **Sign in with Apple** (`?action=auth_apple`, #1402) — verifies the Apple identity-token JWT against Apple's published JWKS and mints the standard iHymns bearer token.
- feat(auth): **self-service account deletion** (`?action=account_delete`, #1403) — bearer-authed, re-authenticated deletion of the caller's own account, satisfying App Store Review Guideline 5.1.1(v) for the native app.
- feat(apple): **in-app account deletion UI** (#1477) — the native app now surfaces a re-authentication-gated "Delete Account" flow (Password or Email-Code, reusing the login UI) that calls `?action=account_delete`, satisfying App Store Review Guideline 5.1.1(v) **in the client** (#1403 was backend-only; `AccountView` previously offered only Sign Out). A wrong/expired re-auth credential (401), rate-limit (429), or last-Global-Admin block (409) leaves the session untouched; only a confirmed 200 clears local token + favourites + setlists + offline caches, exactly like sign-out. +5 Swift-Testing files; `swift test` 514 green, SwiftLint clean.
- feat(analytics): **first-party analytics ingest** (`?action=analytics_ingest`, #1448) — anonymous, consent-gated event batch from native app clients; no user id, device id, or IP address is stored.
- feat(pwa): **admin-configurable native app store IDs + native-app install banner** (#1462) — a global admin can set an App Store / Google Play / Amazon Appstore listing at `/manage/configuration`, which replaces the PWA "Add to Home Screen" prompt with a native-app download banner on that platform once configured. Blank by default; no store listing exists yet.
- feat(security): **secret encryption-at-rest — full P1–P5 build, readers-first, dormant** (#1466). The 8 secrets the running app keeps in `tblAppSettings` (SMTP password, SendGrid/Mailgun/SES keys, Azure Graph client secret, Gmail service-account JSON, and the Sign-in-with-Apple `.p8`) get a self-describing `enc:v1:<alg>:<keyid>:<nonce>:<ciphertext+tag>` envelope — libsodium `crypto_secretbox`, or AES-256-GCM fallback — from `includes/secret_crypto.php`, decrypted at runtime with a master key held in `appWeb/.auth/secrets_master_key.php` (sibling of the docroot, git-ignored, never web-served). A DB dump/backup or a SQL-injection then yields only ciphertext; disclosure needs **two** primitives (a DB read **and** a filesystem read of the key). **Everything is DORMANT and a verified no-op** — every value stays legacy plaintext until a master key is provisioned on all 3 docroots **and** the gated encrypt-in-place migration runs. Shipped in one branch:
  - **P1** — engine + decrypt-capable readers (`EmailService.php`, `getAppSetting()`) + fail-closed encrypt-on-save (`manage/configuration.php`).
  - **P2** — `.github/workflows/deploy.yml` seeds the master key from the `SECRETS_MASTER_KEY` GitHub Secret **only if absent** (idempotent, 64-hex-validated, never clobbers a manual key; `.auth` mirror excludes it) + a Global-Admin **Secret-encryption panel** on `/manage/setup-database` (status, non-secret per-docroot key fingerprint, encrypted/legacy inventory, a decryptable **sentinel** health check, a per-docroot **beacon** parity table, and a "Generate master key" tool that displays a CSPRNG key once and **never writes `.auth/`**).
  - **P3** — the gated `manual`+`confirm=1` `migrate-secret-encrypt-inplace.php` (refuses unless a key is loaded **and** the cross-docroot sentinel is green) + a hardened **Rotate/re-encrypt** tool (password re-auth with brute-force lockout, type-to-confirm, and a **code-enforced cross-docroot keyid-parity gate** so a misordered rotation can't strand a lagging docroot; every decrypt audit-logged by key name only).
  - **P4** — `opcache-bust.php` now accepts the opcache key stored as a one-way `sha256:<hash>` (backward-compatible; the CI sender is unchanged).
  - **P5** — operator runbook in `DEV_NOTES.md`.
  Adversarially reviewed twice (23 findings fixed across both passes, incl. a `configuration.php` load-order fail-open that would have silently stored plaintext, keyid envelope-safety, and the rotation cross-docroot gate). CI guards: `tests/php/test-secret-crypto.php` (45 assertions) + `tests/php/test-secret-crypto-admin.php` (23). Does **not** protect a compromised runtime (RCE reads both). Full plan + honest threat model + operator runbook: `.claude/secret-encryption-strategy.md` / `DEV_NOTES.md`.

- fix(live): **align the two live-following systems' freshness + wake behaviour** (#1386 follow-up). The #1335 Service-Mode join/poll used tighter staleness windows than #1268 Live Follow (90 s join / 120 s poll vs 180 s), so a briefly-backgrounded projector tab could drop congregants or reject joins sooner. Both now share ONE `LIVE_SESSION_FRESHNESS_SECONDS = 180` constant (`includes/service_mode.php`) — 6× the 30 s broadcaster heartbeat. Also, the broadcaster's visibility/focus wake handler called `service_code_current`, which was **read-only**; it now refreshes `LastHeartbeatAt` too (operator-gated, active-only), so a projector waking from sleep immediately revives the session instead of waiting for the next 30 s code-rotate — mirroring Live Follow's wake-beat. No schema change. chore(version): bump to **0.1321.0**.

- fix(admin-login): **"Invalid form submission" lock-out when the login tab is left open** (#1386). Shared-hosting PHP session GC (`gc_maxlifetime` ≈ 24 min) reaps the *server-side* session while the browser cookie persists, so the baked CSRF token vanishes and a legitimate sign-in is rejected (incognito worked only because it's a fresh session). `login.php` now accepts a sign-in when EITHER the session token validates OR the POST is verifiably **same-origin** (`Origin`/`Referer` host matches `HTTP_HOST`) — the same robustness `validateCsrfRequest()` gives AJAX writes (rule #29), applied to the login form. Cross-origin login-CSRF is still rejected (foreign/absent `Origin`/`Referer`).
- fix(live-follow): **the home "Join a live service" button now also joins a worship-team "Go Live" session** (#1386). There are two distinct live-following systems — #1268 Live Follow ("Go Live" on a song → joined by the per-song "Join Live") and #1335 Service Mode ("Join a live service" on the home page) — and the prominent home button only matched Service-Mode (`SessionKind='service'`) sessions, so entering a "Go Live" code there always failed with "that code isn't active". The home join now transparently falls back to the Live-Follow join when no Service-Mode session matches the code, so the one discoverable button works for both. chore(version): bump to **0.1320.0**.

- fix(security): **deep second-pass security audit — auth brute-force, CSV-formula injection, admin-view stored XSS** (#1386 follow-up; deep adversarial sweep, 8 attack angles + verification). Fixed: (1) **admin-login brute-force** — `/manage` `attemptLogin()` now enforces the same IP lockout (10 fails / 15 min) the public `auth_login` has; the highest-value accounts were previously unthrottled; (2) **email-login-code brute-force → account takeover** — `auth_email_login_verify` (6-digit code, 10-min window) had no verification throttle; now caps failed code-verifies per IP; (3) **CSV / formula injection** (CWE-1236) in every admin export (activity-log, analytics, song export, CCLI report, editor skipped-IDs) — a new shared `includes/csv_safe.php` (`ihymns_fputcsv()`) prefixes leading `= + - @` so a malicious song title / search query / user-agent can't run as a formula on an admin's machine; (4) **stored XSS in admin views** — the credit-people merge-preview (link/IPI/ISNI free-text → `innerHTML`) and the editor revision-history username now escape via a quote-safe helper, and `admin_user_create` enforces the username charset allow-list that registration already used; (5) **IDOR** — `setlist_template_save` now verifies org membership before tagging a template to an org (mirrors the `setlist_schedule_save` fix); (6) `bulk_songs` `Cache-Control: private` when gating is on. Latent tier-cap bypasses + Service-Mode presence over-grant that only matter once `content_gating_enabled='1'` are tracked as enablement blockers in #1388. Also removed a duplicate `head-libs` include on the analytics page. chore(version): bump to **0.1319.0**.

- fix(security): **full adversarial security audit — XSS hardening, Host-header injection, IDOR** (#1386, clears CodeQL alert #3 on promotion to `main`). A 6-dimension sweep (input/SQL/XSS/CSRF/auth-IDOR/secrets+dangerous-fns); **SQL-injection, CSRF and secrets were CLEAN**. Fixed: (1) **DOM XSS** — the editor revision-history title + error message now `escapeHtml()` the `songId`/`err.message` (CodeQL #3); (2) **attribute-context XSS root cause** — the shared `js/utils/html.js` `escapeHtml()` used `textContent→innerHTML` which leaves `"`/`'` intact, so a curator/cross-user value could break out of a double-quoted attribute. It now escapes quotes, fixing the favorites/router/search/search-history/home-page findings in one shot; (3) **public stored XSS via `</script>` breakout** — `index.php` JSON-LD + `window.iHymnsConfig` and `person.php` JSON-LD add `JSON_HEX_TAG|_AMP|_APOS|_QUOT` so a DB song/songbook/person name can't close the `<script>`; (4) **Host-header injection / CWE-640 reset-link poisoning** — the password-reset, magic-link, email-verification and admin-notice email links, `getCanonicalUrl()`, and the gating-verifier probe now resolve the host via a new allow-list helper `appCanonicalHost()` instead of raw `$_SERVER['HTTP_HOST']`; (5) `analytics.php` CSV export strips `$_GET['export']` before `Content-Disposition`; (6) `setlist_schedule_save` **IDOR** — adds setlist-ownership + org-membership guards and fixes a phantom `CreatedBy` column (→`UserId`; it threw under STRICT). One LOW finding deferred (CF-Connecting-IP trust — needs a Cloudflare-IP-range infra decision, tracked in #1386). chore(version): bump to **0.1318.0**.

- chore(audit): **retire the legacy JSON-corpus Song Editor** (#1384) — a full-codebase audit for remaining snapshot/pre-MySQL patterns (follow-up to the #1380 setlist-snapshot fix) confirmed the bug class is otherwise closed (DB-direct reads hold everywhere). The one live hazard: `private_html/editor/` still wrote the entire ~3,600-song corpus to a frozen `songs.json` the live MySQL app never reads (silent data loss). Removed the file-writer backend + old UI; `index.php` is now a 301 redirect to the live `/manage/editor/`. chore(version): bump to **0.1317.0**.

- feat(live-follow): **NAT-safe join-token** (#1377) — a congregation behind one church-WiFi NAT is a single IP, so the per-IP poll rate-limit throttled the whole room. Live-Follow now mints a per-follower token at join and rate-limits polling **per token** (mirrors Service Mode), so a full room follows without being collectively throttled. Join cap raised 60→120/min.
- feat(sotd): **country-aware Song of the Day** (#1376) — adds national-civic themes gated on the user's country (US Thanksgiving 4th-Thu-Nov, Canadian Thanksgiving 2nd-Mon-Oct, US Independence Day), on top of the existing hemisphere awareness (broadened southern-zone coverage). No country / old client = verified no-op; liturgical themes stay global.
- fix(maintenance): **a `/manage`-logged-in admin can now use the main app** (#1382) — during maintenance, devs were stuck on the admin portal because `/manage` login only set a path-scoped session and never the cross-surface `ihymns_auth` token the bypass resolves. `/manage` login now mints that token (same shape as the main-app sign-in), so an admin is recognised across the whole site; logout revokes it. Cookie helpers extracted to a shared `includes/auth_cookie.php`. chore(version): bump to **0.1316.0**.

- feat(gating): **complete the dormant content-gating system** — uniform web/offline tier gating (#1357) + signed-URL sealing of licensed audio (#1358). Entirely **dormant + a verified byte-identical no-op** while `content_gating_enabled='0'` (the live state). #1357: the `song.php` HTML render (and the offline `bulk_songs` bundle that renders through it) now strip audio/sheet/MIDI affordances by the **same registry tier caps** the JSON API uses (web/offline ↔ API parity), with the Service-Mode presence token still unlocking (entity-grant **OR** tier-grant). #1358: a new `includes/audio_signing.php` (HMAC signed/expiring URLs, triple-dormant), a gated `audio-media.php` streaming route (Range/206, path-traversal-hardened, `hash_equals`), and a `.htaccess` `/audio/` rewrite — the raw `/data/audio` seal ships **commented** (uncomment per-env, last). Plus the `audio_signing_enabled='0'` migration (rule #19/#20), a Global-Admin **byte-identical no-op verification harness** (`/manage/gating-noop-verify`), and a CI no-op test. No schema beyond the one settings row; no new tier columns (reuses `CanPlayAudio`/`CanDownloadPdf`). Adversarially verified (3 reviewers, 0 blockers). **Operator opt-in required to activate** (provision `AUDIO_SIGNING_KEY` in `.auth/`, run the migration, staged rollout). chore(version): bump to **0.1302.0**.

- fix(live-follow): **"Go Live" code reported as wrong on the follower's device** (#1375). The host (an admin) bypasses maintenance, so "Go Live" mints a code — but the anonymous follower hit a **503 maintenance** response that was shown as "Session not found", blaming a good code. Now the follower (and Service-Mode congregant) gets a distinct "iHymns is briefly down/​unavailable — the code is fine" message and the poll loop keeps following so it resumes. **Codes are also forgiving** now — client + server strip spaces/hyphens/NBSP/smart-punctuation (`"ABC 234"`, `"abc-234"` work), and the entry field disables iOS autocorrect + autocapitalises. (Case was never the cause — it's folded three times.) The join/poll **freshness window widened 90s→180s** plus an immediate heartbeat on tab-visible/focus, so a briefly-backgrounded host session no longer goes stale.
- feat(sotd): **Song of the Day — hemisphere awareness** (#1374). The client sends its hemisphere (derived from its timezone, privacy-safe, no GeoIP), and the **Harvest** theme now falls in each hemisphere's own autumn (Southern = March, Northern = October). Liturgical themes (Advent/Easter/Pentecost…) stay globally synchronous; an old client with no signal is a verified no-op. chore(version): bump to **0.1301.0**.

- chore(version): milestone bump to **0.1300.0** (`includes/infoAppVer.php`; the PWA service-worker cache version auto-syncs off it, #81). README + OpenAPI spec updated to match.

- feat(credit-people): **expand Other Identifiers to the authority-control databases + paste-a-URL auto-extract** (#1367). The "Other identifiers" picker grows from VIAF/Wikidata/ORCID to a central registry — adds **GND, FAST, WorldCat, LoC, IdRef, Trove, LibraryThing, Open Library, CiNii** (+ ISNI display), grouped International/National/Academic/People. One registry (`CREDIT_IDENTIFIER_TYPES` in `credit_people_helpers.php`) now drives the dropdown, the save allow-list + per-type value validation, and the public chip link-out — adding a provider is one line, no schema change (`IdentifierType` is already VARCHAR, rule #20). **Auto-extract:** paste a recognised authority URL (e.g. `d-nb.info/gnd/…`, `id.worldcat.org/fast/…`, `entities.oclc.org/worldcat/entity/…`) into an External Link and the matching Other Identifier is detected, the bare ID extracted, and the row auto-added — making rich author data quicker to enter. chore(version): bump to **0.1255.0**.

- fix(places): **live place typeahead always returned HTTP 500** — `manage/places-api.php` required `dirname(__DIR__)/includes/auth.php` (the public path), but the auth helpers live in `manage/includes/auth.php`; the public file doesn't exist, so a `require_once` fatal fired **at load, before the try/catch**, surfacing as a bare 500 with no body (the admin `detail` never rendered). Broken since the endpoint shipped (#996) — curators only ever saw "saved as typed." Now requires `__DIR__/includes/auth.php`; db_mysql.php/places.php stay on the public path (they really are there). Added a `register_shutdown_function` diagnostic so any future load-time fatal returns a visible JSON 500 instead of a silent one (#1365). chore(version): bump to **0.1254.0**.

- chore(version): bump app version to **0.1253.0** (`includes/infoAppVer.php`; the PWA service-worker cache version auto-syncs off it, #81). README + OpenAPI spec updated to match.
- feat(credit-people): **partial birth/death dates** — historical writers often have only a known YEAR (or month + year). The editor's date fields now accept **`YYYY`**, **`MM/YYYY`**, or **`DD/MM/YYYY`** (was a strict full-date picker that rejected anything incomplete). Stored as the existing `DATE` column (normalised to the first of the period) plus a new `tblCreditPeople.BirthDatePrecision` / `DeathDatePrecision` (`year|month|day`) so display + round-trip know how much is real (`1823` / `March 1823` / `15 March 1823`). Shared `includes/partial_date.php` parser/formatter; all three save paths (manage add/update + the API) parse + store precision (existence-gated — degrades to a full-date save on an un-migrated env). Run the **Credit-people partial dates** migration card.
- fix(editor): **new songs default to the "Misc" songbook** so auto-save works immediately. A new draft with no songbook silently failed to save (the songbook is mandatory) and curators lost edits believing auto-save had run. New drafts now pre-select Misc (the server already coerced blank → Misc on save), and the empty-songbook check is a **non-blocking warning** rather than a hard error — the save always proceeds (filed under Misc), and changing the songbook reassigns it. No more silent data loss.
- fix(api): **Service Mode "Go Live" fatal — `Call to undefined function checkRateLimit()`** (#1360). `includes/rate_limit.php` (which defines `checkRateLimit()`) was `require_once`'d only inside the `setlist_collab_invite` case, so every other caller (`service_session_start` / `service_broadcast` / `service_operator` / `live_follow_*`) fatal'd. Now loaded top-level in `api.php` so it is always defined. Pre-existing/latent since Service Mode #1335; surfaced on first live use.
- chore(version): bump app version to **0.1250.0** (`includes/infoAppVer.php`; the PWA service-worker cache version auto-syncs off it, #81). README + OpenAPI spec updated to match.
- docs(readme): remove the static **Song Library** catalogue block — song counts/songbooks now live in the backend MySQL database (`tblSongbooks` / `tblSongs`), not tied to builds, releases or deployment, so a hard-coded catalogue snapshot in the README only goes stale. Query `tblSongbooks` for the live count.
- feat(api): **server-side content gating + extensible tier registry + read rate limiting + robust CSRF** (#1352 / #1353 / #1354) — turns the previously-advisory gating model into real server-side enforcement, makes the capability set extensible in one line, caps the heaviest public reads, and ends the sporadic "CSRF error" on admin writes. Five strands:
  - **Robust same-origin CSRF** — new `validateCsrfRequest()` in `manage/includes/auth.php` passes a write when EITHER a valid session token is present OR the request is provably same-origin: it requires the `X-Requested-With` header (a browser cannot set it cross-origin without a CORS preflight) AND any present `Origin`/`Referer` host must match `HTTP_HOST`. Because it does not depend on a token that can go stale in a long-lived tab, the intermittent CSRF failures on **duplicate-songs merge/delete**, **places-api** and the editor are fixed. Wired into duplicate-songs merge/delete, places-api, and a single top-level POST gate covering **all** legacy `/manage/editor/api.php` writes (clients already send `X-Requested-With`).
  - **`save_song` moved to the v2 editor API** — the whole-song save now lives in the shared core `manage/editor/save_song_core.php` (`editorSaveSongCore()`), served by **both** `api.php` (a back-compat shim) and `api2.php`; the editor POSTs the save to api2 under its `X-Requested-With` CSRF gate. One save path, no fork.
  - **Extensible tier-capability registry** (#1352) — `TIER_CAPS` in `includes/access_tier_validation.php` is now the single registry for `tblAccessTiers` caps. The 7 original caps stay as their own `TINYINT` columns (the native-app API contract reads them); **new** caps are `'json'`-backed keys inside the additive `tblAccessTiers.Capabilities` column (`migrate-add-tier-capabilities-json.php`, rule #20). **To add a gateable feature, add ONE line to `TIER_CAPS`** (e.g. `'CanRequestSongs' => ['Requests', 'Submit song requests', 'json', 0]`) and run the JSON-backed tier-capabilities migration card on `/manage/setup-database` — the admin checkbox (`manage/tiers.php`), both API CRUD endpoints (`admin_tier_create`/`update`), the `access_tiers` API emit (as `canRequestSongs`) and content gating (`checkTierAccess`) all pick it up with **no schema change**.
  - **Content-gating enforcement** (#1353) — new `includes/content_gating.php::contentGatingApply()` strips gated fields (lyric body, media) from the `song_detail` / `song_data` / `random` API payloads by the requester's tier cap; `ccli_validator.php::checkTierAccess()` now resolves caps from the **live** `tblAccessTiers` row (registry-driven), and the `bulk_audio` offline manifest is entity-gated too. **Entirely dormant and a verified no-op** unless `tblAppSettings.content_gating_enabled='1'`; fail-open and STRICT-safe on un-migrated / edge envs.
  - **Read rate limiting** (#1354) — new `includes/read_rate_limit.php::enforceReadRateLimit()` + `tblReadRateLimit` windowed counter on the heavy public reads (`song_detail` 240/min, `search` 120, `songs_index` 120, `related_songs` 240, bulk 60), keyed **per token where present, else per IP** (a NAT-shared congregation isn't penalised), emitting `429` + `Retry-After`. Fail-open and dormant until `migrate-add-read-rate-limit.php` runs. Window boundaries are computed SQL-side (no per-node clock drift).
  - **XSS hardening** — `JSON_HEX_*` flags added on the remaining inline-script `json_encode` sites so admin-controlled strings can't break out of a `<script>` block.
- feat(gating): **tier-aware gating on the web + offline path** (#1357) — `song.php` (and the `bulk_songs` offline bundle it renders through) now composes the TIER axis with the entity model so it gates copyrighted lyrics consistently with the `song_detail` API. Two independent axes — entity (`require_licence`, authoritative on deny) and tier (the viewer's plan, governs copyrighted) — with a valid Service-Mode presence unlock overriding the tier; so a licence-holder, a premium user and a present congregant are each handled without over-gating. Dormant + fail-open.
- feat(api-platform): **API key usage dashboard + `catalogue:read` keyed reads + dev quickstart + `content:gated` + self-serve requests** — `/manage/api-keys` shows per-key **usage today** + an editable **per-key rate-limit** modal (Phase B); a `catalogue:read` scope lets a trusted integrator read the public catalogue at its **own** per-key limit instead of the anonymous IP limit (Phase A, `enforceReadRateLimitKeyed()`; public stays open); a **developer quickstart** + Swagger Try-it (Phase C). A separate, **dormant `content:gated`** scope lets a deliberately-vetted key read gated/copyrighted content unstripped (a per-partner licensing grant — kept apart from `catalogue:read` so a plain read key never unlocks it). **Self-serve key requests** (Phase D pt 1, new `tblApiKeyRequests` + `request_api_keys` entitlement): an admin requests a `catalogue:read` key with a justification; a global admin reviews the queue and approves (mints + relays) or rejects. Reuses the #1066 key infra. Strategy: `.claude/api-platform-strategy.md` (Phase D webhooks + #1358 audio gating still to come).
- fix(api): **scope + cap the heavy public reads** — `?action=songs` with no `songbook` called `getSongs(null)`, materialising the whole ~3,600-song corpus unauthenticated (the #929 OOM, the same hole `bulk_songs` was hardened against, verified no client used it) — now 400s without a songbook and is rate-limited; `songs_list` rate-limited; `X-RateLimit-Remaining` added.
- fix(csrf): **completed the manage AJAX CSRF sweep** — every AJAX-write admin page (duplicate-songs, places-api, editor, api-keys, tags, languages, activity-log, songbooks) now uses the robust `validateCsrfRequest` (the last three were a client-sends-header/server-ignores-it mismatch); `<form>`-POST pages correctly keep `validateCsrf` (a reload re-bakes the token).
- feat(service-mode): **broadcaster front-ends — projection song-nav + leader device** (#1335) — the two song-driver UIs that let an operator push the congregation's current song to every joined device (the `service_broadcast` endpoint already served both mechanisms). One shared driver core, `js/modules/service-broadcast.js` (`ServiceBroadcaster`): song search over `/api?action=songs_index`, **arrangement-aware** section navigation (projects through the song's `arrangement` so a broadcast lands on the same `.lyric-component` the congregant scrolls to, mirroring `song.php` #160), and the broadcast POST with de-dupe + ended-session teardown — consumed by **both** front-ends (no per-page fork; rule #1/#26). `/manage/service-projection` gains a dockable operator console over the projection overlay (collapsible for a clean code-only screen); new `/manage/service-lead` is the separate leader-device page (lists the operator's active **channel-scoped** sessions → connect-and-drive, a join-code read-out, a 45s keepalive heartbeat, stop-driving / end-service). Adversarially reviewed; two high findings fixed in the same change — a stored-XSS where org-admin-controlled venue/schedule names entered the inline `<script>` without `JSON_HEX_*` (now hardened, the `tiers.php`/`credit-people.php` convention), and the arrangement-blind section index above. Service Mode stays **dormant** (`content_gating_enabled='0'` + no `require_licence:ccli` rows). Remaining rule #26 items — the projection **QR** + a **live multi-device verify** on alpha — tracked in #1339.
- chore(version): bump app version to **0.990.0** (`includes/infoAppVer.php`; the PWA service-worker cache version auto-syncs off it, #81).
- fix(admin): **`/manage/duplicate-songs` rendered a fully blank page** (#1228) — the GET detection path relied on `$db->query()` returning `false` on error, but the DB layer runs mysqli under `MYSQLI_REPORT_STRICT` (it **throws**), so a query against a table unmigrated on the environment (the newer `tblSongLinkSuggestionsDismissed`) white-screened the whole page. Each optional edge-source table is now existence-probed, the `NOT EXISTS(dismissed)` pre-filter is dropped when that table is absent, and the whole detection pass is wrapped in a try/catch that degrades to an actionable error card (pointing at Setup Database) — never a blank page.
- feat(songbooks): **badge unofficial songbooks** (#1223) — the public **/songbooks** list, the **home** songbook grid and the **songbook header** now show a themed **"Unofficial"** badge beside the abbreviation on any book with `tblSongbooks.IsOfficial = 0` (curated groupings / pseudo-songbooks, #502). Official + unofficial books surface together as one "Songbooks" family — **presentation only, no storage merge** (a song's home is `tblSongs.SongbookAbbr NOT NULL`; converting an unofficial book to a Collection would orphan its songs). One shared `.songbook-unofficial-badge` class (Bootstrap 5.3 theme-aware `warning-subtle` tokens → adapts to light / dark / high-contrast); the label is folded into each tile's accessible name (mirrors the language-badge a11y pattern #680/#856) and the word "Unofficial" — not colour alone — carries the meaning (WCAG 1.4.1). Completes the last open item on #1223.
- refactor(admin): **rename "Catalogue" → "Collection"** in the user-facing admin UI (#1223) — page title, headings, buttons, empty-state + member copy, confirm dialogs and every flash message on `/manage/catalogues`, plus the admin-nav entry. The table (`tblCatalogues`/`tblCatalogueSongs`), route (`/manage/catalogues`), activity-log keys (`admin.catalogues.*`), entity type `'catalogue'`, form fields and the `manage_songbooks` entitlement all stay `catalogue` **internally** — owner decision, no data/route migration. Vocabulary folds into #945.
- feat(tags): **canonicalisation suggestions** (#1222) — `/manage/tags` now lists curator-added tags that fuzzily match a standard CCLI/OpenLyrics theme (shared `includes/song_similarity.php` scorer) and offers a one-click **fold-into-standard** that reuses the existing irreversible **Merge** (variant = source/deleted, standard theme = target). Steers ad-hoc free-text tags back toward the canonical vocabulary.
- feat(tags): **standard CCLI / OpenLyrics theme vocabulary** (#1152) — new `appWeb/.sql/migrate-seed-theme-vocabulary.php` adopts the OpenLyrics `themelist.txt` taxonomy (the same vocabulary as CCLI SongSelect) so Browse-by-Theme is interoperable with the wider worship-software ecosystem. Strictly-additive, idempotent migration adds `tblSongTags.ParentId` (self-FK → 2-level Parent/Child hierarchy), `tblSongTags.CcliThemeId` (dormant SongSelect theme number) and `tblSongTags.Source` (`'curator'` | `'ccli-openlyrics'`); leaf names live in `Name`, the path via `ParentId`; same-named curator tags are promoted to the standard source rather than duplicated. Surfaced with its hierarchy on `/manage/tags`.
- feat(admin): **Duplicate & Counterpart Review** (#1215) — `/manage/duplicate-songs` reworked from a flat same-title / ISRC / URL **merge** list into one scored, sectioned review surface that absorbs the old `/manage/song-link-suggestions` page (#807/#808). Every candidate group now shows a **probability %** + the signals that fired (title · first lyric line · shared writer · shared **ISWC / CCLI / ISRC**), via a new shared scorer `includes/song_similarity.php` that also lights up the dormant `tblSongLinkSuggestions.Confidence` / `Signal` columns from #1066 (#1216). Candidates are clustered by union-find over exact-title, hard-identifier, shared-URL and fuzzy-suggestion edges, so a retitled arrangement that shares an ISWC now groups with its original. Three sections by book span: **Cross-book counterparts** (one-click **Link as counterparts** — bulk, writes `tblSongLinks`; #1219), **Same title in one official songbook** (almost always *different hymns* — merge guarded behind a type-to-confirm + server `force` flag; #1218), and **Same title in one collection** (probable import duplicate). Adds per-group **Dismiss** + **Rebuild**. The page is now curator-visible (`edit_songs`) for Link/Dismiss while the destructive **Merge** stays admin-gated (`manage_duplicate_songs`); `/manage/song-link-suggestions` 302-redirects to the unified page and the nav entry is relabelled "Duplicates & Links" (#1220). Deferred signal ideas tracked in #1221.
- feat(catalogue): **Works — composition grouping** (#840) — new top-level `tblWorks` entity that groups multiple `tblSongs` rows representing the same underlying composition across different songbooks / arrangements / translations. Mirrors the MusicBrainz Work ↔ Recording relationship. Optional ISWC, optional notes, unlimited parent / child nesting (cycle-prevention enforced server-side at update time). New `/work/<slug>` public page with categorised "Find this work elsewhere" panel, and a "Part of work" + sibling-version list on every song page that belongs to one. New `/manage/works` admin CRUD page gated on the `manage_works` entitlement.
- feat(admin): **URL → provider auto-detect** (#841) for the External-Links card-list editor. Paste a Wikipedia / YouTube / Spotify / etc. URL and the provider dropdown auto-selects the matching registry entry. Single global module (`js/modules/external-link-detect.js`) loaded on every `/manage/*` page; one-line `attachAutoDetect(rowEl)` wiring per consumer. 30+ providers covered, full unit-test coverage in `tests/test-external-link-detect.js`.
- refactor(admin): **URL-detect rules in MySQL** (#845) — replaces the JS-hard-coded `RULES` array with a curator-editable `tblExternalLinkPatterns` table. Each link type can carry many host / path patterns; sub-domain matching (suffix vs exact) and optional path-prefix discrimination are both supported. New `/manage/external-link-types` admin page (gated on `manage_external_link_types`). Pages that ship `_iHymnsLinkTypes` to JS attach each type's patterns via the shared `attachExternalLinkPatterns()` helper. JS falls back to the bundled `RULES` array on pre-migration deployments — no behaviour change at first install.
- refactor(admin): **responsive list-view convention** (#842) — `.admin-table-responsive` + `data-col-priority="primary|secondary|tertiary"` lets pages opt into a column-priority hide-on-narrow rule. Below 768 px secondary + tertiary columns hide; below 992 px tertiary only. Pages opted in this PR: Credit People (the original phone-clipping motivator), Songbooks, Songbook Series, Works. Convention documented in `DEV_NOTES.md`.
- feat(admin): **bulk-promote in-use Credit People into the register** (#846) — new `/manage/credit-people-bulk-promote` page lists every distinct name cited on a song-credit row that doesn't have a `tblCreditPeople` row yet, scores each against existing registry rows + sibling candidates with a Levenshtein + token-set blend, and lets the curator pick per-row Register / Merge-into-existing / Skip. The whole submit runs in one transaction with a shared `bulk_run_id` on the audit log so the run is reviewable as a unit. CTA on `/manage/credit-people` only renders when there's unregistered-but-cited work waiting.
- docs: **comprehensive refresh** (#843) — visitor in-app help adds an "External links & Works" section; admin in-app help adds Works, External Links and Mobile Admin sections; OpenAPI spec adds `Work` schema + `ExternalLink` shared schema + `/api?action=work&slug=…` endpoint; `DEV_NOTES.md` documents the external-links data shape, Works schema and the responsive convention.

- feat(songs): **cross-book counterparts** (#807) — new `tblSongLinks` join table + editor sidebar section + public song-page "Also appears in" dropdown + `api.php?action=song_links` endpoint. Lets curators declare that two-or-more `tblSongs` rows in different songbooks are the same hymn (e.g. Amazing Grace as MP-031 / CH-376 / SDAH-108 / SoF-29 / JP-006). Distinct from `tblSongTranslations` (different language) and `tblSongbooks.ParentSongbookId` (translated / edition derivatives at songbook level).
- feat(admin): **song link suggestions** (#808) — new `/manage/song-link-suggestions` page lists pre-computed similar-titled-song candidates with Link / Dismiss / Open-in-editor actions. Builder script at `tools/build-song-link-suggestions.php` computes pairwise title + first-line + author similarity, writing into `tblSongLinkSuggestions`. Inline 5-suggestion panel in the editor sidebar surfaces candidates for the open song.

## [0.25.1] — 2026-04-28
- Add Song of the Day with Christian calendar theming (#108)
- Add configurable page transition animations (#106)
- Add display preferences and presentation mode (#95)
- Add import/export for favourites and set lists (#103)
- Add keyboard number quick-jump from any page (#96)
- Add keyboard shortcuts help overlay (#104)
- Add lyrics full-text search with toggle (#93)
- Add missing song request form (#107)
- Add offline status indicator with cached song count (#112)
- Add print-optimised layout for set lists (#113)
- Add recently searched terms with quick re-search (#110)
- Add scroll-linked reading progress indicator on song pages (#109)
- Add side-by-side song comparison view (#102)
- Add smooth scroll-to-top button on long pages (#97)
- Add songbook colour-coding across the entire app (#98)
- Add songbook table of contents with alphabetical index (#111)
- Add transpose / capo indicator for song pages (#101)
- Add update-available notification for users (#83)
- Add worship set list / playlist feature (#94)
- Admin surface phases 1–4
- Admin surface: shared footer, editor back-links, final casing fixes
- Collapse iHymns menu to single "Manage" entry
- Compose App ID from shared base + platform suffix (#100)
- Favicon on every admin page + fix admin footer to viewport bottom
- Fix blank admin dashboard — align getCurrentUser column casing
- Fix code review issues: bugs, leaks, color fallbacks
- Fix requireEditor/Admin/GlobalAdmin regression from PR #417
- Fix security vulnerabilities: XSS, cache poisoning
- Fix: Audio/Sheet Music buttons show "coming soon" toast (#80)
- Fix: Favorites clear-all button event listener accumulation (#79)
- Fix: Service worker cache version auto-syncs with app version (#81)
- Gate: username + password sign-in (email service not yet wired up)
- Header dropdowns: cap height to viewport and scroll internally
- Header menu: entitlement-driven Curator + Admin sections
- Implement MIDI audio playback module (#90)
- Implement PDF sheet music viewer module (#91)
- Implement client-side fuzzy search with Fuse.js (#82)
- Implement recently viewed songs history (#92)
- Make deploys take effect immediately in the browser
- Match back-to-top button to app theme tokens
- Merge pull request #153 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Merge pull request #158 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Merge pull request #433 from MWBMPartners/claude/admin-surface-phases-1-4
- Merge pull request #477 from MWBMPartners/claude/songbook-grid-breakpoints
- Merge pull request #479 from MWBMPartners/claude/issue-462-licence-inheritance
- Merge pull request #483 from MWBMPartners/claude/issue-281-translations-ui
- Merge pull request #484 from MWBMPartners/claude/issue-285-missing-songs-ui
- Merge pull request #485 from MWBMPartners/claude/issue-289-notifications-ui
- Merge pull request #486 from MWBMPartners/claude/openapi-refresh
- Merge pull request #505 from MWBMPartners/alpha
- Merge pull request #513 from MWBMPartners/claude/fix-displayorder-migration
- Merge pull request #519 from MWBMPartners/claude/manage-schema-audit
- Merge pull request #576 from MWBMPartners/claude/copy-alpha-to-beta-cingh
- Precache recently viewed songs in service worker (#105)
- Prepare deep linking for native app, PWA, and browser (#99)
- Schema + migration + shared-setlist storage move to MySQL
- Settings sync: push on change, pull on auth-changed, opt-in toggle
- Settings: split into Account/Profile + App tabs, add profile editing
- Split header menus: account vs app/admin
- Strip access-control detail from channel gate + default-open bootstrap
- Temporarily disable channel gate while admin accounts are sorted
- Username rename (self-service + admin) + Profile-tab deep-link
- Whitelist toast notification type parameter
- app refresh upload (early dev 1)
- chore(admin): audit fixes — XSS, a11y, colspan
- chore(manage): consistency cleanup — \$activePage backfill, drop redundant requires, role-check helper (#516, #530)
- chore(misc): hygiene roll-ups — manifest icons split, dashboard cards aligned, app.js promise/init order (#530)
- chore: alphabetise manifest.json properties
- chore: full SQL data export, directory access safeguards
- chore: re-parse songs.json, user migration script, GitHub Wiki sync
- chore: rebuild songs.json from .SourceSongData with BCP 47 language support
- chore: reorder manifest.json — identity fields first, rest alphabetical
- docs(api): add OpenAPI 3.0 specification for all API endpoints (#291)
- docs(api): comprehensive OpenAPI audit — closes #291 (#575)
- docs(api): comprehensive OpenAPI refresh for the 0.10.x alpha batch
- docs(api): cover remaining 37 api.php actions + 5 editor endpoints
- docs(legal): update Privacy Policy and Terms of Use
- docs(openapi): refresh spec for 0.25.0 + document recent endpoints
- docs: refresh in-app help, README, CHANGELOG, DEV_NOTES, .claude memory
- docs: update README, in-app help, and wiki with recent features
- feat(a11y): colour vision deficiency mode selector and high contrast toggle (#319)
- feat(access): CCLI licence validation and tiered content access (#346)
- feat(activity-log): API-switch error catch-all + unknown-action row (#535 Phase 4)
- feat(activity-log): admin CRUD instrumentation (#535 Phase 2)
- feat(activity-log): expand tblActivityLog schema for #535 + fix typo writer
- feat(activity-log): instrument auth + session events (#535 Phase 1c)
- feat(activity-log): logActivity() helper + privacy policy doc (#535)
- feat(activity-log): retention prune + admin viewer UI (#535 Phase 5)
- feat(activity-log): user actions — favourites, setlists, song requests (#535 Phase 3)
- feat(admin): /manage/schema-audit page — diff schema.sql vs DB vs migrations (#518)
- feat(admin): CCLI usage report + CSV export (#317)
- feat(admin): analytics CSV export + search-query logging (#404)
- feat(admin): analytics dashboard at /manage/analytics (#404)
- feat(admin): backup upload path + audit log entry (#405)
- feat(admin): card-layout reorder/hide on /manage/ + username case + v0.25.0
- feat(admin): catalogue-wide missing-numbers report page (#285)
- feat(admin): pinned sidebar at lg+, hamburger below — closes #460
- feat(admin): regular-user content gating — restrictions, tiers, per-user AccessTier
- feat(admin): restore pre-flight summary + transactional data-load + pre-restore snapshot (#405)
- feat(admin): restore-from-backup action on the Setup dashboard (#405)
- feat(admin): runtime entitlements editor at /manage/entitlements (#407)
- feat(admin): song-request triage queue at /manage/requests (#403)
- feat(analytics): add GDPR-compliant consent banner and settings toggle
- feat(analytics): add Matomo and Fathom analytics platform support
- feat(analytics): add unified event tracking module
- feat(api): add 15 new API endpoints for all database features
- feat(auth): cross-subdomain cookie + sliding expiry so sign-in persists (#390)
- feat(auth): enforce registration_mode setting (#236)
- feat(auth): passwordless email login via magic link and 6-digit code (#294)
- feat(auth): promote magic-link as primary sign-in path (#395)
- feat(claude): in-repo session history via sync script + secret scrubber
- feat(community): song request submission form + API (#403)
- feat(db): CamelCase naming, user groups, song requests, missing song detection, interactive installer
- feat(db): migrate song data from JSON to MySQL (#270, #271, #272, #273, #274, #275, #276, #277, #278)
- feat(db): organisation support with licensing and content lockout (#326)
- feat(db): user-features catch-up migration for the 3 audit-discovered drift items
- feat(db): web form to configure db_credentials.php from Setup dashboard (#272)
- feat(debug): on-demand PHP error display via ?_debug=1&_dev=1
- feat(debug): wire ?_debug=1&_dev=1 into /manage/* admin pages
- feat(editor): ProPresenter-style arrangement builder + smarter presets
- feat(editor): Tags tab — per-song add/remove with live-search (#496)
- feat(editor): add arrangement customisation to Structure tab (#161)
- feat(editor): add checkboxes for verified, lyricsPublicDomain, musicPublicDomain (#222, #225)
- feat(editor): bulk tag / move / verify / export in multi-select bar (#399)
- feat(editor): clearance + layout + textarea fit + label + language names
- feat(editor): credit chip autocomplete — live dedupe across all 5 lists (#495)
- feat(editor): credit_search endpoint — live-search distinct credits (#495)
- feat(editor): debounced auto-save as safety net (#394)
- feat(editor): find-missing-numbers modal in the sidebar (#285)
- feat(editor): multi-select mode + bulk delete (#399 MVP)
- feat(editor): per-song save endpoint + client wiring (closes #394 core)
- feat(editor): tune name, ISWC, arrangers/adaptors/translators (#497)
- feat(editor+admin): revision history UI + diff + restore + /manage/revisions (#400)
- feat(favorites): add batch select mode with bulk operations (#119)
- feat(favorites): add custom tags/categories with filter (#122)
- feat(footer): add 'By Number' button and narrow device adjustments
- feat(header): replace logo with navigation dropdown menu
- feat(home): add quick-access recent songbook tabs (#121)
- feat(home): responsive songbook grid — scale columns with viewport
- feat(infra): database backup script with retention policy (#322)
- feat(licences): inheritance + multi-licence effective set (#462)
- feat(manage): credit-people actions — Add / Edit / Rename / Merge / Delete / View Songs (closes #545) (#563)
- feat(manage): credit-people read-only list page + entitlement (#545 next slice) (#560)
- feat(manage): in-app Help / Guides page for admin surface (#574)
- feat(manage): name-first Entity/Target pickers on /restrictions (#498)
- feat(misc): let Misc songbook hold unnumbered songs (#392)
- feat(notifications): in-app bell + dropdown + mark-read API (#289)
- feat(offline+perf): cloud-download buttons, fix SW stall, ETag pages, N+1 fix
- feat(og): add OG preview images for songbooks, setlists, and Android Smart App Banner
- feat(portal): channel gating + /admin alias + richer admin dashboard (#407)
- feat(pwa): Background Sync for favourites + setlists (#338)
- feat(pwa): bulk audio manifest endpoint + Settings toggle (#401)
- feat(pwa): cache audio + sheet music on-demand for offline playback (#401)
- feat(pwa): generate app icons and screenshot placeholders
- feat(pwa): offline song-request queuing (#337)
- feat(pwa): platform-specific install prompts for all iOS browsers (#175)
- feat(pwa): restore "New version — Refresh" toast on SW update (#396)
- feat(pwa): wire audio pre-cache + per-songbook eviction + real size readout (#401)
- feat(rate-limit): per-user buckets + apply to setlist_collab_invite (#321)
- feat(related-songs): add content-based TF-IDF lyric similarity (#118)
- feat(roles+ux): entitlements map, Edit-Song button, clearer verse demarcation (#407)
- feat(schema): credit-people registry tables for #545 (data layer only) (#553)
- feat(search): expand common scripture-reference abbreviations (#397)
- feat(search): scripture-tag-aware search merges curated tag matches (#397)
- feat(security): add Content-Security-Policy header with nonces (#117)
- feat(security): add SRI hashes to CDN resources (#116)
- feat(seo): centre-safe OG layout + contextual song previews (#172, #173)
- feat(setlist): scheduled setlists API endpoints (#398 half)
- feat(setlist): scheduling UI wired + collaboration endpoints + 'Up next' card (#398)
- feat(settings): add Privacy section with analytics consent toggle
- feat(setup): add Drop Legacy Tables action to the Setup dashboard
- feat(share): enhance social share cards with rich metadata (#123)
- feat(shortcuts): add keyboard shortcuts for worship control (#125)
- feat(shortcuts): settings toggle to enable/disable keyboard shortcuts (#406)
- feat(song page): surface tune name, ISWC + arranger/adaptor/translator credits
- feat(song): add related songs section on song pages (#118)
- feat(song): practice / memorisation mode with progressive masking (#402)
- feat(songbooks): Official flag + Publisher/Year/Copyright/Affiliation (#502)
- feat(stats): add usage statistics dashboard page (#120)
- feat(transitions): enhance page transitions with loading bar and staggered animations (#149)
- feat(ui): full-width layout + admin header redesigned to match main site
- feat(ui): multi-language translations switcher on song page (#281)
- feat(ui): popular songs, recently viewed, browse by tag on home page (#303, #304, #305)
- feat(ui): search autocomplete suggestions (#307)
- feat(ui): setlist scheduling, templates, PDF export buttons (#300, #301, #302)
- feat(ui): song request submission form (#280)
- feat: Android — Kotlin/Jetpack Compose project setup
- feat: Apple universal app — Xcode project setup (Swift 6.3 / SwiftUI)
- feat: Apple — Widgets (Song of the Day + Recent Favourites)
- feat: BCP 47 language support, email login UI, and magic link routing (#237, #295, #296)
- feat: Phase 1 batch — copyright fix, gestures, permalinks, writer links, SEO, setlists, transitions, title case, songbook sort, colour mode fix
- feat: Song Editor — edit metadata (title, number, songbook, CCLI)
- feat: Song Editor — web-based developer tool UI
- feat: WCAG 2.1 AA accessibility compliance
- feat: Web PWA — MIDI audio playback
- feat: Web PWA — PDF sheet music viewer
- feat: add Miscellaneous songbook (Misc) for non-published songs
- feat: add SQLite database for song storage
- feat: add branded offline fallback page for uncached PWA launches
- feat: add experimental manifest properties + reorder
- feat: add language field (IETF BCP 47) + Add/Delete song buttons
- feat: add verified/publicDomain fields, verified badge, JSON schema (#222-#226)
- feat: admin dashboard & enhanced user management (#260)
- feat: align colour scheme with iLyrics dB + colourblind-friendly mode
- feat: automated Apple app packaging (App Store, TestFlight, direct)
- feat: automated Web PWA build & packaging
- feat: badge contrast fix, song arrangements, SEO enhancements (#159, #160, #151)
- feat: build Web PWA core — Milestone 2 complete
- feat: build song data parser and generate songs.json
- feat: bulk download API reduces 3,612 requests to 6 for offline songs
- feat: centralise song data in data_share/ and add server-side setlist sharing (#154, #155)
- feat: complete PWA rebuild with PHP backend, modern UI, and enhanced features
- feat: complete colour scheme redesign — clean neutral slate
- feat: comprehensive PWA manifest + update Claude context
- feat: comprehensive in-app help system
- feat: deep linking with clean URLs and browser title updates
- feat: disable numpad live search by default, respect default songbook
- feat: download all songs for offline use
- feat: editor song list — title case + songbook badge (#249)
- feat: email login disable, CAPTCHA config, ad platform settings (#339, #340, #341)
- feat: enhanced user account system with role hierarchy, password reset, custom arrangements
- feat: expand Song of the Day seasons and search lyrics (#163)
- feat: fixed header and footer — always visible on screen
- feat: hide PWA install banner when already installed cross-subdomain (#248)
- feat: lyrics copy protection and expanded font size range
- feat: move editor to /manage/ with session-based auth (#227, #228, #229)
- feat: numeric keypad toggle for song number search
- feat: offline song update detection and auto-update (#131, #132)
- feat: per-songbook and per-song offline download options
- feat: physical keyboard support for number search keypad
- feat: project setup — new structure, plan, docs, and song data
- feat: record song views for history tracking (#287)
- feat: redirect to app root on direct include file access
- feat: related songs, export, health dashboard, review workflow, accessibility, rate limit, cleanup (#308-#325)
- feat: replace MWBM Partners Ltd branding with iHymns + cross-domain storage bridge (#133)
- feat: replace native prompt()/confirm() with Bootstrap modals (#114)
- feat: restrict access to private_html/ via HTTP Basic Auth
- feat: shared data/ directory uploaded one level up from SFTP paths
- feat: show full songbook name responsively, abbreviation on narrow screens
- feat: song keys, chords, setlist scheduling/templates/PDF/collab, discovery, preferences, revisions (#298-#316)
- feat: song translation linking, fix auto-scroll iOS, fix PWA offline blank page
- feat: sortable song list — title (default), number, songbook (#251)
- feat: split language into Language, Script, Region fields (#240)
- feat: subdomain cookie sync for *.ihymns.app (#133)
- feat: subtle JSON fallback indicator in footer (Alpha/Beta only)
- feat: unit tests + fix songbook abbreviation badges
- feat: update footer links and enhance copyright display logic
- feat: web-accessible database setup dashboard at /manage/setup-database.php
- fix(admin): Bootstrap JS everywhere, drag-drop, offline buttons, friendly labels
- fix(admin): drop stale DB_CONFIG reference — fatal killed footer
- fix(admin): full-height sidebar column (#499)
- fix(admin): generic clean-URL rewrite so every /manage/<name>.php works
- fix(admin): make pinned footer visible — solid surface, stronger shadow
- fix(admin): pin header + footer to viewport like the main site
- fix(admin-nav): account button overflowed header, hid theme + hamburger
- fix(admin-nav): keep long display names on one line in header
- fix(admin/schema-audit): skip ON DELETE/UPDATE + FULLTEXT lines in parser
- fix(api): document CSRF defence policy + add X-Requested-With guard (#293 B15)
- fix(api): remove dead song_translations duplicate + sync OpenAPI contract (#291)
- fix(api): restore licence cases + document missing endpoints (#291)
- fix(auth): Settings Account card now reflects signed-in state consistently
- fix(auth): idle timeout + session_regenerate_id on privilege change (#531)
- fix(badges): automated WCAG contrast for songbook number badges
- fix(config): hard-coded values → APP_CONFIG (#529)
- fix(db): catch up missing tblSongbooks.Colour in songbook-meta migration
- fix(db): defensive AFTER-clause helper in songbook-meta migration
- fix(db): relax SQL strict mode in user-features-catchup migration
- fix(db): use MySQL 8.0+ expression-default syntax for SongsJson
- fix(db/audit): backfill @migration-adds doctags on credit-fields migration
- fix(editor): Save writes only modified songs, not whole corpus
- fix(editor): make Add/Delete song buttons more visible (#238)
- fix(editor): rename 'Save JSON' to 'Save' and clarify DB-first save path
- fix(editor): resolve toolbar button ID mismatches (#235)
- fix(home): dedupe Popular Songs and Recently Viewed lists (#549) (#550)
- fix(home,editor): show titles on home + tighten editor toolbar on mobile (#546, #547) (#548)
- fix(install): skip interactive prompts when run non-interactively
- fix(js): JS↔PHP contract sync — entitlements + localStorage centralisation (#528)
- fix(logs): error_log on silent best-effort catches (#534)
- fix(manage): load config.php in admin bootstrap so head-libs.php sees APP_CONFIG (#542)
- fix(manage): load entitlements.php in auth bootstrap
- fix(migrate-users): bind_param type/variable mismatch crashed user insert
- fix(migrations): strict mysqli reporting + prepared statement consistency (#525)
- fix(php): drop literal `<?= ... ?>` from HTML comment to stop runtime fatal
- fix(php): inline-script + non-HTML endpoint hardening (#526)
- fix(pwa): IDB private-mode safe + storage quota awareness helper (#354)
- fix(pwa): Safari install banner blank + iOS safe-area support (#174)
- fix(pwa): hide install banner on non-Safari iOS browsers
- fix(pwa): offline boot reliability — vendor population, SW first-install, songs_json precache (#354)
- fix(pwa): replace navigator.onLine with fetch-failure detection (#354 B24)
- fix(pwa): share icon direction + empty default banner HTML (#177)
- fix(pwa): silence manifest warnings + allow CDN sourcemap fetches
- fix(schema): MySQL 8.0+ DEFAULT-parens on every TEXT/MEDIUMTEXT (#525)
- fix(security): comprehensive security audit fixes (#293)
- fix(security): hash API tokens and reset tokens with SHA-256 (#292)
- fix(seo): add dynamic OG image for social sharing previews (#170)
- fix(setlist): remove text from Web Share API to prevent broken share links
- fix(settings): disable reduce motion by default so animations are enabled
- fix(setup): don't leak text/plain Content-Type through the Setup dashboard
- fix(sheet-music): add loading guard to prevent rapid-click race condition (#115)
- fix(songdata): bulk-load credits in search paths to kill N+1 (#533)
- fix(spa): run inline <script> tags in injected page templates
- fix(spa+admin): home sections via module; drop Bootstrap double-load
- fix(stats): equalise Most Viewed badge widths regardless of digit count
- fix(sw): prevent caching 301 redirects in navigation handler (#140)
- fix(sw): skip non-GET requests in fetch handler — Cache API rejects them (#544)
- fix(ui): arrangement badge contrast, tooltips (#266)
- fix(writer): improve name matching with multiple slug variants
- fix: PHP/MySQL safety audit Tier 1 + 2 fixes (closes #556, #557, #558) (#559)
- fix: PWA banner — App Store verification, remove broken Smart App Banner (#267)
- fix: PWA bugs — sync bar, badge contrast, chorus/refrain, iOS auto-scroll (#262, #263, #265, #269)
- fix: PWA standalone safe area insets for header, footer, overlays (#267)
- fix: Popular Songs and Browse by Theme work in JSON fallback and offline
- fix: SDAH song number badges unreadable in dark mode (#134)
- fix: Song of the Day not rendering due to async data race (#108)
- fix: SongData.php falls back to JSON when MySQL not configured
- fix: add .htaccess rules for /manage/ and use clean URLs (#227)
- fix: align offline songbook list and show estimated storage sizes
- fix: arrangement badge contrast, tooltips, and compact layout
- fix: auto-scroll iOS support and floating stop button (#267)
- fix: auto-scroll not working on iOS due to sub-pixel rounding
- fix: call skipWaiting() so new SW activates immediately
- fix: clean up Web PWA UI — search bar, icons, songbook colours
- fix: code review and security hardening
- fix: comprehensive code review + security hardening
- fix: correct SQLite database path — dirname 3 not 2 (#253)
- fix: critical editor bugs — form visibility, lyrics/lines, missing fields
- fix: deduplicate concurrent navigation fetches in service worker (#140)
- fix: deploy sync detection + softer songbook card colours
- fix: disable all caching on beta — nuke service worker + cache headers
- fix: editor HTML-to-JS ID mismatches + admin-only user management
- fix: enable PWA offline support on iOS (and all platforms)
- fix: expand public domain detection to include all variant spellings (#225)
- fix: footer text center alignment and bottom safe area on mobile (#267)
- fix: hide .php from all URLs, JSON fallback for SongData
- fix: include data/songs.json inside each SFTP upload directory
- fix: merge duplicate class attribute on search-clear-btn
- fix: migration scripts work via web dashboard (no CLI required)
- fix: offline download continues in background and downloads 4x faster
- fix: prefix copyright notice with "Application" to distinguish from song copyrights (#230)
- fix: preserve SQLite directory and protect DB from git/web access
- fix: put songbook abbreviation inside coloured square in Recent section (#162)
- fix: re-enable service worker + add Cloudflare no-cache headers
- fix: remove CSS background override on songbook card headers
- fix: remove config.php include from service worker to prevent PHP errors
- fix: render song titles in Title Case consistently across all surfaces
- fix: revert version-bump to beta-only, show build timestamp on alpha
- fix: scroll progress bar visibility and safe areas in presentation mode
- fix: scroll-to-top button overlapping footer on all platforms
- fix: separate copyright from writer credits, show in header and footer
- fix: service worker never caches CDN resources — fixes 503 errors
- fix: set Vendor Parent to NULL — MWBM Partners Ltd is the top-level vendor
- fix: setlist sync bar, badge text contrast, admin menu links
- fix: show song numbers in Recent section on home page (#162)
- fix: song editor loads songs.json from canonical data/ location
- fix: song list spacing, version bump regex, pre-release version, lyrics resilience
- fix: split copyright at © in all credit line handlers, zero issues remaining
- fix: temporarily remove CSP header to clear poisoned SW cache
- fix: widen song number badge min-width for consistent alignment
- initial folder structure
- perf(editor): batch writers/composers/components — 10,800 → 3 queries
- perf(editor): include tags in bulk ?action=load (#496 follow-up)
- refactor(api): land 5c+5d delta missed by stacked-PR base mishap (#570, #571) (#572)
- refactor(api): migrate 14 endpoints from PDO to mysqli (#554 Batch 5b) (#569)
- refactor(api): migrate 7 endpoints from PDO to mysqli (#554 Batch 5a) (#567)
- refactor(db): finish PDO→mysqli migration — closes #554, #555, #568 (#573)
- refactor(includes): PDO→mysqli — #554 Batch 4 part 1 of 2 (9 /includes/* libraries) (#565)
- refactor(manage): PDO→mysqli migration — #554 Batch 1 (tiers, groups, restrictions, requests) (#561)
- refactor(manage): PDO→mysqli migration — #554 Batch 2 (users, songbooks, organisations) (#562)
- refactor(manage): PDO→mysqli — #554 Batch 3 (analytics, activity-log, ccli-report, revisions, schema-audit, index) (#564)
- refactor(manage): consolidate Bootstrap library loads into head-libs.php (#527)
- refactor(manage): extract renderEntityPicker helper for reuse (#498)
- refactor(manage): migrate auth.php from PDO to mysqli (#554 Batch 4 part 2) (#566)
- refactor: align infoAppVer.php with phpWhoIs structure + platform info files
- refactor: centralise localStorage key names in constants.js (#139)
- refactor: consolidate web app to single appWeb/public_html/ directory
- refactor: extract shared escapeHtml() into js/utils/html.js (#137)
- refactor: modularise Web/PWA into PHP components
- refactor: single canonical songs.json — copy during build/deploy
- refactor: unify Chorus/Refrain — Chorus as primary, Refrain as alias
- refactor: update PHP code for PHP 8.5 compatibility
- refactor: use DIRECTORY_SEPARATOR in all PHP require/include paths
- refactor: use DIRECTORY_SEPARATOR in all path string segments
- refactor: use PHP constants, remove shortcut vars, update wiki + OpenAPI
- refactor: use unique platform-specific Application IDs
- security(favorites): replace innerHTML with DOM APIs (#504)
- security: add CSRF checks + reduce error info-leak on admin pages
- style(admin): unify song editor + /manage pages with main-site palette
- style(credits): canonical '; ' separator for multi-credit join strings

## [0.25.0] — 2026-04-24
- Add Song of the Day with Christian calendar theming (#108)
- Add configurable page transition animations (#106)
- Add display preferences and presentation mode (#95)
- Add import/export for favourites and set lists (#103)
- Add keyboard number quick-jump from any page (#96)
- Add keyboard shortcuts help overlay (#104)
- Add lyrics full-text search with toggle (#93)
- Add missing song request form (#107)
- Add offline status indicator with cached song count (#112)
- Add print-optimised layout for set lists (#113)
- Add recently searched terms with quick re-search (#110)
- Add scroll-linked reading progress indicator on song pages (#109)
- Add side-by-side song comparison view (#102)
- Add smooth scroll-to-top button on long pages (#97)
- Add songbook colour-coding across the entire app (#98)
- Add songbook table of contents with alphabetical index (#111)
- Add transpose / capo indicator for song pages (#101)
- Add update-available notification for users (#83)
- Add worship set list / playlist feature (#94)
- Admin surface phases 1–4
- Admin surface: shared footer, editor back-links, final casing fixes
- Collapse iHymns menu to single "Manage" entry
- Compose App ID from shared base + platform suffix (#100)
- Favicon on every admin page + fix admin footer to viewport bottom
- Fix blank admin dashboard — align getCurrentUser column casing
- Fix code review issues: bugs, leaks, color fallbacks
- Fix requireEditor/Admin/GlobalAdmin regression from PR #417
- Fix security vulnerabilities: XSS, cache poisoning
- Fix: Audio/Sheet Music buttons show "coming soon" toast (#80)
- Fix: Favorites clear-all button event listener accumulation (#79)
- Fix: Service worker cache version auto-syncs with app version (#81)
- Gate: username + password sign-in (email service not yet wired up)
- Header dropdowns: cap height to viewport and scroll internally
- Header menu: entitlement-driven Curator + Admin sections
- Implement MIDI audio playback module (#90)
- Implement PDF sheet music viewer module (#91)
- Implement client-side fuzzy search with Fuse.js (#82)
- Implement recently viewed songs history (#92)
- Make deploys take effect immediately in the browser
- Match back-to-top button to app theme tokens
- Merge pull request #153 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Merge pull request #158 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Merge pull request #433 from MWBMPartners/claude/admin-surface-phases-1-4
- Merge pull request #477 from MWBMPartners/claude/songbook-grid-breakpoints
- Merge pull request #479 from MWBMPartners/claude/issue-462-licence-inheritance
- Merge pull request #483 from MWBMPartners/claude/issue-281-translations-ui
- Merge pull request #484 from MWBMPartners/claude/issue-285-missing-songs-ui
- Merge pull request #485 from MWBMPartners/claude/issue-289-notifications-ui
- Merge pull request #486 from MWBMPartners/claude/openapi-refresh
- Merge pull request #505 from MWBMPartners/alpha
- Precache recently viewed songs in service worker (#105)
- Prepare deep linking for native app, PWA, and browser (#99)
- Schema + migration + shared-setlist storage move to MySQL
- Settings sync: push on change, pull on auth-changed, opt-in toggle
- Settings: split into Account/Profile + App tabs, add profile editing
- Split header menus: account vs app/admin
- Strip access-control detail from channel gate + default-open bootstrap
- Temporarily disable channel gate while admin accounts are sorted
- Username rename (self-service + admin) + Profile-tab deep-link
- Whitelist toast notification type parameter
- app refresh upload (early dev 1)
- chore(admin): audit fixes — XSS, a11y, colspan
- chore: alphabetise manifest.json properties
- chore: full SQL data export, directory access safeguards
- chore: re-parse songs.json, user migration script, GitHub Wiki sync
- chore: rebuild songs.json from .SourceSongData with BCP 47 language support
- chore: reorder manifest.json — identity fields first, rest alphabetical
- docs(api): add OpenAPI 3.0 specification for all API endpoints (#291)
- docs(api): comprehensive OpenAPI refresh for the 0.10.x alpha batch
- docs(api): cover remaining 37 api.php actions + 5 editor endpoints
- docs(legal): update Privacy Policy and Terms of Use
- docs(openapi): refresh spec for 0.25.0 + document recent endpoints
- docs: refresh in-app help, README, CHANGELOG, DEV_NOTES, .claude memory
- docs: update README, in-app help, and wiki with recent features
- feat(a11y): colour vision deficiency mode selector and high contrast toggle (#319)
- feat(access): CCLI licence validation and tiered content access (#346)
- feat(admin): CCLI usage report + CSV export (#317)
- feat(admin): analytics CSV export + search-query logging (#404)
- feat(admin): analytics dashboard at /manage/analytics (#404)
- feat(admin): backup upload path + audit log entry (#405)
- feat(admin): card-layout reorder/hide on /manage/ + username case + v0.25.0
- feat(admin): catalogue-wide missing-numbers report page (#285)
- feat(admin): pinned sidebar at lg+, hamburger below — closes #460
- feat(admin): regular-user content gating — restrictions, tiers, per-user AccessTier
- feat(admin): restore pre-flight summary + transactional data-load + pre-restore snapshot (#405)
- feat(admin): restore-from-backup action on the Setup dashboard (#405)
- feat(admin): runtime entitlements editor at /manage/entitlements (#407)
- feat(admin): song-request triage queue at /manage/requests (#403)
- feat(analytics): add GDPR-compliant consent banner and settings toggle
- feat(analytics): add Matomo and Fathom analytics platform support
- feat(analytics): add unified event tracking module
- feat(api): add 15 new API endpoints for all database features
- feat(auth): cross-subdomain cookie + sliding expiry so sign-in persists (#390)
- feat(auth): enforce registration_mode setting (#236)
- feat(auth): passwordless email login via magic link and 6-digit code (#294)
- feat(auth): promote magic-link as primary sign-in path (#395)
- feat(claude): in-repo session history via sync script + secret scrubber
- feat(community): song request submission form + API (#403)
- feat(db): CamelCase naming, user groups, song requests, missing song detection, interactive installer
- feat(db): migrate song data from JSON to MySQL (#270, #271, #272, #273, #274, #275, #276, #277, #278)
- feat(db): organisation support with licensing and content lockout (#326)
- feat(db): web form to configure db_credentials.php from Setup dashboard (#272)
- feat(editor): add arrangement customisation to Structure tab (#161)
- feat(editor): add checkboxes for verified, lyricsPublicDomain, musicPublicDomain (#222, #225)
- feat(editor): bulk tag / move / verify / export in multi-select bar (#399)
- feat(editor): debounced auto-save as safety net (#394)
- feat(editor): find-missing-numbers modal in the sidebar (#285)
- feat(editor): multi-select mode + bulk delete (#399 MVP)
- feat(editor): per-song save endpoint + client wiring (closes #394 core)
- feat(editor+admin): revision history UI + diff + restore + /manage/revisions (#400)
- feat(favorites): add batch select mode with bulk operations (#119)
- feat(favorites): add custom tags/categories with filter (#122)
- feat(footer): add 'By Number' button and narrow device adjustments
- feat(header): replace logo with navigation dropdown menu
- feat(home): add quick-access recent songbook tabs (#121)
- feat(home): responsive songbook grid — scale columns with viewport
- feat(infra): database backup script with retention policy (#322)
- feat(licences): inheritance + multi-licence effective set (#462)
- feat(misc): let Misc songbook hold unnumbered songs (#392)
- feat(notifications): in-app bell + dropdown + mark-read API (#289)
- feat(offline+perf): cloud-download buttons, fix SW stall, ETag pages, N+1 fix
- feat(og): add OG preview images for songbooks, setlists, and Android Smart App Banner
- feat(portal): channel gating + /admin alias + richer admin dashboard (#407)
- feat(pwa): Background Sync for favourites + setlists (#338)
- feat(pwa): bulk audio manifest endpoint + Settings toggle (#401)
- feat(pwa): cache audio + sheet music on-demand for offline playback (#401)
- feat(pwa): generate app icons and screenshot placeholders
- feat(pwa): offline song-request queuing (#337)
- feat(pwa): platform-specific install prompts for all iOS browsers (#175)
- feat(pwa): restore "New version — Refresh" toast on SW update (#396)
- feat(pwa): wire audio pre-cache + per-songbook eviction + real size readout (#401)
- feat(related-songs): add content-based TF-IDF lyric similarity (#118)
- feat(roles+ux): entitlements map, Edit-Song button, clearer verse demarcation (#407)
- feat(search): expand common scripture-reference abbreviations (#397)
- feat(search): scripture-tag-aware search merges curated tag matches (#397)
- feat(security): add Content-Security-Policy header with nonces (#117)
- feat(security): add SRI hashes to CDN resources (#116)
- feat(seo): centre-safe OG layout + contextual song previews (#172, #173)
- feat(setlist): scheduled setlists API endpoints (#398 half)
- feat(setlist): scheduling UI wired + collaboration endpoints + 'Up next' card (#398)
- feat(settings): add Privacy section with analytics consent toggle
- feat(setup): add Drop Legacy Tables action to the Setup dashboard
- feat(share): enhance social share cards with rich metadata (#123)
- feat(shortcuts): add keyboard shortcuts for worship control (#125)
- feat(shortcuts): settings toggle to enable/disable keyboard shortcuts (#406)
- feat(song): add related songs section on song pages (#118)
- feat(song): practice / memorisation mode with progressive masking (#402)
- feat(stats): add usage statistics dashboard page (#120)
- feat(transitions): enhance page transitions with loading bar and staggered animations (#149)
- feat(ui): full-width layout + admin header redesigned to match main site
- feat(ui): multi-language translations switcher on song page (#281)
- feat(ui): popular songs, recently viewed, browse by tag on home page (#303, #304, #305)
- feat(ui): search autocomplete suggestions (#307)
- feat(ui): setlist scheduling, templates, PDF export buttons (#300, #301, #302)
- feat(ui): song request submission form (#280)
- feat: Android — Kotlin/Jetpack Compose project setup
- feat: Apple universal app — Xcode project setup (Swift 6.3 / SwiftUI)
- feat: Apple — Widgets (Song of the Day + Recent Favourites)
- feat: BCP 47 language support, email login UI, and magic link routing (#237, #295, #296)
- feat: Phase 1 batch — copyright fix, gestures, permalinks, writer links, SEO, setlists, transitions, title case, songbook sort, colour mode fix
- feat: Song Editor — edit metadata (title, number, songbook, CCLI)
- feat: Song Editor — web-based developer tool UI
- feat: WCAG 2.1 AA accessibility compliance
- feat: Web PWA — MIDI audio playback
- feat: Web PWA — PDF sheet music viewer
- feat: add Miscellaneous songbook (Misc) for non-published songs
- feat: add SQLite database for song storage
- feat: add branded offline fallback page for uncached PWA launches
- feat: add experimental manifest properties + reorder
- feat: add language field (IETF BCP 47) + Add/Delete song buttons
- feat: add verified/publicDomain fields, verified badge, JSON schema (#222-#226)
- feat: admin dashboard & enhanced user management (#260)
- feat: align colour scheme with iLyrics dB + colourblind-friendly mode
- feat: automated Apple app packaging (App Store, TestFlight, direct)
- feat: automated Web PWA build & packaging
- feat: badge contrast fix, song arrangements, SEO enhancements (#159, #160, #151)
- feat: build Web PWA core — Milestone 2 complete
- feat: build song data parser and generate songs.json
- feat: bulk download API reduces 3,612 requests to 6 for offline songs
- feat: centralise song data in data_share/ and add server-side setlist sharing (#154, #155)
- feat: complete PWA rebuild with PHP backend, modern UI, and enhanced features
- feat: complete colour scheme redesign — clean neutral slate
- feat: comprehensive PWA manifest + update Claude context
- feat: comprehensive in-app help system
- feat: deep linking with clean URLs and browser title updates
- feat: disable numpad live search by default, respect default songbook
- feat: download all songs for offline use
- feat: editor song list — title case + songbook badge (#249)
- feat: email login disable, CAPTCHA config, ad platform settings (#339, #340, #341)
- feat: enhanced user account system with role hierarchy, password reset, custom arrangements
- feat: expand Song of the Day seasons and search lyrics (#163)
- feat: fixed header and footer — always visible on screen
- feat: hide PWA install banner when already installed cross-subdomain (#248)
- feat: lyrics copy protection and expanded font size range
- feat: move editor to /manage/ with session-based auth (#227, #228, #229)
- feat: numeric keypad toggle for song number search
- feat: offline song update detection and auto-update (#131, #132)
- feat: per-songbook and per-song offline download options
- feat: physical keyboard support for number search keypad
- feat: project setup — new structure, plan, docs, and song data
- feat: record song views for history tracking (#287)
- feat: redirect to app root on direct include file access
- feat: related songs, export, health dashboard, review workflow, accessibility, rate limit, cleanup (#308-#325)
- feat: replace MWBM Partners Ltd branding with iHymns + cross-domain storage bridge (#133)
- feat: replace native prompt()/confirm() with Bootstrap modals (#114)
- feat: restrict access to private_html/ via HTTP Basic Auth
- feat: shared data/ directory uploaded one level up from SFTP paths
- feat: show full songbook name responsively, abbreviation on narrow screens
- feat: song keys, chords, setlist scheduling/templates/PDF/collab, discovery, preferences, revisions (#298-#316)
- feat: song translation linking, fix auto-scroll iOS, fix PWA offline blank page
- feat: sortable song list — title (default), number, songbook (#251)
- feat: split language into Language, Script, Region fields (#240)
- feat: subdomain cookie sync for *.ihymns.app (#133)
- feat: subtle JSON fallback indicator in footer (Alpha/Beta only)
- feat: unit tests + fix songbook abbreviation badges
- feat: update footer links and enhance copyright display logic
- feat: web-accessible database setup dashboard at /manage/setup-database.php
- fix(admin): Bootstrap JS everywhere, drag-drop, offline buttons, friendly labels
- fix(admin): generic clean-URL rewrite so every /manage/<name>.php works
- fix(admin): make pinned footer visible — solid surface, stronger shadow
- fix(admin): pin header + footer to viewport like the main site
- fix(admin-nav): account button overflowed header, hid theme + hamburger
- fix(admin-nav): keep long display names on one line in header
- fix(auth): Settings Account card now reflects signed-in state consistently
- fix(badges): automated WCAG contrast for songbook number badges
- fix(editor): make Add/Delete song buttons more visible (#238)
- fix(editor): rename 'Save JSON' to 'Save' and clarify DB-first save path
- fix(editor): resolve toolbar button ID mismatches (#235)
- fix(install): skip interactive prompts when run non-interactively
- fix(migrate-users): bind_param type/variable mismatch crashed user insert
- fix(pwa): Safari install banner blank + iOS safe-area support (#174)
- fix(pwa): hide install banner on non-Safari iOS browsers
- fix(pwa): share icon direction + empty default banner HTML (#177)
- fix(security): comprehensive security audit fixes (#293)
- fix(security): hash API tokens and reset tokens with SHA-256 (#292)
- fix(seo): add dynamic OG image for social sharing previews (#170)
- fix(setlist): remove text from Web Share API to prevent broken share links
- fix(settings): disable reduce motion by default so animations are enabled
- fix(setup): don't leak text/plain Content-Type through the Setup dashboard
- fix(sheet-music): add loading guard to prevent rapid-click race condition (#115)
- fix(spa): run inline <script> tags in injected page templates
- fix(stats): equalise Most Viewed badge widths regardless of digit count
- fix(sw): prevent caching 301 redirects in navigation handler (#140)
- fix(ui): arrangement badge contrast, tooltips (#266)
- fix(writer): improve name matching with multiple slug variants
- fix: PWA banner — App Store verification, remove broken Smart App Banner (#267)
- fix: PWA bugs — sync bar, badge contrast, chorus/refrain, iOS auto-scroll (#262, #263, #265, #269)
- fix: PWA standalone safe area insets for header, footer, overlays (#267)
- fix: Popular Songs and Browse by Theme work in JSON fallback and offline
- fix: SDAH song number badges unreadable in dark mode (#134)
- fix: Song of the Day not rendering due to async data race (#108)
- fix: SongData.php falls back to JSON when MySQL not configured
- fix: add .htaccess rules for /manage/ and use clean URLs (#227)
- fix: align offline songbook list and show estimated storage sizes
- fix: arrangement badge contrast, tooltips, and compact layout
- fix: auto-scroll iOS support and floating stop button (#267)
- fix: auto-scroll not working on iOS due to sub-pixel rounding
- fix: call skipWaiting() so new SW activates immediately
- fix: clean up Web PWA UI — search bar, icons, songbook colours
- fix: code review and security hardening
- fix: comprehensive code review + security hardening
- fix: correct SQLite database path — dirname 3 not 2 (#253)
- fix: critical editor bugs — form visibility, lyrics/lines, missing fields
- fix: deduplicate concurrent navigation fetches in service worker (#140)
- fix: deploy sync detection + softer songbook card colours
- fix: disable all caching on beta — nuke service worker + cache headers
- fix: editor HTML-to-JS ID mismatches + admin-only user management
- fix: enable PWA offline support on iOS (and all platforms)
- fix: expand public domain detection to include all variant spellings (#225)
- fix: footer text center alignment and bottom safe area on mobile (#267)
- fix: hide .php from all URLs, JSON fallback for SongData
- fix: include data/songs.json inside each SFTP upload directory
- fix: merge duplicate class attribute on search-clear-btn
- fix: migration scripts work via web dashboard (no CLI required)
- fix: offline download continues in background and downloads 4x faster
- fix: prefix copyright notice with "Application" to distinguish from song copyrights (#230)
- fix: preserve SQLite directory and protect DB from git/web access
- fix: put songbook abbreviation inside coloured square in Recent section (#162)
- fix: re-enable service worker + add Cloudflare no-cache headers
- fix: remove CSS background override on songbook card headers
- fix: remove config.php include from service worker to prevent PHP errors
- fix: render song titles in Title Case consistently across all surfaces
- fix: revert version-bump to beta-only, show build timestamp on alpha
- fix: scroll progress bar visibility and safe areas in presentation mode
- fix: scroll-to-top button overlapping footer on all platforms
- fix: separate copyright from writer credits, show in header and footer
- fix: service worker never caches CDN resources — fixes 503 errors
- fix: set Vendor Parent to NULL — MWBM Partners Ltd is the top-level vendor
- fix: setlist sync bar, badge text contrast, admin menu links
- fix: show song numbers in Recent section on home page (#162)
- fix: song editor loads songs.json from canonical data/ location
- fix: song list spacing, version bump regex, pre-release version, lyrics resilience
- fix: split copyright at © in all credit line handlers, zero issues remaining
- fix: temporarily remove CSP header to clear poisoned SW cache
- fix: widen song number badge min-width for consistent alignment
- initial folder structure
- refactor: align infoAppVer.php with phpWhoIs structure + platform info files
- refactor: centralise localStorage key names in constants.js (#139)
- refactor: consolidate web app to single appWeb/public_html/ directory
- refactor: extract shared escapeHtml() into js/utils/html.js (#137)
- refactor: modularise Web/PWA into PHP components
- refactor: single canonical songs.json — copy during build/deploy
- refactor: unify Chorus/Refrain — Chorus as primary, Refrain as alias
- refactor: update PHP code for PHP 8.5 compatibility
- refactor: use DIRECTORY_SEPARATOR in all PHP require/include paths
- refactor: use DIRECTORY_SEPARATOR in all path string segments
- refactor: use PHP constants, remove shortcut vars, update wiki + OpenAPI
- refactor: use unique platform-specific Application IDs
- security: add CSRF checks + reduce error info-leak on admin pages
- style(admin): unify song editor + /manage pages with main-site palette

## [0.10.0] — 2026-04-24
- Add Song of the Day with Christian calendar theming (#108)
- Add configurable page transition animations (#106)
- Add display preferences and presentation mode (#95)
- Add import/export for favourites and set lists (#103)
- Add keyboard number quick-jump from any page (#96)
- Add keyboard shortcuts help overlay (#104)
- Add lyrics full-text search with toggle (#93)
- Add missing song request form (#107)
- Add offline status indicator with cached song count (#112)
- Add print-optimised layout for set lists (#113)
- Add recently searched terms with quick re-search (#110)
- Add scroll-linked reading progress indicator on song pages (#109)
- Add side-by-side song comparison view (#102)
- Add smooth scroll-to-top button on long pages (#97)
- Add songbook colour-coding across the entire app (#98)
- Add songbook table of contents with alphabetical index (#111)
- Add transpose / capo indicator for song pages (#101)
- Add update-available notification for users (#83)
- Add worship set list / playlist feature (#94)
- Compose App ID from shared base + platform suffix (#100)
- Fix code review issues: bugs, leaks, color fallbacks
- Fix security vulnerabilities: XSS, cache poisoning
- Fix: Audio/Sheet Music buttons show "coming soon" toast (#80)
- Fix: Favorites clear-all button event listener accumulation (#79)
- Fix: Service worker cache version auto-syncs with app version (#81)
- Implement MIDI audio playback module (#90)
- Implement PDF sheet music viewer module (#91)
- Implement client-side fuzzy search with Fuse.js (#82)
- Implement recently viewed songs history (#92)
- Merge pull request #153 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Merge pull request #158 from MWBMPartners/claude/rebuild-pwa-php-KjLJB
- Precache recently viewed songs in service worker (#105)
- Prepare deep linking for native app, PWA, and browser (#99)
- Whitelist toast notification type parameter
- app refresh upload (early dev 1)
- chore: alphabetise manifest.json properties
- chore: full SQL data export, directory access safeguards
- chore: re-parse songs.json, user migration script, GitHub Wiki sync
- chore: rebuild songs.json from .SourceSongData with BCP 47 language support
- chore: reorder manifest.json — identity fields first, rest alphabetical
- docs(api): add OpenAPI 3.0 specification for all API endpoints (#291)
- docs(api): comprehensive OpenAPI refresh for the 0.10.x alpha batch
- docs(api): cover remaining 37 api.php actions + 5 editor endpoints
- docs(legal): update Privacy Policy and Terms of Use
- docs: refresh in-app help, README, CHANGELOG, DEV_NOTES, .claude memory
- docs: update README, in-app help, and wiki with recent features
- feat(a11y): colour vision deficiency mode selector and high contrast toggle (#319)
- feat(access): CCLI licence validation and tiered content access (#346)
- feat(admin): analytics CSV export + search-query logging (#404)
- feat(admin): analytics dashboard at /manage/analytics (#404)
- feat(admin): backup upload path + audit log entry (#405)
- feat(admin): restore pre-flight summary + transactional data-load + pre-restore snapshot (#405)
- feat(admin): restore-from-backup action on the Setup dashboard (#405)
- feat(admin): runtime entitlements editor at /manage/entitlements (#407)
- feat(admin): song-request triage queue at /manage/requests (#403)
- feat(analytics): add GDPR-compliant consent banner and settings toggle
- feat(analytics): add Matomo and Fathom analytics platform support
- feat(analytics): add unified event tracking module
- feat(api): add 15 new API endpoints for all database features
- feat(auth): cross-subdomain cookie + sliding expiry so sign-in persists (#390)
- feat(auth): enforce registration_mode setting (#236)
- feat(auth): passwordless email login via magic link and 6-digit code (#294)
- feat(auth): promote magic-link as primary sign-in path (#395)
- feat(community): song request submission form + API (#403)
- feat(db): CamelCase naming, user groups, song requests, missing song detection, interactive installer
- feat(db): migrate song data from JSON to MySQL (#270, #271, #272, #273, #274, #275, #276, #277, #278)
- feat(db): organisation support with licensing and content lockout (#326)
- feat(db): web form to configure db_credentials.php from Setup dashboard (#272)
- feat(editor): add arrangement customisation to Structure tab (#161)
- feat(editor): add checkboxes for verified, lyricsPublicDomain, musicPublicDomain (#222, #225)
- feat(editor): bulk tag / move / verify / export in multi-select bar (#399)
- feat(editor): debounced auto-save as safety net (#394)
- feat(editor): multi-select mode + bulk delete (#399 MVP)
- feat(editor): per-song save endpoint + client wiring (closes #394 core)
- feat(editor+admin): revision history UI + diff + restore + /manage/revisions (#400)
- feat(favorites): add batch select mode with bulk operations (#119)
- feat(favorites): add custom tags/categories with filter (#122)
- feat(footer): add 'By Number' button and narrow device adjustments
- feat(header): replace logo with navigation dropdown menu
- feat(home): add quick-access recent songbook tabs (#121)
- feat(infra): database backup script with retention policy (#322)
- feat(misc): let Misc songbook hold unnumbered songs (#392)
- feat(og): add OG preview images for songbooks, setlists, and Android Smart App Banner
- feat(portal): channel gating + /admin alias + richer admin dashboard (#407)
- feat(pwa): bulk audio manifest endpoint + Settings toggle (#401)
- feat(pwa): cache audio + sheet music on-demand for offline playback (#401)
- feat(pwa): generate app icons and screenshot placeholders
- feat(pwa): platform-specific install prompts for all iOS browsers (#175)
- feat(pwa): restore "New version — Refresh" toast on SW update (#396)
- feat(pwa): wire audio pre-cache + per-songbook eviction + real size readout (#401)
- feat(related-songs): add content-based TF-IDF lyric similarity (#118)
- feat(roles+ux): entitlements map, Edit-Song button, clearer verse demarcation (#407)
- feat(search): expand common scripture-reference abbreviations (#397)
- feat(search): scripture-tag-aware search merges curated tag matches (#397)
- feat(security): add Content-Security-Policy header with nonces (#117)
- feat(security): add SRI hashes to CDN resources (#116)
- feat(seo): centre-safe OG layout + contextual song previews (#172, #173)
- feat(setlist): scheduled setlists API endpoints (#398 half)
- feat(setlist): scheduling UI wired + collaboration endpoints + 'Up next' card (#398)
- feat(settings): add Privacy section with analytics consent toggle
- feat(setup): add Drop Legacy Tables action to the Setup dashboard
- feat(share): enhance social share cards with rich metadata (#123)
- feat(shortcuts): add keyboard shortcuts for worship control (#125)
- feat(shortcuts): settings toggle to enable/disable keyboard shortcuts (#406)
- feat(song): add related songs section on song pages (#118)
- feat(song): practice / memorisation mode with progressive masking (#402)
- feat(stats): add usage statistics dashboard page (#120)
- feat(transitions): enhance page transitions with loading bar and staggered animations (#149)
- feat(ui): popular songs, recently viewed, browse by tag on home page (#303, #304, #305)
- feat(ui): search autocomplete suggestions (#307)
- feat(ui): setlist scheduling, templates, PDF export buttons (#300, #301, #302)
- feat(ui): song request submission form (#280)
- feat: Android — Kotlin/Jetpack Compose project setup
- feat: Apple universal app — Xcode project setup (Swift 6.3 / SwiftUI)
- feat: Apple — Widgets (Song of the Day + Recent Favourites)
- feat: BCP 47 language support, email login UI, and magic link routing (#237, #295, #296)
- feat: Phase 1 batch — copyright fix, gestures, permalinks, writer links, SEO, setlists, transitions, title case, songbook sort, colour mode fix
- feat: Song Editor — edit metadata (title, number, songbook, CCLI)
- feat: Song Editor — web-based developer tool UI
- feat: WCAG 2.1 AA accessibility compliance
- feat: Web PWA — MIDI audio playback
- feat: Web PWA — PDF sheet music viewer
- feat: add Miscellaneous songbook (Misc) for non-published songs
- feat: add SQLite database for song storage
- feat: add branded offline fallback page for uncached PWA launches
- feat: add experimental manifest properties + reorder
- feat: add language field (IETF BCP 47) + Add/Delete song buttons
- feat: add verified/publicDomain fields, verified badge, JSON schema (#222-#226)
- feat: admin dashboard & enhanced user management (#260)
- feat: align colour scheme with iLyrics dB + colourblind-friendly mode
- feat: automated Apple app packaging (App Store, TestFlight, direct)
- feat: automated Web PWA build & packaging
- feat: badge contrast fix, song arrangements, SEO enhancements (#159, #160, #151)
- feat: build Web PWA core — Milestone 2 complete
- feat: build song data parser and generate songs.json
- feat: bulk download API reduces 3,612 requests to 6 for offline songs
- feat: centralise song data in data_share/ and add server-side setlist sharing (#154, #155)
- feat: complete PWA rebuild with PHP backend, modern UI, and enhanced features
- feat: complete colour scheme redesign — clean neutral slate
- feat: comprehensive PWA manifest + update Claude context
- feat: comprehensive in-app help system
- feat: deep linking with clean URLs and browser title updates
- feat: disable numpad live search by default, respect default songbook
- feat: download all songs for offline use
- feat: editor song list — title case + songbook badge (#249)
- feat: email login disable, CAPTCHA config, ad platform settings (#339, #340, #341)
- feat: enhanced user account system with role hierarchy, password reset, custom arrangements
- feat: expand Song of the Day seasons and search lyrics (#163)
- feat: fixed header and footer — always visible on screen
- feat: hide PWA install banner when already installed cross-subdomain (#248)
- feat: lyrics copy protection and expanded font size range
- feat: move editor to /manage/ with session-based auth (#227, #228, #229)
- feat: numeric keypad toggle for song number search
- feat: offline song update detection and auto-update (#131, #132)
- feat: per-songbook and per-song offline download options
- feat: physical keyboard support for number search keypad
- feat: project setup — new structure, plan, docs, and song data
- feat: record song views for history tracking (#287)
- feat: redirect to app root on direct include file access
- feat: related songs, export, health dashboard, review workflow, accessibility, rate limit, cleanup (#308-#325)
- feat: replace MWBM Partners Ltd branding with iHymns + cross-domain storage bridge (#133)
- feat: replace native prompt()/confirm() with Bootstrap modals (#114)
- feat: restrict access to private_html/ via HTTP Basic Auth
- feat: shared data/ directory uploaded one level up from SFTP paths
- feat: show full songbook name responsively, abbreviation on narrow screens
- feat: song keys, chords, setlist scheduling/templates/PDF/collab, discovery, preferences, revisions (#298-#316)
- feat: song translation linking, fix auto-scroll iOS, fix PWA offline blank page
- feat: sortable song list — title (default), number, songbook (#251)
- feat: split language into Language, Script, Region fields (#240)
- feat: subdomain cookie sync for *.ihymns.app (#133)
- feat: subtle JSON fallback indicator in footer (Alpha/Beta only)
- feat: unit tests + fix songbook abbreviation badges
- feat: update footer links and enhance copyright display logic
- feat: web-accessible database setup dashboard at /manage/setup-database.php
- fix(auth): Settings Account card now reflects signed-in state consistently
- fix(badges): automated WCAG contrast for songbook number badges
- fix(editor): make Add/Delete song buttons more visible (#238)
- fix(editor): rename 'Save JSON' to 'Save' and clarify DB-first save path
- fix(editor): resolve toolbar button ID mismatches (#235)
- fix(install): skip interactive prompts when run non-interactively
- fix(migrate-users): bind_param type/variable mismatch crashed user insert
- fix(pwa): Safari install banner blank + iOS safe-area support (#174)
- fix(pwa): hide install banner on non-Safari iOS browsers
- fix(pwa): share icon direction + empty default banner HTML (#177)
- fix(security): comprehensive security audit fixes (#293)
- fix(security): hash API tokens and reset tokens with SHA-256 (#292)
- fix(seo): add dynamic OG image for social sharing previews (#170)
- fix(setlist): remove text from Web Share API to prevent broken share links
- fix(settings): disable reduce motion by default so animations are enabled
- fix(setup): don't leak text/plain Content-Type through the Setup dashboard
- fix(sheet-music): add loading guard to prevent rapid-click race condition (#115)
- fix(stats): equalise Most Viewed badge widths regardless of digit count
- fix(sw): prevent caching 301 redirects in navigation handler (#140)
- fix(ui): arrangement badge contrast, tooltips (#266)
- fix(writer): improve name matching with multiple slug variants
- fix: PWA banner — App Store verification, remove broken Smart App Banner (#267)
- fix: PWA bugs — sync bar, badge contrast, chorus/refrain, iOS auto-scroll (#262, #263, #265, #269)
- fix: PWA standalone safe area insets for header, footer, overlays (#267)
- fix: Popular Songs and Browse by Theme work in JSON fallback and offline
- fix: SDAH song number badges unreadable in dark mode (#134)
- fix: Song of the Day not rendering due to async data race (#108)
- fix: SongData.php falls back to JSON when MySQL not configured
- fix: add .htaccess rules for /manage/ and use clean URLs (#227)
- fix: align offline songbook list and show estimated storage sizes
- fix: arrangement badge contrast, tooltips, and compact layout
- fix: auto-scroll iOS support and floating stop button (#267)
- fix: auto-scroll not working on iOS due to sub-pixel rounding
- fix: call skipWaiting() so new SW activates immediately
- fix: clean up Web PWA UI — search bar, icons, songbook colours
- fix: code review and security hardening
- fix: comprehensive code review + security hardening
- fix: correct SQLite database path — dirname 3 not 2 (#253)
- fix: critical editor bugs — form visibility, lyrics/lines, missing fields
- fix: deduplicate concurrent navigation fetches in service worker (#140)
- fix: deploy sync detection + softer songbook card colours
- fix: disable all caching on beta — nuke service worker + cache headers
- fix: editor HTML-to-JS ID mismatches + admin-only user management
- fix: enable PWA offline support on iOS (and all platforms)
- fix: expand public domain detection to include all variant spellings (#225)
- fix: footer text center alignment and bottom safe area on mobile (#267)
- fix: hide .php from all URLs, JSON fallback for SongData
- fix: include data/songs.json inside each SFTP upload directory
- fix: merge duplicate class attribute on search-clear-btn
- fix: migration scripts work via web dashboard (no CLI required)
- fix: offline download continues in background and downloads 4x faster
- fix: prefix copyright notice with "Application" to distinguish from song copyrights (#230)
- fix: preserve SQLite directory and protect DB from git/web access
- fix: put songbook abbreviation inside coloured square in Recent section (#162)
- fix: re-enable service worker + add Cloudflare no-cache headers
- fix: remove CSS background override on songbook card headers
- fix: remove config.php include from service worker to prevent PHP errors
- fix: render song titles in Title Case consistently across all surfaces
- fix: revert version-bump to beta-only, show build timestamp on alpha
- fix: scroll progress bar visibility and safe areas in presentation mode
- fix: scroll-to-top button overlapping footer on all platforms
- fix: separate copyright from writer credits, show in header and footer
- fix: service worker never caches CDN resources — fixes 503 errors
- fix: set Vendor Parent to NULL — MWBM Partners Ltd is the top-level vendor
- fix: setlist sync bar, badge text contrast, admin menu links
- fix: show song numbers in Recent section on home page (#162)
- fix: song editor loads songs.json from canonical data/ location
- fix: song list spacing, version bump regex, pre-release version, lyrics resilience
- fix: split copyright at © in all credit line handlers, zero issues remaining
- fix: temporarily remove CSP header to clear poisoned SW cache
- fix: widen song number badge min-width for consistent alignment
- initial folder structure
- refactor: align infoAppVer.php with phpWhoIs structure + platform info files
- refactor: centralise localStorage key names in constants.js (#139)
- refactor: consolidate web app to single appWeb/public_html/ directory
- refactor: extract shared escapeHtml() into js/utils/html.js (#137)
- refactor: modularise Web/PWA into PHP components
- refactor: single canonical songs.json — copy during build/deploy
- refactor: unify Chorus/Refrain — Chorus as primary, Refrain as alias
- refactor: update PHP code for PHP 8.5 compatibility
- refactor: use DIRECTORY_SEPARATOR in all PHP require/include paths
- refactor: use DIRECTORY_SEPARATOR in all path string segments
- refactor: use PHP constants, remove shortcut vars, update wiki + OpenAPI
- refactor: use unique platform-specific Application IDs
- security: add CSRF checks + reduce error info-leak on admin pages
- style(admin): unify song editor + /manage pages with main-site palette

## [0.10.x] — 2026-04-18 (alpha)

- feat: **auth** — magic-link (email + 6-digit code) is now the primary sign-in path; cross-subdomain `HttpOnly` cookie + 30-day sliding expiry; HttpOnly cookie ITP-resistant persistence
- feat: **admin portal** — `/manage/` dashboard now shows Library + People & Activity stat groups, recent-users panel, quick-link tiles for every admin surface; `/admin/` alias redirects to `/manage/`
- feat: **channel gating** — alpha/beta subdomains gated by `access_alpha` / `access_beta` entitlements; embedded magic-link form on the gate page
- feat: **entitlements system** — granular role→capability map with a `/manage/entitlements` editor; overrides stored in `tblAppSettings`; client-side mirror for UI decisions
- feat: **analytics** — `/manage/analytics` dashboard (7/30/90-day range) with Top Songs, Top Songbooks, Top Search Queries, Zero-Result Queries; per-panel CSV export
- feat: **song requests** — public `/request-a-song` form with rate limit + honeypot; admin triage queue at `/manage/requests`
- feat: **editor per-song save** — `/api?action=save_song` UPSERT endpoint; auto-save now per-song (not full-corpus)
- feat: **editor multi-select** — bulk delete with typed-DELETE safety for ≥10 rows
- feat: **song revision audit log** — `tblSongRevisions` writes on every per-song save
- feat: **scripture search** — abbreviation expansion (`Ps 23` → `Psalm 23`) + tag-based curated match merged to top of results
- feat: **practice mode** — Full / Dimmed / Hidden cycle for memorisation; tap-to-reveal
- feat: **keyboard shortcuts toggle** — Settings → Accessibility lets users turn shortcuts on/off
- feat: **PWA update toast** — "New version — Refresh" reliably appears when SW deploys
- feat: **PWA offline audio** — on-demand caching on playback; `/api?action=bulk_audio` for pre-caching; Settings toggle
- feat: **setlist scheduling API** — `setlist_schedule_set / _clear / _upcoming` endpoints
- feat: **backup restore** — admin UI with server-list and upload-from-device paths; audit log on upload
- feat: **Misc songbook** — songs now permitted without a song number (schema `Number` is NULL-able); book-glyph badge renders instead of `#null`
- feat: **Title Case** rendering everywhere song titles are displayed
- feat: **admin area re-skin** — unified with main-site palette (indigo/violet accent) via shared `css/admin.css`
- fix: **Settings Account card** — now flips live on auth-state change via `ihymns:auth-changed` event bus
- fix: **editor Save button** — renamed from "Save JSON" with clearer DB-first wording
- fix: **Usage Stats badges** — consistent width regardless of digit count
- security: CSRF tokens on all admin POST forms; DB error messages no longer leaked to clients

## [1.0.0] — 2026-04-06
- app refresh upload (early dev 1)
- feat: Android — Kotlin/Jetpack Compose project setup
- feat: Apple universal app — Xcode project setup (Swift 6.3 / SwiftUI)
- feat: Apple — Widgets (Song of the Day + Recent Favourites)
- feat: Song Editor — edit metadata (title, number, songbook, CCLI)
- feat: Song Editor — web-based developer tool UI
- feat: WCAG 2.1 AA accessibility compliance
- feat: Web PWA — MIDI audio playback
- feat: Web PWA — PDF sheet music viewer
- feat: add Miscellaneous songbook (Misc) for non-published songs
- feat: align colour scheme with iLyrics dB + colourblind-friendly mode
- feat: automated Apple app packaging (App Store, TestFlight, direct)
- feat: automated Web PWA build & packaging
- feat: build Web PWA core — Milestone 2 complete
- feat: build song data parser and generate songs.json
- feat: comprehensive in-app help system
- feat: deep linking with clean URLs and browser title updates
- feat: numeric keypad toggle for song number search
- feat: project setup — new structure, plan, docs, and song data
- feat: restrict access to private_html/ via HTTP Basic Auth
- feat: shared data/ directory uploaded one level up from SFTP paths
- feat: unit tests + fix songbook abbreviation badges
- fix: comprehensive code review + security hardening
- fix: deploy sync detection + softer songbook card colours
- fix: set Vendor Parent to NULL — MWBM Partners Ltd is the top-level vendor
- fix: song editor loads songs.json from canonical data/ location
- initial folder structure
- refactor: align infoAppVer.php with phpWhoIs structure + platform info files
- refactor: single canonical songs.json — copy during build/deploy
- refactor: update PHP code for PHP 8.5 compatibility
- refactor: use unique platform-specific Application IDs

## [Unreleased] - 2026-04-05
- fix: default SFTP port to 22 if secret is empty or non-numeric
- refactor: rewrite deploy.yml to match phpWhoIs pattern exactly
- feat: unit tests + fix songbook abbreviation badges
- fix: comprehensive code review + security hardening
- feat: automated Android packaging (Play Store, Amazon, direct APK)
- feat: automated Apple app packaging (App Store, TestFlight, direct)
- feat: automated Web PWA build & packaging
- feat: Web PWA — PDF sheet music viewer
- feat: Web PWA — MIDI audio playback
- refactor: update PHP code for PHP 8.5 compatibility
- fix: set Vendor Parent to NULL — MWBM Partners Ltd is the top-level vendor
- refactor: use unique platform-specific Application IDs
- feat: Android — Full feature parity with Apple app
- feat: Android — Kotlin/Jetpack Compose project setup
- refactor: align infoAppVer.php with phpWhoIs structure + platform info files
- feat: Apple — Widgets (Song of the Day + Recent Favourites)
- feat: Apple — Code signing and notarisation
- feat: Apple — App Store submission preparation
- feat: Apple — Auto-update checker
- feat: Apple — In-app help
- feat: Apple — Share sheet
- feat: Apple �� Spotlight integration
- feat: Apple — Audio playback
- feat: Apple — Favorites system
- feat: Apple — Dark mode support
- feat: Apple — tvOS large-screen lyrics display
- feat: Apple — Search functionality
- feat: Apple — Song detail view
- feat: Apple — Song list and songbook browser
- feat: Apple — Song data model (Codable)
- feat: Apple universal app — Xcode project setup (Swift 6.3 / SwiftUI)
- docs: update PROJECT_STATUS with all completed milestones
- feat: CI test/lint workflow
- feat: GitHub Releases workflow
- feat: automated changelog generation
- feat: automated semver version bumping
- feat: GitHub Actions SFTP deployment pipeline
- feat: Song Editor — bulk import/export
- feat: Song Editor — JSON validation and save
- feat: Song Editor — CCLI number support
- feat: Song Editor — edit writers/composers
- feat: Song Editor — edit song structure/arrangement
- feat: Song Editor — edit metadata (title, number, songbook, CCLI)
- feat: Song Editor — web-based developer tool UI
- feat: comprehensive in-app help system
- feat: align colour scheme with iLyrics dB + colourblind-friendly mode
- feat: WCAG 2.1 AA accessibility compliance
- feat: numeric keypad toggle for song number search
- feat: deep linking with clean URLs and browser title updates
- feat: build Web PWA core — Milestone 2 complete
- feat: build song data parser and generate songs.json
- feat: project setup — new structure, plan, docs, and song data
- app refresh upload (early dev 1)
- initial folder structure
- Update README.md
- Initial commit


# 📝 iHymns — Changelog

> All notable changes to this project are documented here.
> Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
> Versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
> `v1.x.x` = Phase 1 (local JSON) | `v2.x.x` = Phase 2 (iLyrics dB)

---

## [Unreleased]

### 📋 Added — 2026-04-05

- Created `Project_Plan.md` — comprehensive project plan with technology stack, architecture, milestones
- Created `PROJECT_STATUS.md` — project status tracker
- Created `CHANGELOG.md` — this changelog
- Created `DEV_NOTES.md` — developer notes
- Created `README.md` — project overview with structure diagram
- Created `.claude/ProjectBrief.md` — Claude AI context and project brief
- Created `.gitignore` — comprehensive ignore rules for macOS, Windows, Linux, VS Code, Xcode, Node.js, Android
- Created `help/` documentation — getting started, searching, songbooks, favorites, troubleshooting, FAQ
- Analysed existing song data: 5 songbooks, ~7,415 source files
- Documented song data format and structure
- Created `package.json` — Node.js project configuration (v1.0.0-alpha.1)
- Built `tools/parse-songs.js` — song data parser (fully annotated, ~500 lines)
- Generated `data/songs.json` — 3,612 songs parsed (5.22 MB), with structured components, writer/composer credits, audio/sheet music flags
- 31 songs have no lyrics (empty source files — placeholder entries in source data)
- Built Web PWA core (Milestone 2) in `appWeb/public_html/`:
  - `index.php` — main SPA layout with Bootstrap 5.3, responsive navbar, search, dark mode toggle
  - `includes/infoAppVer.php` — centralised version metadata with auto-computed copyright year
  - `css/styles.css` — custom styles: songbook cards, song list, lyrics view, dark mode, animations
  - `css/print.css` — print-optimised stylesheet for song lyrics
  - `js/app.js` — main entry point: data loading, SPA router, module orchestration
  - `js/utils/helpers.js` — shared DOM helpers, text formatting, hash routing, debounce
  - `js/modules/songbook.js` — songbook grid (home) and song list views
  - `js/modules/song-view.js` — song detail view with formatted lyrics, breadcrumb, actions
  - `js/modules/search.js` — Fuse.js fuzzy search (title, lyrics, songbook, writer, number)
  - `js/modules/favorites.js` — localStorage-based favourites management
  - `js/modules/settings.js` — dark mode, PWA install banner, update checker
  - `manifest.json` — PWA manifest for installability
  - `service-worker.js` — offline caching (cache-first for static, network-first for data)
  - `assets/favicon.svg` — SVG favicon (music notes on purple circle)

### 🏗 Changed — 2026-04-05

- Set up new project directory structure: `appWeb/`, `apple/`, `android/`, `tools/`, `data/`, `help/`, `.github/workflows/`
- Configured `appWeb/` with deployment structure: `public_html/`, `data_share/`, `private_html/`
- Added song editor feature to Phase 1 plan (developer tool in `appWeb/private_html/editor/`)
- Added smart install banner feature (PWA install or app store links)
- Added auto-update checking with regular polling
- Added Apple TV remote control feature to Phase 2 plan
- Added automated semver versioning (`v1.x.x` Phase 1, `v2.x.x` Phase 2)
- Added SFTP deployment pipeline design (modelled on phpWhoIs)

### 🗑 Removed — 2026-04-05

- Deleted `appFlutter/` — old Flutter implementation (replaced by native apps)
- Deleted `appAppleIOS/` — old Swift implementation (starting fresh with Swift 6.3/SwiftUI)
- Deleted `backend/` — old PHP API + SQL (replaced by JSON data layer in Phase 1)
- Deleted `notes` — old ChatGPT reference link

### 🎫 GitHub Issues — 2026-04-05

- Created 49 Phase 1 issues (#1–#63, odd numbers + extras)
- Created 14 Phase 2 issues (#2–#30, even numbers)
- Created 7 infrastructure issues (#64–#70) for SFTP deployment, semver, CI/CD
- Created custom labels: `phase-1`, `phase-2`, `platform-web`, `platform-apple`, `platform-android`, `infrastructure`, `data`, `tooling`

---

## Version History

| Version | Date | Summary |
| --- | --- | --- |
| 0.1.0-planning | 2026-04-05 | Project planning, documentation, and structure setup |

---

This changelog is maintained alongside all code changes.
