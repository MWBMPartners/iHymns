# #1769 P2 — resolver + pipeline: implementation blueprint

> Durable blueprint distilled from the sequential Fable P2 design pass (orchestrator-verified against source).
> P2's PRIME INVARIANT: **byte-identical no-op** in both gating-off and gating-on states. The new pipeline is
> wired as DELEGATES that must reproduce today's enforcement branch-for-branch; P3 does the actual emitter
> adoption. Everything here is subordinate to that. `CG`=`includes/content_gating.php`, `CV`=`includes/ccli_validator.php`,
> `ATV`=`includes/access_tier_validation.php`.

## Commit protocol (order is load-bearing — capture goldens from REAL pre-refactor code)
- **A — seam extraction (pure code motion, no behaviour change).** In CG only: move the body of
  `contentGatingApply` after context-resolve (CG:256–349) into `_contentGatingApplyLegacyCore(array $song, string $tier, bool $hasCcli, bool $presenceCcli): array` (rebuild the `$can` closure from $tier/$hasCcli inside), and the media switch (CG:432–434 region) into `_contentGatingMediaAllowedLegacyCore(string $kind, string $tier, bool $hasCcli, bool $presenceCcli): bool`. Public fns call them with values already computed. Verified clean: after CG:253 the core reads only $tier/$hasCcli/$presenceCcli/$song. Existing test-gating-noop + test-media-kind-cap-shared stay green (proof of pure motion).
- **B — golden capture.** `tools/capture-gating-goldens.php` (CLI-only) runs the two legacy cores over the §matrix, writes `tests/php/fixtures/gating-goldens.json` + a `matrixSha`. DB-free deterministic (checkTierAccess→matrix without DB; gatingRulesApply self-gates off). Commit the fixture.
- **C — `includes/licence_registry.php`** (§below) + `test-licence-registry.php`. Nothing consumes it yet.
- **D — TIER_CAPS 5th `enforce` element** (ATV) + `tierCapEnforce()` + extend `test-gating-registry.php`. Additive: existing 4-tuples untouched; frozen-7 get NO enforce; `EnforceJson` enters the union inert.
- **E — the pipeline.** NEW `access_context.php` + `access_resolver.php`; rewrite the two CG delegates; **delete** the `_…LegacyCore` fns (goldens frozen); `test-gating-equivalence.php` + extend `test-gating-noop.php` + structural guards. RUN the mutation checklist (below) before merging.
- **F — the six licence-vocab sites** (§below) + conferral fixture + 3 schema COMMENTs.
- **G — docs/annotations** (Wiki Architecture, project-rules §18 xref, CHANGELOG; annotate every new file ELI5+detailed).
- Deploy step: baseline `manage/gating-noop-verify.php` before deploying E to alpha, verify after (on-server sha closes the CI-has-no-MySQL gap).

## `includes/access_context.php` — the ONE viewer struct
`ACCESS_VIEWER_KEYS` (exact keys, this order): `gatingEnabled`(bool, in-band flag), `userId`(?int), `platform`(string, informational — no P2 decision reads it), `tier`(string, resolveEffectiveTier, ''→public, anon→public), `caps`(array<string,bool> = the six TIER_ACTION_CAP_MAP actions via `checkTierAccess($tier,$action,$hasCcli)`→`!empty(allowed)`, NO presence OR here), `hasCcli`(bool, contentGating_userHasCcli deny-on-error), `licences`(string[] = getUserEffectiveLicenceTypes + presence-injected 'ccli', deny-on-error→[]), `presenceToken`(?string, never reads $_COOKIE), `presenceCcli`(bool), `bypass`(bool, content:gated key).
- `accessViewerContext(?int $userId, string $platform='PWA', ?string $presenceToken=null, ?array $apiKeyScopes=null): array` — statement order: resolve bypass(1) → **bypass short-circuit returns neutral struct (tier=public, caps all-false FAIL-CLOSED, hasCcli/presenceCcli false, licences [])** → tier(3) → hasCcli(4) → presenceCcli(6, verbatim CG:238–253 inner-catch) → licences(5, try/catch→[], inject 'ccli' if presenceCcli) → caps(7). Throw semantics per CG (bypass/tier throws propagate → delegate catch fail-opens; licences throw MUST be internally caught →[] else the whole song fail-opens differently than today).
- `apiKeyScopes`: `null`=self-resolve exactly as CG:214–218 (function_exists guards, ihk_ prefix, apiKeyVerify content:gated); `[]`=NO resolve, zero header reads/side-effects (the MEDIA delegate passes this — today's byte gate has NO bypass branch); non-empty list=pre-verified.
- PURE seam `accessViewerAssemble(bool $gatingEnabled,?int $userId,string $platform,string $tier,bool $hasCcli,array $licenceTypes,?string $presenceToken,bool $presenceCcli,bool $bypass): array` — computes caps via checkTierAccess, injects presence-ccli, stamps all keys. The DB-free test seam.

## `includes/access_resolver.php`
- `accessApplySong(array $song, array $viewer): array` — FIRST LINE `if (empty($viewer['gatingEnabled'])) return $song;` then try{ bypass→return; read caps; `$canViewCopyrighted=caps[view_copyrighted]||presenceCcli`; `$canPlayAudio=caps[play_audio]||presenceCcli`; others plain; **rights-fact READ-READY but inert** (§facts below); `$isCopyrighted = array_key_exists('lyricsPublicDomain',$song) && $song['lyricsPublicDomain']===false`; strip-lyric-body block VERBATIM CG order (unset [components,translations,annotations,vocalParts] loop → contentRestricted=true → restrictionReason [3-way: lyrics_not_available_on_tier / copyrighted_requires_higher_tier / licence_required_for_lyrics(dead in P2)]); media filter via contentGatingMediaKindCap + $capBool VERBATIM; hasAudio/hasSheetMusic flips only if key exists; `offlineAllowed` UNCONDITIONAL; `gatingRulesApply($song,$tier)` LAST } catch → error_log('[content_gating] apply failed: '...) + return $song (part-mutated local — matches CG:350–356).
- `accessMediaAllowed(string $kind, array $songFacts, array $viewer): bool` — `if(empty gatingEnabled) return true;` NO bypass consult (today's byte gate has none); `$cap=contentGatingMediaKindCap($kind); if null return true; return !empty(caps[$cap]) || ($cap==='play_audio' && presenceCcli);` catch→true.
- `accessResolve(array $viewer, array $facts, string $action): array{allowed,reason,upgradeTo,source}` — the P3/P6 primitive, NOT WIRED in P2. Order: gatingEnabled-off→open; bypass→allow; checkTierAccess verdict (reason/upgradeTo passthrough); presence OR for {view_copyrighted,play_audio}; fact AND-gate (lyric family on lyricsRightsLicenceKey, music family on musicRightsLicenceKey, key-membership only, coverage-refine is P6); unknown action→checkTierAccess.
- Require content_gating.php at top of access_resolver (re-entry safe; CG requires context lazily inside fn bodies, no file-scope cycle).

## The delegates (CG rewrite, signatures byte-identical)
`contentGatingApply`: `if(!contentGatingEnabled()) return $song;` require context+resolver; `try{$viewer=accessViewerContext($userId,$platform,$presenceToken);}catch{error_log('[content_gating] apply failed: '); return $song;}` return `accessApplySong($song,$viewer)`.
`contentGatingMediaAllowed`: `if(!contentGatingEnabled()) return true;` require; `try{$viewer=accessViewerContext($userId,'PWA',$presenceToken,[]);}catch{error_log('[content_gating] media gate failed: '); return true;}` return `accessMediaAllowed($kind,[],$viewer)`.
Keep in CG unchanged: contentGatingMediaKindCap, contentGatingEnabled, contentGating_userHasCcli. Call sites unchanged: api.php:989/1164/1285/2028, song-media.php:216, gating-noop-verify.php:132.

## Rights-facts NO-OP proof (triple lock — the deliberate dead branch)
Fact read from PAYLOAD not DB. Locks: (1) SongData does NOT select the columns → key absent on every payload (guard §8.6-g scans SongData.php); (2) no row has a value (P1 NULL DEFAULT NULL, nothing writes); (3) guard `$k!==null && $k!==''`. Only flips true when a future emitter emits a non-empty key naming a licence the viewer lacks = P6 semantics (tested positively so machinery is proven live). Music fact NOT consulted by accessApplySong in P2 (only accessResolve).

## TIER_CAPS 5th `enforce` element (ATV, additive)
Optional index [4]: `['payload'=>[keys],'media'=>[kinds],'actions'=>[names],'requiresCoverage'=>coverageKey]`. `tierCapEnforceShapeValid()` + `tierCapEnforce($k)= tierCapsEffective()[$k][4]??null` (validated). Frozen-7 NEVER get one (guard iterates array_keys(TIER_CAPS) asserting null). EnforceJson: new probe `gatingCapEnforceColumnExists`, `_tierCapsUnionAndEmit` appends `, EnforceJson` when present, `tierCapsMergeUnion` appends [4] only when shape-valid. ZERO production callers of tierCapEnforce in P2 (inert).

## licence_registry.php
`LICENCE_COVERAGE_KINDS = [lyrics_display,lyrics_print,music_reproduction,audio_playback]` + `licenceCoverageKindValid`. `licenceKeyValid = preg_match('/^[a-z][a-z0-9_]{1,29}$/')`. `licenceTypesTableExists($db)` (tierCapsColumnExists shape). `licenceTypesAll(?$db=null)`: existence-gated SELECT (Enabled=1, ORDER BY SortOrder,LicenceKey), drop malformed-key rows, covers=json_decode(assoc) keep valid kinds, ANY failure → `LICENCE_TYPES_FALLBACK`. Readers: licenceType/licenceConfersTier(NULL for both no-row AND confers-nothing — deliberate)/licenceCovers/licenceTypeKeys/licenceTypesForPicker(?$db,$includeNone). `LICENCE_TYPES_FALLBACK` = the 5 P1 seeds byte-derived (ccli→ConfersTier ccli+covers lyrics_display/lyrics_print+RequiresNumber+authority ccli; mrl→null+music_reproduction+ccli; ihymns_basic→free; ihymns_pro→premium; custom→null). Require db_mysql only (no cycle).

## The six vocab sites (Commit F)
1. organisations.php:59 `$LICENCE_TYPES = licenceTypesForPicker($db, includeNone:true)` — picker GAINS mrl+custom (intended).
2. restrictions.php:82 `$RESTRICTIONS_LICENCE_TYPES = licenceTypesForPicker($db)` (no none) — gains mrl/custom targets, drops dead none.
3. my-organisations.php:82 `$LICENCE_TYPES = licenceTypeKeys($db)` — fallback == today's literal exactly.
4. **ccli_validator.php:241 $licenceToTier (ENFORCEMENT-relevant, runs regardless of flag)**: keep literal as FALLBACK; `$confers = try licenceConfersTier($db,$licence) catch null; $mapped = $confers ?? ($licenceToTier[$licence] ?? 'free')`. IDENTITY table (all 9 inputs preserved incl. mrl→free ACCIDENT, custom→free). ⚠ Intended end-state (mrl confers no tier, sub-Q5) NOT shipped in P2 (would change live tiers for orgs holding mrl even gating-off) — one-line flip on the P6 first-enable checklist, OWNER decision; file tracking issue.
5-6. schema.sql:1196/1251 (+1278 tblContentLicences) COMMENTs → "Licence vocabulary token -- registry: tblLicenceTypes (#459/#1769 P2...)". Base-schema (no migrate mirror) so rule #19 not implicated; run schema-coverage + schema-installs anyway.

## Tests/guards (Commit E) + MUTATION CHECKLIST (run break→red→restore before merge)
Golden matrix (capture tool §B): tier×6 (incl. made_up_tier), hasCcli×2, presenceCcli×2, 10 song shapes (full copyrighted / PD / lyricsPublicDomain absent / =null(NOT copyrighted) / malformed media row / media [] / media absent / missing hasAudio+hasSheetMusic / missing all body keys / pre-existing restrictionReason). sha = sha256(json_encode(out, JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES)) — matches gating-noop-verify.php:133.
- `test-gating-equivalence.php` (DB-free): build viewer via accessViewerAssemble(true,...); assert accessApplySong===golden + sha; accessMediaAllowed===golden; fact pos/neg pair (null/''==inert, 'ccli'+licences[]==stripped licence_required_for_lyrics, 'ccli'+['ccli']==inert); accessResolve unit matrix; bypass identity (apply returns input; media IGNORES bypass); off identity.
- extend `test-gating-noop.php`: (11) direct-pipeline off no-op incl. key-absent; (12) assemble keys===ACCESS_VIEWER_KEYS in order; (13) source-order guard (contentGatingEnabled before accessViewerContext, window≥300 chars).
- `test-licence-registry.php`: seed-parity via tests/php/lib/sql_seed_parser.php (LICENCE_TYPES_FALLBACK == schema seeds); conferral fixture (legacyLicenceToTier all 9 inputs == new under fallback); DB part SKIP-without-DB; grammar/covers truth table; malformed-row-drop + throwing-double→fallback via tests/php/lib/mysqli_doubles.php.
- parity guards: registry⇄emit⇄enforce (EnforceJson 5-tuple round-trip); frozen-7-no-enforce (iterate TIER_CAPS keys); kind→cap⇄caps-key alignment (extend test-media-kind-cap-shared tree-wide to cover access_resolver); RequiresCcli-derivation parity (checkTierAccess ccli verdicts; DOCUMENT the tier-name-keyed-not-column-keyed hard gate, file for-consideration issue); viewer-struct mechanism (assemble keys + every $viewer['x'] literal ∈ ACCESS_VIEWER_KEYS); 'gatingEnabled' containment (only access_context + tests); facts-inertness lock (SongData has neither column name); fixture matrixSha integrity.
- MUTATION CHECKLIST: swap the 2 restrictionReason strings; drop array_values from media filter; make offlineAllowed conditional; remove presence OR from play_audio; move delegate bail after context call; change fallback ihymns_pro→'pro'; add enforce to a column cap; make accessMediaAllowed honour bypass; emit lyricsRightsLicenceKey from SongData. Each → the named guard red.

## Scope fence — P2 does NOT: adopt emitters (api.php/song-media/audio-media/checkBulkAccess = P3); collapse song.php fork / auth-forwarding (P3); admin hub / Rights panel / Plans copy (P4); retire feature-gating (P5); write the rights columns / SongData-select them / consume CoversJson in enforcement / flip mrl conferral / register caps / "fix" RequiresCcli hard gate / add bypass to media path (P6). Known intended deltas (PR body): pickers gain mrl+custom, restrictions loses none, 3 schema COMMENTs modernised, 2 new includes, goldens fixture.
