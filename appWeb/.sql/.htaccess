# iHymns — Deny all web access to SQL directory
# Defense-in-depth: this directory should be outside the web root.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
